# 🔐 로그인 시스템 설계도

> **이 문서는 매 작업 시작 전에 반드시 참조한다.**
> 로컬 SQLite → Firebase Firestore 이주 + 로그인/권한 시스템 도입 마스터 플랜.
> 작업이 한 번에 끝나지 않으므로, 모든 단계는 이 설계도에 동기화한다.

---

## 0. 작업 원칙

1. **설계 → 구현** 순서 엄수. 설계도 수정 없이 구현 변경 금지.
2. **단계별 백업** 필수. SQLite 원본 (`problem_bank.db`) 은 절대 손실되면 안 됨.
3. **하위 호환** 유지. 한 단계가 끝나도 GUI 가 그대로 동작해야 함 (점진 이주).
4. **공통 ID/스키마 규칙** 은 `system_architecture.md` 의 규칙(난이도 A/B/C/D, source 코드 등)과 100% 동일하게.
5. 작업 시작 시 → 이 설계도의 "현재 단계" 섹션을 먼저 확인.

---

## 1. 요구사항 (확정본)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 한글 원본 파일 | 관리자가 배포 → 개인 PC 폴더에 보관 (현 방식 유지) |
| 2 | 로컬 → Firebase 이주 | **○** (전체 이주) |
| 3 | 권한 분리 | 관리자 / 일반 사용자 |
| 4 | 데이터 위치 | **데이터 = Firebase, 프로그램 = 로컬** |
| 5 | 문제 풀(pool) | **공통** (모든 사용자 공유) |
| 6 | 시험지 | **개인별** |
| 7 | 선호도 (Good/Soso/Bad) | **개인 / 공통 토글** (이미 구현됨, 검증 완료) |

---

## 2. 현 상태 점검 결과

### 2-1. 선호도 토글 코드 ✅ 정상
- `main_gui.py:1710` `_get_pref_filter_ids()` — scope 가 "mine" / "all" 에 따라 `user_ids` 를 바르게 구성
- `main_gui.py:2516` 라디오 버튼 "내 것만" / "남의 것도"
- `db_engine.py:1522` `get_problem_ids_by_preference(user_ids: list, ...)` — 이미 다중 사용자 지원
- → **이주 후 그대로 재사용 가능**. user_ids 출처만 Firestore 사용자 목록으로 교체.

### 2-2. 사용자 ID 임시값 ⚠
- `main_gui.py:67` `self.current_user_id = "user1"  # TODO: 로그인 구현 시 교체`
- 로그인 시스템 도입 시 이 한 줄이 진입점.

### 2-3. Firebase 코드 (기존)
- `backend/app/db/firebase.py` 가 존재하나 `app.core.config` 미존재 → **사실상 dead code**
- → 다시 처음부터 깨끗이 만든다 (재활용 X).

### 2-4. Firebase 프로젝트 (`naegiwangbank`)
- Firestore: `problems` 컬렉션 (테스트 20건 존재)
- Storage: `naegiwangbank.firebasestorage.app` (이미지 30폴더 17.4MB — 옛날 방식 유물, 사용 안 함)
- → **Storage 는 건드리지 않는다.** 좌표 기반이라 이미지 업로드 불필요.

### 2-5. 로컬 DB 규모
| 테이블 | 행수 |
|---|---|
| problems | 29,451 |
| saved_tests | 161 |
| problem_scores | 2,131 |
| user_directories | 30 |
| problem_preferences | 15 |

---

## 3. Firestore 스키마 설계

### 3-1. 컬렉션 트리

```
users/                       (Firebase Auth UID 를 문서 ID 로)
  {uid}/
    - email
    - display_name
    - role: "admin" | "user"
    - created_at
    - active: boolean

problems/                    (전 사용자 공유 — 공통 풀)
  {prob_id}/
    - source, source_detail, school, year, ...
    - large_unit, middle_unit, unit_code
    - difficulty (A/B/C/D)
    - 좌표 메타 (page, x1, y1, x2, y2 등)
    - 그 외 현 SQLite problems 컬럼 그대로

problem_preferences/         (선호도 — 개인 데이터, 공통 조회 가능)
  {auto-id}/
    - user_id (= uid)
    - problem_id
    - preference: "Good"|"Soso"|"Bad"
    - updated_at
  → 인덱스: (user_id, preference), (problem_id)

problem_scores/              (정답률 — 개인)
  {auto-id}/
    - user_id, problem_id, correct, attempts, updated_at

saved_tests/                 (시험지 — 개인)
  {test_id}/
    - user_id (소유자)
    - title, created_at
    - problem_ids: [...]
    - meta (단원/난이도 분포 등)

user_directories/            (사용자별 폴더/디렉토리 구조 — 개인)
  {auto-id}/
    - user_id, name, parent_id, ...
```

### 3-2. 권한 (Firestore Security Rules 개요)

```
problems/        : read=인증된 사용자, write=admin 만
problem_preferences/ : read=인증된 사용자, write=본인 것만
problem_scores/  : read/write=본인 것만 (admin 은 read all)
saved_tests/     : read/write=본인 것만 (admin 은 read all)
user_directories/: read/write=본인 것만
users/           : read=본인 + admin, write=admin 만
```

---

## 4. 권한 모델

| 기능 | 일반 사용자 | 관리자 |
|---|:---:|:---:|
| 로그인 | ○ | ○ |
| 문제 조회 (problems) | ○ | ○ |
| 문제 등록/수정 (인덱서, 단원분류 등) | **X (메뉴 자체 숨김)** | ○ |
| 본인 시험지 / 선호도 / 점수 | ○ | ○ |
| 다른 사용자 시험지/선호도 조회 | 토글 ON 시 선호도만 | ○ (전체) |
| 사용자 추가/삭제, 역할 변경 | X | ○ |
| 라이선스/Firebase 설정 | X | ○ |

### 4-1. 배포/빌드 정책 (확정: C안 — 단일 빌드 + 이중 잠금)

- **배포 .exe 는 1개** (관리자/일반 동일 빌드)
- **앞단(GUI) 차단**: 일반 사용자에게는 등록/관리 메뉴·탭·버튼이 **렌더링 자체가 안 됨**
  - 코드 패턴: `if self.current_user_role == "admin": <메뉴 생성>`
  - 등록 폼, 인덱서 실행 버튼, 단원분류 수정, 사용자 관리 등 전부 해당
- **뒷단(Firestore Rules) 차단**: 누군가 코드 우회로 호출해도 서버가 거부
  - `problems` 쓰기 = admin 만, `users` 쓰기 = admin 만, 등
- 결과: "안 보이고, 뒤에서도 막힘" 이중 잠금. 분리 빌드 불필요.

### 4-2. role 결정 흐름

```
앱 시작
  ↓
로그인 (Firebase Auth)
  ↓
users/{uid} 문서 조회 → role 필드 읽음
  ↓
self.current_user_id   = uid
self.current_user_role = "admin" | "user"
  ↓
GUI 빌드 시 role 분기로 메뉴 구성
```

`role` 이 없거나 `active=false` 이면 접근 거부 → 로그인 화면으로 복귀.

---

## 5. 마이그레이션 5단계 (Phase Plan)

각 Phase 끝에서 GUI 가 멀쩡히 동작해야 한다 (rollback 가능 상태).

### Phase 1 — 데이터 이주 (problems 만 우선)
- 스크립트: `migrate_problems_to_firestore.py`
- 입력: `problem_bank.db` problems 테이블 29,451 행
- 출력: `problems/{prob_id}` 문서
- 배치: 500 행/batch, idempotent (재실행 안전)
- 검증: 행수 일치 + 표본 10건 필드 일치
- **GUI 는 아직 SQLite 그대로** (조회는 변경 없음, 단순 백업본 생성)

### Phase 2 — 로그인 시스템
- Firebase Authentication (이메일/비밀번호) 활성화
- `users/{uid}` 문서 자동 생성 훅 (또는 가입 후 admin 이 role 부여)
- 로컬 GUI: 로그인 다이얼로그 → 성공 시 `current_user_id = uid`, `current_user_role = role`
- 토큰 캐시: `%APPDATA%\sangsung_math\auth_token.json` (자동 로그인)
- 첫 admin 계정 1개를 콘솔에서 수동 시드

### Phase 3 — 개인 데이터 이주
- 대상: saved_tests / problem_scores / problem_preferences / user_directories
- 매핑 규칙: 기존 `user_id="user1"` → 첫 admin uid 로 일괄 매핑
- 신규 사용자가 들어오면 빈 상태에서 시작
- 스크립트: `migrate_user_data_to_firestore.py`

### Phase 4 — GUI 데이터 소스 전환 (SQLite → Firestore)
- `LocalDBEngine` → `FirestoreEngine` (인터페이스 동일하게 유지하는 게 핵심)
- 메서드 시그니처는 동일. 내부만 Firestore 쿼리로 교체.
- 단계 내 단계: 읽기 전환 → 쓰기 전환 → SQLite 제거
- 캐시 레이어 도입 (29,451 문제를 매번 Firestore 호출하면 비용/속도 모두 ✗)
  - 첫 진입 시 problems 전체 로드 → 메모리 캐시 + 로컬 디스크 캐시 (`%APPDATA%`)
  - 변경 감지: `updated_at` watermark 기반 증분 동기화

#### Phase 4 세부 분할 (2026-04-27 확정)
| 하위 | 목표 | 대상 메서드 | 위험도 |
|---|---|---|---|
| 4-A ✅ | 캐시 인프라 + problems READ | query_counts, search_problems, fetch_random_problems, search_exams_grouped, search_schools, get_subject_unit_codes, get_unique_years, get_problems_by_files, search_mock_exam_brands, get_ids_by_mock_brands, get_unit_stats, get_naesin_score_thresholds, get_all_problem_ids, check_duplicate_exam | ★★★ |
| 4-B ✅ | 개인 데이터 READ (Firestore 직접, 캐시 X) | get_folders, get_tests, get_test_detail, get_all_tests_sorted, get_user_storage_stats, get_preferences_bulk, get_problem_ids_by_preference, get_all_preference_user_ids, get_scores_for_test, get_problem_ids_by_rate, get_problem_ids_with_scores, get_correct_rates_bulk, is_folder_protected, get_protected_folder_id, get_folder_test_count, get_all_subfolder_ids, is_folder_empty | ★★☆ |
| 4-B+ ✅ | main_gui.py uid 와이어링 + SQLite legacy user_id 갱신 | 28곳 `self.current_user_id` 주입 + SQLite 207행 admin uid 통합 + Phase 2-H mis-place 정리 | ★★☆ |
| 4-C ✅ | 개인 데이터 WRITE | save_test, delete_test, create_folder, rename_folder, delete_folder, set_preference, save_scores | ★★☆ |
| 4-D ✅ | problems WRITE (admin) + 캐시 무효화 | update_problem_meta, set_exclusion_status, delete_problems, delete_exam, delete_exams_by_filenames | ★★★ |

#### 핵심 원칙
1. **인터페이스 불변**: 46개 public 메서드 시그니처/반환값 100% 유지 → main_gui.py / hwp_generator.py 무수정
2. **이중 엔진 + 토글**: `LocalDBEngine`(SQLite) 그대로 두고 `FirestoreEngine` 신설. config 플래그(`DATA_ENGINE=sqlite|firestore`) 한 줄로 전환·롤백
3. **캐시 아키텍처**:
   ```
   [GUI] ──read──> [FirestoreEngine] ──hit──> [%APPDATA% problems_cache.sqlite]
                          │                              ▲
                          └─miss──> Firestore ───────────┘
                          └─write──> Firestore ──> updated_at 갱신
   ```
   - 첫 부팅: problems 전체 다운로드 (~30초, 1회)
   - 재부팅: `users/{uid}.last_sync_at` 기준 증분 fetch (보통 0~수십건)
   - admin write 시 로컬 캐시 즉시 갱신
   - 메뉴에 "캐시 새로고침" 추가
4. **개인 데이터(2,339건)는 캐시 X**: 신선도 우선, 호출 빈도 낮음 → Firestore 직접

#### 산출물 예상
- `backend/firestore_engine.py` (신규)
- `backend/data_cache.py` (신규, 캐시 매니저)
- `backend/data_engine.py` (신규, 추상 인터페이스 + 팩토리)
- `backfill_updated_at.py` (1회용)
- `tests/test_engine_parity.py` (SQLite vs Firestore 결과 비교)

#### 위험 / 완화책
- Firestore LIKE 불가 → 캐시(SQLite) 내부에서 LIKE
- RANDOM() 불가 → 필터링 후 클라이언트 `random.sample()`
- WriteBatch 500 한도 → 자동 분할 helper
- updated_at 누락 → 4-A 시작 시 전체 백필 1회

### Phase 5 — 인덱서 직접 쓰기
- 인덱서들 (`naesin_ang.py`, `suneung_special.py`, `suneung_wansung.py`, `mock_exam.py`) 이
  바로 Firestore 에 쓰도록 수정
- 권한: admin 만 가능 (Security Rules 가 보장)
- 결과: 신규 등록 즉시 모든 사용자에게 전파

---

## 6. 캐시 / 성능 전략

문제 29,451 건 + 향후 증가. Firestore 직접 쿼리는 비용·지연 모두 부담.

- **로컬 캐시 파일**: `%APPDATA%\sangsung_math\problems_cache.sqlite`
- 시작 시 `users/{uid}/last_sync_at` 기준 증분 fetch
- 메모리 인덱스 (단원, 난이도, 학교, 년도) 는 GUI 부팅 시 1회 빌드
- 선호도/점수 등 개인 데이터는 실시간 listener 사용 가능 (양 적음)

---

## 7. 보안 / 운영

- `firebase_key.json` (서비스 계정 키) 은 클라이언트에 절대 배포 X
  - 클라이언트는 Firebase Auth 토큰으로만 접근 (Security Rules 가 권한 강제)
  - 서비스 계정 키는 마이그레이션 스크립트 (개발자 PC) 에서만 사용
- `.gitignore` 에 `firebase_key.json`, `*_cache.sqlite`, `auth_token.json` 추가 확인
- 비밀번호 분실 → Firebase 콘솔에서 admin 이 재설정 메일 발송

---

## 8. 미결정/추후 결정 사항

- [ ] 첫 admin 계정 이메일/비번 (선생님이 직접 입력 예정)
- [ ] 오프라인 모드 지원 여부 (지금은 인터넷 필수)
- [ ] saved_tests 의 공유 기능 (선생님 → 학생 배포) — 향후 확장

### 결정 완료
- [x] **2026-04-26**: 배포 빌드 정책 = **C안** (단일 빌드 + 메뉴 숨김 + Firestore Rules 이중 잠금). §4-1 참조.
- [x] **2026-04-26**: 계정 생성 방식 = **관리자 발급 only**. 자가가입 차단. §8-1 참조.

### 8-1. 계정 발급 방식 (확정)

- **모든 계정 (admin / user) 은 관리자가 ID/비번 정해서 발급**
- 자가가입(Sign Up) 화면 **없음**. 로그인 화면만 존재.
- Firebase Authentication 의 "이메일/비밀번호" 사용
  - 단, 가입 API 는 클라이언트에서 호출 불가하게 설정 (admin SDK 로만 생성)
- 관리자 GUI 에 "사용자 관리" 탭:
  - 신규 계정 생성: ID + 임시 비번 + role 선택 → Firebase Auth 계정 + `users/{uid}` 문서 동시 생성
  - 비번 재설정: 관리자가 새 임시 비번 발급
  - 계정 비활성화: `users/{uid}.active = false`
- 첫 admin 1명은 콘솔에서 수동 시드 (Firebase Console + Firestore 직접 입력)
- 일반 사용자는 받은 ID/비번으로 로그인만 가능. 첫 로그인 시 비번 변경 강제 (Phase 2 옵션)

### 8-2. ID 형식 (확정 — 가짜 도메인 통일)

Firebase Auth 의 "이메일/비밀번호" 는 이메일 형식을 강제하지만,
사용자 입장에서는 짧은 ID 만 보여주고 싶다.

**규칙**: 사용자에게는 짧은 ID 만 노출, 내부적으로 가짜 도메인 `@sangsung.local` 부착.

| 사용자 화면 | 내부 (Firebase Auth email) |
|---|---|
| `admin` | `admin@sangsung.local` |
| `kim` | `kim@sangsung.local` |
| `choi` | `choi@sangsung.local` |

- 로그인 입력 처리: `if "@" not in input: input += "@sangsung.local"`
- `users/{uid}` 문서에는 표시용 ID 도 별도 저장 (`display_id: "kim"`)
- 가짜 도메인이라 외부 메일 발송 안 됨 → 비번 분실 시 admin 이 GUI 에서 직접 재설정
- 첫 admin: ID=`admin` / 비번=`123456` (2026-04-27 변경됨)

---

## 9. 현재 단계

> **Phase 5 — 전체 완료 (2026-04-28)**
>
> Phase 4 + Phase 5 모두 완료. 마이그레이션 본 사이클 종료 (★ 데이터 최적화 단계는 별도).
>
> Phase 5 결과:
> - 인덱서가 직접 SQL 을 쓰지 않음 → `HWPMetadataParserV2._post_process_file` 가 활성 엔진(`LocalDBEngine | FirestoreEngine`)의 `ingest_indexed_problems(items)` 한 호출로 일원화.
> - SQLite 모드: `INSERT OR REPLACE` 배치. Firestore 모드: batch upsert (400/chunk) + write-through 캐시 (`cache.upsert_one()`).
> - 잠금/제외 보존: `difficulty_locked` / `unit_code_locked` / `is_excluded` 가 1 인 기존 doc 은 재인덱싱이 덮어쓰지 않음 (사용자 편집 보호).
> - admin only — Firestore Rules + main_gui.py "문제 등록" 탭 자체가 admin 일 때만 빌드 (line 129/134/139).
> - 검증: `tests/test_5_indexer_smoke.py` 29/29 PASS (양 모드). 4-A/4-B+/4-C/4-D 회귀 모두 PASS.

작업할 때마다 이 섹션을 갱신:
- 시작일 / 종료일
- 완료 항목 체크
- 발견한 이슈 / 변경된 결정

### Phase 진척표

| Phase | 상태 | 시작일 | 종료일 | 비고 |
|---|---|---|---|---|
| 0. 설계 | ✅ 완료 | 2026-04-26 | 2026-04-26 | 본 문서 |
| 1. problems 이주 | ✅ 완료 | 2026-04-26 | 2026-04-26 | 29,451건 / 검증 통과. Blaze 업그레이드 완료. |
| 2. 로그인 시스템 | ✅ 완료 | 2026-04-26 | 2026-04-27 | 2-A~2-H 전부 완료. Rules 배포 + 검증 8/8 통과. |
| 3. 개인 데이터 이주 | ✅ 완료 | 2026-04-27 | 2026-04-27 | 2,339건 (saved_tests 162, scores 2131, prefs 16, dirs 30) / 5.8s. 검증 4/4. |
| 4. GUI 데이터 소스 전환 | ✅ 완료 | 2026-04-27 | 2026-04-28 | LocalDBEngine → FirestoreEngine. 4-A 캐시 인프라 + 4-B/B+ READ + 4-C 개인 WRITE + 4-D problems WRITE 모두 완료. write-through 캐시 패턴. |
| 5. 인덱서 직접 쓰기 | ✅ 완료 | 2026-04-28 | 2026-04-28 | parser→engine 라우팅 (`ingest_indexed_problems`). SQLite/Firestore 모드 일관, write-through 캐시, lock/exclude 보존. smoke 29/29 PASS. |

### Phase 1 작업 기록 (2026-04-26)

- **백업**: `problem_bank.db.bak_pre_migration_20260426_155650` (20.4 MB)
- **스크립트**: `migrate_problems_to_firestore.py` (idempotent, batch=500, 6가지 모드)
- **기존 테스트 데이터**: 20건 삭제 (옛 이미지 방식 스키마, 신규 좌표 방식과 비호환)
- **이주 결과**: 29,451건 / 79.1초 / 372 docs/s
- **요금제 전환**: Spark → Blaze (일일 20K writes/day 한도 때문)
- **검증**: count 일치 ✓ + 표본 5건 전 필드 일치 ✓
- **이슈 발견 (별도 점검 필요)**: source 코드 `NAESIN_A`, `NAESIN_N` 이 system_architecture.md 에 미문서화 — 이주는 그대로 보존, 추후 표준화 결정 필요

---

## 10. 데이터 최적화 (별도 단계 — 상세 미정)

> **DB 데이터량이 커서 (29,451건+ 문제 + 향후 증가) 최적화 작업이 별도로 필요하다.**
> 이번 마이그레이션 사이클 안에 반드시 다루되, 상세 결정은 해당 시점에 한다.

### 다룰 것으로 예상되는 항목 (체크리스트, 결정 시점에 확정)
- [ ] 캐시 전략 강화 (로컬 SQLite 미러 + 증분 sync — §6 참조)
- [ ] Firestore 복합 인덱스 (단원·난이도·소스·년도 조합 쿼리)
- [ ] 불필요 필드 제거 (`search_text`, `file_hash` 등 GUI 가 안 쓰는지 검토 → 저장/전송 비용↓)
- [ ] 미사용/중복 정리 (`is_excluded=1` 영구 삭제 검토, orphan 데이터)
- [ ] 페이지네이션 / Lazy load (한 번에 29K 다 안 받기)
- [ ] 압축 (로컬 캐시 gzip)
- [ ] 비용 모니터링 (Blaze 청구서 확인 → 핫스팟 식별)

### 진행 시점
- Phase 4 (GUI 데이터 소스 전환) 진입 직전 또는 도중에 다룰 가능성이 가장 높음
- 그 전이라도 명백한 핫스팟 (예: 문제 목록 매번 전체 다운로드) 발견 시 즉시 처리

---

## 11. 참조

- 시스템 아키텍처: `system_architecture.md`
- 단원 분류: `단원분류_기술서.md`, `memory/unit_classification.md`
- 현재 작업 로그: `CURRENT_WORK.md`
- 안전 규칙: `SAFETY_RULES.md`
