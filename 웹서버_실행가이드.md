# 🚀 상승 Solution 웹 서버 - 실행 가이드

**최종 업데이트**: 2026-05-23

---

## ⚠️ 운영 역할 분리 (2026-05-23 확정)

| 기능 | 거점 | 진입점 |
|---|---|---|
| **문제 출제** | 🌐 **이 웹 서버** | 브라우저에서 http://localhost:8000 |
| **문제 등록 (인덱싱)** | 💻 **로컬 GUI** | `python backend/main_gui.py` |

→ **이 가이드는 출제 서버 실행 전용**입니다. 새 시험지 인덱싱은 로컬 GUI로 진행하세요.
→ 자세한 정책: `서버클라이언트_설계도.md` § 0-B

---

## 📋 빠른 시작 (3단계)

### 1️⃣ **첫 실행 (의존성 설치)**
```bash
더블클릭: 서버클라이언트시작.bat
```
- Python, FastAPI, Uvicorn 등 자동 설치
- 의존성 설치 후 서버 자동 시작
- 브라우저 자동 열림 (`http://localhost:8000`)

### 2️⃣ **정상 작동 확인**
- 브라우저에 로그인 화면이 나타나면 ✅ 성공
- 검은색 "서버" 창이 떠 있으면 서버 실행 중

### 3️⃣ **종료**
- 검은색 서버 창에서 `Ctrl+C` 입력 → 서버 종료
- 또는 `Alt+F4` (서버 창)

---

## 🛠️ 배치 파일 가이드

| 파일 | 용도 | 언제 사용 |
|------|------|---------|
| **서버클라이언트시작.bat** | 🎯 **기본** | 매번 이것 실행 |
| 진단_웹서버.bat | 🔍 문제 진단 | "안 먹는다" 할 때 |
| 포트정리.bat | 🗑️ 포트 충돌 해제 | "포트 8000 이미 사용" 에러 |

---

## ⚠️ 자주 나는 에러 & 해결

### ❌ "Python을 찾을 수 없습니다"
**원인**: Python이 설치되지 않았거나 PATH에 없음

**해결**:
1. [Python 3.9+ 다운로드](https://www.python.org/downloads/)
2. 설치 시 **"Add Python to PATH"** 반드시 체크 ✅
3. 컴퓨터 재시작
4. 다시 시도

### ❌ "포트 8000 이미 사용 중"
**원인**: 다른 프로그램이 8000 포트 점유

**해결**:
```bash
더블클릭: 포트정리.bat
```
또는 수동:
```bash
netstat -ano | findstr :8000    # PID 확인
taskkill /PID [PID번호] /F      # 종료
```

### ❌ "FastAPI / Uvicorn 에러"
**원인**: 의존성 미설치

**해결**:
```bash
명령 프롬프트 열기 (Win+R → cmd)
cd G:\문제은행\문제은행2
pip install -r requirements.txt
```

### ❌ "server.main 모듈을 찾을 수 없음"
**원인**: 현재 디렉토리 잘못 지정

**확인**:
- `서버클라이언트시작.bat` 안의 경로가 맞는지 확인
- 일반적으로: `G:\문제은행\문제은행2`

### ❌ "로그인 화면이 안 나옴"
**원인**: Firestore 연결 실패 (Firebase 키 누락)

**확인**:
1. `backend/firebase_init.py` 파일 존재 확인
2. Firebase 서비스 계정 키 파일 확인
3. `GOOGLE_APPLICATION_CREDENTIALS` 환경변수 확인

---

## 🔧 고급: 수동 실행

```bash
# 1. 터미널 열기 (Win+R → cmd)
# 2. 폴더 이동
cd G:\문제은행\문제은행2

# 3. 의존성 확인 (필요 시만)
pip install -r requirements.txt

# 4. 서버 시작
set NAEGIWANGBANK_SESSION_SECRET=dev-secret-test
python -m uvicorn server.main:app --host 0.0.0.0 --port 8000 --reload

# 5. 브라우저 열기
http://localhost:8000
```

---

## 📊 서버 상태 확인

### 정상 실행 신호 ✅
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete
```

### 포트 상태 확인
```bash
netstat -ano | findstr :8000
```

### 프로세스 확인
```bash
tasklist | findstr python
```

---

## 🌐 외부 접속 (선택사항)

### Cloudflare Tunnel 사용
```bash
# 1. cloudflared 설치
choco install cloudflared

# 2. Tunnel 설정
cloudflared tunnel create solutio
cloudflared tunnel route dns solutio solutio.risingmath.kr

# 3. Tunnel 실행
cloudflared tunnel run solutio
```

### 외부 접속 URL
```
https://solutio.risingmath.kr
```

---

## 📝 로그 확인

### 진단 로그
```
진단_웹서버.log
```

### 서버 실행 로그 (실시간)
- 검은색 서버 창의 터미널 메시지 확인

---

## 🚀 개발 모드 vs 운영 모드

### 개발 모드 (현재)
```
--reload        # 파일 변경 시 자동 재시작
--host 0.0.0.0  # 모든 인터페이스 공개
workers 1       # 단일 워커 (HWP COM 직렬화)
```

### 운영 모드 (나중)
```bash
python -m uvicorn server.main:app --host 0.0.0.0 --port 8000 --workers 1
```

---

## 🔗 관련 문서

- [FastAPI 공식 문서](https://fastapi.tiangolo.com/)
- [Uvicorn 공식 문서](https://www.uvicorn.org/)
- [Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/)

---

## 💡 팁

1. **서버 창 유지** — 최소화만 하고 닫지 말 것
2. **정기 재시작** — 메모리 누수 방지 (하루 1회)
3. **로그 모니터링** — 에러 발생 시 서버 창 메시지 확인

---

**문제 해결 체크리스트**:
- [ ] Python 3.9+ 설치 확인
- [ ] `서버클라이언트시작.bat` 관리자 권한 실행
- [ ] 포트 8000 충돌 확인 (포트정리.bat)
- [ ] `진단_웹서버.bat` 실행 후 [X] 항목 해결
- [ ] 방화벽 포트 8000 허용 확인
