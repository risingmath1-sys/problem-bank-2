# 🔍 AnG 엔진 기술적 분석 보고서

## 1. 개발 난이도 평가

### Phase 1: 스캔 엔진 ⭐⭐⭐⭐ (난이도: 높음)

**난이도가 높은 이유:**
- **HWP API의 불안정성**: `pywin32`를 통한 HWP COM 제어는 예측 불가능한 동작이 많음
- **마커 검색의 복잡성**: ★S/★E를 찾는 과정에서 무한루프, 커서 이동 오류 발생 가능
- **높이 측정의 정밀도**: HwpUnit → mm 변환 공식의 정확도 검증 필요
- **미주 번호 추출**: HWP 문서 구조에 따라 미주 위치가 달라질 수 있음

**예상 문제점:**
```python
# 이런 코드가 무한루프에 빠질 가능성 높음
while hwp.HAction.Execute("RepeatFind", ...):
    # 커서가 움직이지 않으면?
    # 같은 위치를 계속 찾으면?
```

### Phase 2: DB 스키마 및 회원 관리 ⭐⭐ (난이도: 보통)

**상대적으로 쉬운 이유:**
- Firebase SDK는 문서화가 잘 되어 있음
- Authentication, Firestore 모두 검증된 기술
- 파일명 동기화는 단순 파일 시스템 스캔

**주의사항:**
- 권한 시스템(Custom Claims) 설정 시 보안 고려 필요

### Phase 3: 자동 배치 및 출제 시스템 ⭐⭐⭐⭐⭐ (난이도: 매우 높음)

**가장 어려운 이유:**
- **2단 자동 배치 로직**: 높이 계산 + 단 넘김 판단이 매우 복잡
- **복사/붙여넣기 안정성**: 수식, 그림, 미주를 포함한 복사가 제대로 될지 불확실
- **여러 HWP 파일 동시 제어**: 원본 파일 열기 → 복사 → 시험지에 붙여넣기 과정에서 HWP 인스턴스 충돌 가능
- **실시간 리스너**: 웹-로컬 프로그램 간 동기화 타이밍 이슈

---

## 2. 시스템 안정성 평가

### 🔴 높은 리스크 영역

#### A. HWP COM 제어의 불안정성
**문제:**
- HWP는 GUI 프로그램이라 백그라운드 자동화에 취약
- 보안 경고, 팝업 등으로 인한 중단 가능성
- 메모리 누수, 프로세스 좀비화 위험

**해결 방안:**
```python
# 타임아웃 설정
import signal
def timeout_handler(signum, frame):
    raise TimeoutError("HWP 작업 시간 초과")

signal.signal(signal.SIGALRM, timeout_handler)
signal.alarm(30)  # 30초 제한

try:
    # HWP 작업
    hwp.HAction.Execute(...)
finally:
    signal.alarm(0)  # 타이머 해제
    hwp.Quit()  # 반드시 종료
```

#### B. 높이 측정의 정확도
**문제:**
- HwpUnit → mm 변환이 부정확하면 2단 배치 시 문제가 페이지 하단에서 잘림
- 여백, 단 간격, 헤더/푸터 높이를 정확히 계산해야 함

**해결 방안:**
- 실제 테스트 파일로 반복 검증
- 안전 마진(예: 5mm) 추가하여 여유 공간 확보

#### C. 파일 결손 처리
**문제:**
- 회원이 파일을 삭제했거나 이동시킨 경우
- 파일명이 변경된 경우

**해결 방안:**
- 에러 로그 상세 기록
- 결손 파일은 건너뛰고 계속 진행
- 사용자에게 명확한 오류 메시지 제공

### 🟡 중간 리스크 영역

#### D. Firebase 무료 티어 제한
**제한 사항:**
- Firestore: 읽기 50,000/일, 쓰기 20,000/일
- Storage: 1GB, 다운로드 10GB/월
- Hosting: 10GB/월

**예상 사용량:**
- 문항 1000개 × 검색 100회/일 = 100,000 읽기/일 ⚠️ **초과 가능**

**해결 방안:**
- 클라이언트 측 캐싱 적극 활용
- 검색 쿼리 최적화 (인덱스 활용)
- 필요 시 유료 플랜 전환 ($25/월)

### 🟢 낮은 리스크 영역

#### E. React 웹 대시보드
- 검증된 기술 스택
- UI/UX 구현은 상대적으로 안정적

---

## 3. 개발 병목 구간 분석

### 🚨 Critical Path (병목 1순위)

#### **HWP API 제어 로직 (Phase 1 + Phase 3)**

**왜 병목인가:**
- 모든 기능의 핵심이 HWP 자동화에 의존
- 한 번 잘못 설계하면 전체 시스템이 불안정
- 디버깅이 매우 어려움 (HWP 내부 상태를 직접 볼 수 없음)

**예상 소요 시간:**
- 마커 검색 로직 안정화: 1주
- 높이 측정 정확도 검증: 1주
- 복사/붙여넣기 로직: 2주
- **총 4주** (전체 개발 기간의 40%)

**돌파 전략:**
1. **작은 단위로 테스트**: 한 번에 하나씩 검증
2. **로그 상세 기록**: 모든 HWP API 호출을 로깅
3. **예외 처리 철저히**: try-except로 모든 HWP 작업 감싸기
4. **프로세스 격리**: 각 작업마다 HWP 프로세스 새로 시작

### ⚠️ Secondary Bottleneck (병목 2순위)

#### **2단 자동 배치 알고리즘 (Phase 3)**

**왜 병목인가:**
- 높이 계산 오류 → 문제 잘림 → 사용 불가능한 시험지
- 엣지 케이스가 많음 (긴 문제, 짧은 문제, 그림 포함 등)

**예상 소요 시간:**
- 기본 로직 구현: 1주
- 엣지 케이스 처리: 1주
- **총 2주**

**돌파 전략:**
1. **시뮬레이션 먼저**: HWP 없이 높이 데이터만으로 배치 로직 검증
2. **안전 마진 추가**: 계산된 높이보다 5mm 여유 확보
3. **수동 조정 옵션**: 자동 배치 실패 시 사용자가 수동으로 조정 가능

### 📊 병목 구간 타임라인

```
Week 1-2:  [████████] Phase 1 - 마커 검색 + 높이 측정 (병목!)
Week 3-4:  [████] Phase 1 - Firebase 연동
Week 5-6:  [██] Phase 2 - 회원 관리
Week 7-9:  [████████████] Phase 3 - 자동 배치 (병목!)
Week 10:   [██] Phase 3 - 웹 대시보드
```

---

## 4. 리스크 완화 전략

### 전략 1: HWP 안정성 강화
```python
class SafeHwpController:
    def __init__(self):
        self.hwp = None
        self.max_retries = 3
        
    def execute_with_retry(self, action, *args):
        for attempt in range(self.max_retries):
            try:
                return action(*args)
            except Exception as e:
                if attempt == self.max_retries - 1:
                    raise
                self.restart_hwp()
                
    def restart_hwp(self):
        if self.hwp:
            self.hwp.Quit()
        self.hwp = win32.gencache.EnsureDispatch("HWPFrame.HwpObject")
```

### 전략 2: 점진적 개발
- MVP(Minimum Viable Product) 먼저 완성
- 한 번에 하나의 기능만 추가
- 각 단계마다 충분한 테스트

### 전략 3: 대안 시나리오 준비
- **Plan A**: HWP COM 제어 (현재 설계)
- **Plan B**: HWP → PDF 변환 후 이미지 추출 (안정성 우선)
- **Plan C**: 수동 작업 + 부분 자동화 (최소 기능)

---

## 5. 최종 평가

### 개발 난이도: ⭐⭐⭐⭐ (5점 만점에 4점)
- HWP API 제어가 핵심 난관
- 웹/DB는 상대적으로 쉬움

### 시스템 안정성: ⭐⭐⭐ (5점 만점에 3점)
- HWP 의존성으로 인한 불안정성 존재
- 충분한 예외 처리와 테스트로 완화 가능

### 예상 개발 기간: **10주** (약 2.5개월)
- Phase 1: 4주
- Phase 2: 2주
- Phase 3: 4주

### 성공 확률: **70%**
- HWP API 안정화에 성공하면 → 90%
- HWP API 문제 발생 시 → 50% (대안 필요)

---

## 6. 권장 사항

### ✅ 즉시 시작 가능
1. Phase 1 개발 시작
2. 테스트 HWP 파일 3-5개 준비 (★S/★E 마커 수동 삽입)
3. 기본 HWP 제어 로직 검증

### ⚠️ 주의 필요
1. HWP API 안정성 모니터링
2. 높이 측정 정확도 반복 검증
3. 에러 로그 상세 기록

### 🔄 유연성 확보
1. Plan B(PDF 방식) 준비
2. 수동 보정 기능 추가
3. 단계별 사용자 피드백 수렴

---

**결론**: 도전적이지만 실현 가능한 프로젝트입니다. HWP API 제어가 가장 큰 변수이므로, Phase 1에서 이를 철저히 검증하는 것이 성공의 열쇠입니다.
