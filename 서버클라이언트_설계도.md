# 서버-클라이언트 시스템 설계도

> 본 문서는 데스크탑 .exe → 학원 내 윈도우 서버 + 브라우저 클라이언트 전환 설계만 다룬다.
> 진행 상황은 `서버클라이언트_작업현황.md` 참조.
> 기존 시스템 정보는 `system_architecture.md`, `로그인시스템설계도.md`, `배포용설계도.md` 참조.

---

## ⚠️ 0-A. 동등성 원칙 (Parity Doctrine, 2026-05-04 사용자 지시)

> **이 섹션은 본 프로젝트의 최우선 원칙이다. 다른 모든 결정은 이 원칙에 종속된다.**

### 원칙
1. **로컬 프로그램(`backend/main_gui.py` 6,902줄)이 정답이다.**
   - 매우 심혈을 기울여 만든 프로그램이며, 모든 디테일이 의도된 것이다.
   - 서버-클라이언트 시스템은 **로컬 프로그램과 99% 동일하게** 작동해야 한다.
   - 새 발상으로 "개선"하거나 "단순화"하지 않는다. 의문이 생기면 로컬 동작을 그대로 모사한다.

2. **현재 동등성 평가: 약 30% 일치 (2026-05-04 기준).**
   - 메모리에 적혀 있던 "server/ ~80% 완성"은 *코드 줄 수* 기준이지 *기능 동등성* 기준이 아니다.
   - 진척 보고는 항상 **동등성 기준**으로 한다 — 줄 수, 라우트 수, 템플릿 수가 아니라 **로컬과 동일하게 작동하는 항목 수 / 전체 항목 수**.

3. **서버-클라이언트 작업 중 로컬 시스템(`backend/main_gui.py` 등 데스크탑 GUI 코드)은 손대지 않는다.**
   - 별도 명시 지시가 있을 때만 로컬을 건드린다.
   - 공유 모듈(`backend/hwp_core.py`, `backend/hwp_generator.py`, `backend/firestore_engine.py` 등)을 수정해야 할 경우, **로컬에서도 그대로 동작해야 함**을 검증한다 — 인터페이스 호환 유지.

### 동등성 검증 절차
- 어떤 기능이든 서버에 구현하기 전에 **로컬에서 동일 기능 코드 위치를 먼저 읽는다**.
  - 탭 1 (랜덤 출제) — `backend/main_gui.py` 의 어느 라인 범위?
  - 탭 2 (원본 출제) — 어느 라인 범위?
  - 등등
- 구현 후에는 **체크리스트로 1:1 비교** — 필터 항목, 옵션, 버튼, 표시 컬럼, 정렬 순서, 기본값 등.
- 차이가 발견되면 **로컬 동작을 정답으로** 본다.

### 진척 보고 형식
- ❌ "server/ 1,474줄 작성 완료" — 의미 없음
- ✅ "랜덤출제 탭 우측 패널 7개 메뉴 중 4개 동작 (소스/난이도/형식/년도) — 3개 미완 (최신기출우선/제외/상세필터)"

### 동등성 체크리스트 위치
- `서버클라이언트_작업현황.md` 에 **동등성 체크리스트** 별도 섹션 유지.
- 항목 단위로 ✅/⚠️/❌ 표시. 체크리스트 통과 = 동등성 99% 달성 = 출시 가능.

### 출시 정책 (2026-05-05 사용자 지시)
- **MVP / Phase 단계 구분 없음.** 모든 탭(0~6)을 동시에 99% 동등성으로 구현한다.
- 부분 출시(랜덤출제만 먼저) 안 한다 — 새 선생님이 실제 업무에서 쓰는 전 흐름이 동작해야 출시.
- 진척 보고는 탭별 동등성 % 와 전체 가중 평균.

---

## ⚠️ 0-B. 운영 역할 분리 정책 (2026-05-23 사용자 지시)

> **서버 이전 후 확정 정책. 기능별로 코드 거점이 다르다.**

### 역할 분리

| 기능 | 거점 | 진입점 | 비고 |
|---|---|---|---|
| **문제 출제** | 🌐 **SC (학원 서버)** | `server/` — FastAPI + HTMX + Firestore 캐시 | 랜덤출제 / 원본출제 / 시험지 관리 / 출제 결과 |
| **문제 등록 (인덱싱)** | 💻 **로컬** (학원 PC) | `backend/main_gui.py` + HWP COM | 신규 시험지 HWP 파일 인덱싱 → Firestore 업로드 |

### 결정 근거
- 출제는 다수 선생님(맥/윈도우 무관)이 브라우저로 접근 → 서버
- 인덱싱은 HWP COM (Windows 전용) + 관리자 1명만 수행 → 로컬 GUI 가 빠르고 안정적

### 코드 거점 정책
- **인덱싱 관련 코드 (`backend/main_gui.py` 의 문제 등록 탭, `backend/hwp_metadata_parser_v2.py`, `backend/indexers/*`, `backend/curriculum_config.json`, `backend/unit_hierarchy.json`)** 은 **로컬 위주로** 잘 손봐야 함.
  - 서버는 같은 `backend/` 를 import 만 함 — 변경 시 양쪽 동시 영향이지만 _실행 거점은 로컬_
  - 서버 등록 라우트(`server/routes/api_register.py`)는 _원격 트리거_ 용도로만 유지 (관리자 편의)
- **출제 관련 코드 (`server/routes/api_exam.py`, `server/templates/random_exam.html` 등)** 은 **SC 위주로** 손봐야 함.
- 둘 다 공유하는 모듈 (`backend/firestore_engine.py`, `backend/data_cache.py`, `backend/hwp_generator.py`) 은 양쪽 호환 유지.

### 단원 매핑 시스템 (2026-05-23 정상화)
- **현재 정책**: `unit_hierarchy.json` 단일 코드 체계 (2015 ↔ 2022 사실상 통합). 변환 불필요.
- **자세한 내용**: `system_architecture.md` 의 "단원 코드 체계 (2026-05-23 업데이트)" 섹션 참조.
- **재발 방지 안전망**: `audit_db.py` 정합성 검사, `_report_mapping_failures()` 자동 보고, `bulk_update_field()` 안전 헬퍼.
- **인덱싱 코드 위치**: `backend/indexers/naesin_n_unit_map.json` 등 — **로컬에서 우선 손볼 것**.

---

## 0. 전환 배경 (2026-05-02 결정)

### 동기
- 새 선생님 노트북이 **맥** → 윈도우 전용 .exe 사용 불가
- HWP COM API는 윈도우 전용 (한글 프로그램이 윈도우만 지원)
- 맥에서 네이티브 HWP 자동화는 불가능 → 서버에서 처리 후 결과만 전달

### 핵심 발상
- **HWP COM이 돌아가는 윈도우 PC를 학원에 1대 두고, 모든 사용자는 브라우저로 접속**
- 출제 결과 HWP 파일은 브라우저로 다운로드
- 맥/윈도우/태블릿 무관하게 동일 환경

### 폐기/연기된 결정
- ~~PyInstaller .exe 배포 (배포용설계도.md)~~ → 폐기
- ~~admin SDK 키 분리(A안) Phase 6 Web SDK 전환~~ → 서버에서만 키 보유로 자연 해결

---

## 1. 결정 사항 요약 (2026-05-02)

| 항목 | 결정 |
|---|---|
| 서버 프레임워크 | **FastAPI** (Python) |
| 프론트 기술 | **HTMX** (서버 렌더 + 부분 갱신) |
| 데이터베이스 | **Firestore 유지** (이미 29,451건 마이그레이션 완료) |
| 외부 접속 | **Cloudflare Tunnel** (학원 내 + 외부 동시) |
| 인증 | **기존 Firebase Auth 그대로** (id_token 검증) |
| 동시 사용자 | **3명** (HWP COM 큐로 직렬화) |
| 권한 | **admin / user 2단계 유지** |
| HWP 원본 | **서버 PC에 보관** (모든 출제는 서버에서 수행) |
| 출제 결과 | **브라우저 즉시 다운로드** |
| 데드라인 | **2026-05-09** — 새 선생님 사용 시작 (전 기능 동등성 99%) |
| 테스트 방식 | 사용자(risingmath1)는 보고만 받음, 개발자 검증 후 보고 |
| 병행 운영 | 윈도우 선생님은 기존 .exe 계속 사용 (Firestore 동기화) |

---

## 2. 시스템 아키텍처

### 전체 구조
```
┌─────────────────────────────────────────────────────────────┐
│ 사용자 (맥/윈도우 무관)                                     │
│   ┌───────────────┐  ┌───────────────┐  ┌──────────────┐   │
│   │ 새 선생님 맥  │  │ 기존 선생님 PC│  │ 사용자(학생) │   │
│   │  Safari/Chrome│  │  Chrome/Edge  │  │ Chrome/Edge  │   │
│   └───────┬───────┘  └───────┬───────┘  └──────┬───────┘   │
└───────────┼───────────────────┼─────────────────┼───────────┘
            │                   │                 │
            └───────HTTPS───────┴─────────────────┘
                                │
                  ┌─────────────▼──────────────┐
                  │   Cloudflare Tunnel        │
                  │   (무료, 외부→내부 터널)   │
                  └─────────────┬──────────────┘
                                │ (학원 안에서는 직접 LAN 접속)
                  ┌─────────────▼──────────────┐
                  │   학원 윈도우 서버 PC      │
                  │  ┌──────────────────────┐  │
                  │  │  FastAPI (uvicorn)   │  │
                  │  │  - HTMX 페이지 렌더  │  │
                  │  │  - REST API          │  │
                  │  │  - Firebase Auth 검증│  │
                  │  └──────┬───────────────┘  │
                  │         │                  │
                  │  ┌──────▼───────────────┐  │
                  │  │  HWP 출제 큐 워커    │  │
                  │  │  asyncio.Queue       │  │
                  │  │  → hwp_generator.py  │  │
                  │  │     (기존 코드 재사용)│ │
                  │  └──────┬───────────────┘  │
                  │         │                  │
                  │  ┌──────▼───────────────┐  │
                  │  │  한글(HWP) COM       │  │
                  │  │  source + target 2개 │  │
                  │  │  (기존 구조 유지)    │  │
                  │  └──────────────────────┘  │
                  │                            │
                  │  📂 HWP 원본 파일들        │
                  │  📂 출제 결과 폴더         │
                  └─────────────┬──────────────┘
                                │
                  ┌─────────────▼──────────────┐
                  │  Firestore (Google Cloud)  │
                  │  - users (인증/권한)       │
                  │  - problems (29,451건)     │
                  │  - saved_tests             │
                  │  - exclusion_settings      │
                  └────────────────────────────┘
```

### 컴포넌트 책임 분리

| 컴포넌트 | 책임 |
|---|---|
| 브라우저 클라이언트 | UI 렌더 (HTMX 부분 갱신), 파일 다운로드 |
| Cloudflare Tunnel | 외부 → 내부 HTTPS 터널, DDoS 방어 |
| FastAPI 서버 | HTTP 라우팅, Firebase Auth 검증, 큐 관리, 정적 파일 |
| HWP 워커 | 큐에서 작업 1개씩 꺼내 hwp_generator로 출제 |
| HWP COM (한글) | 실제 한글 파일 조작 (source/target 2-인스턴스 유지) |
| Firestore | 메타데이터 저장소 (기존 그대로) |

---

## 3. 기술 스택

### 백엔드
- **Python 3.11+** (현재 버전 그대로)
- **FastAPI** — 비동기 HTTP 서버, 자동 OpenAPI 문서
- **uvicorn** — ASGI 서버 (FastAPI 구동)
- **Jinja2** — HTML 템플릿 (HTMX와 함께)
- **firebase-admin** — Firestore + Auth (기존 그대로)
- **pywin32 / pyhwpx** — HWP COM (기존 그대로)

### 프론트엔드
- **HTMX** — `hx-get`, `hx-post`, `hx-target` 으로 부분 갱신
- **Tailwind CSS** (CDN) — 빠른 스타일링
- **순수 HTML** + 약간의 JavaScript (필요 시)

### 인프라
- **Cloudflare Tunnel** — 외부 노출 (무료)
- **NSSM** — FastAPI 윈도우 서비스 자동 시작 (선택)

### 재사용되는 기존 코드 (변경 거의 없음)
- `backend/hwp_core.py` (1,626줄) — HWP COM 워커
- `backend/hwp_generator.py` (584줄) — 출제 로직
- `backend/hwp_metadata_parser_v2.py` — 인덱싱
- `backend/firestore_engine.py` — DB 쿼리
- `backend/firebase_init.py` — Firebase 초기화
- `backend/auth.py` — 인증 (REST 호출 부분)
- `backend/indexers/` — 소스별 인덱서
- `backend/curriculum_config.json`, `backend/unit_hierarchy.json` — 메타

### 새로 작성하는 코드
- `server/` 폴더 전체 (아래 §4 참조)
- HTMX 템플릿 (탭별 HTML)
- `server/hwp_worker.py` — 큐 + 워커 (기존 hwp_generator 호출)

---

## 4. 폴더 구조

```
G:\문제은행\문제은행2\
├── backend\                     ← 기존 (거의 그대로)
│   ├── hwp_core.py
│   ├── hwp_generator.py
│   ├── hwp_metadata_parser_v2.py
│   ├── firestore_engine.py
│   ├── firebase_init.py
│   ├── auth.py
│   ├── indexers\
│   └── ...
│
├── server\                      ★ 신규
│   ├── __init__.py
│   ├── main.py                  ← FastAPI 진입점 (uvicorn 으로 실행)
│   ├── config.py                ← 설정 (포트, CORS, 키 경로 등)
│   ├── auth_dep.py              ← 의존성: Bearer 토큰 검증
│   │
│   ├── routes\
│   │   ├── __init__.py
│   │   ├── pages.py             ← HTMX 페이지 라우트 (/, /login, /random, ...)
│   │   ├── api_auth.py          ← POST /api/auth/login, /logout
│   │   ├── api_problems.py      ← GET /api/problems (필터/검색)
│   │   ├── api_exam.py          ← POST /api/exam/random, GET /api/exam/{id}/status
│   │   ├── api_library.py       ← GET/POST/DELETE /api/library
│   │   └── api_meta.py          ← GET /api/meta (단원 트리, 학교 목록 등)
│   │
│   ├── workers\
│   │   ├── __init__.py
│   │   ├── hwp_worker.py        ← HWP 출제 워커 (asyncio Task)
│   │   └── job_queue.py         ← asyncio.Queue 래퍼 + Job 상태 저장
│   │
│   ├── templates\               ← Jinja2 + HTMX
│   │   ├── base.html            ← 공통 레이아웃 (네비/폰트/Tailwind CDN)
│   │   ├── login.html
│   │   ├── home.html
│   │   ├── random_exam.html     ← 탭 1 (랜덤출제)
│   │   ├── original_exam.html   ← 탭 2 (원본출제)
│   │   ├── library.html         ← 탭 3 (시험지관리)
│   │   ├── manage.html          ← 탭 5 (문제관리, admin)
│   │   ├── register.html        ← 탭 4 (문제등록, admin)
│   │   └── users.html           ← 탭 6 (사용자관리, admin)
│   │   └── partials\            ← HTMX 부분 갱신용
│   │       ├── problem_list.html
│   │       ├── filter_panel.html
│   │       ├── exam_progress.html
│   │       ├── random_step3.html       ← 출제 문항 확인 (정렬/랜덤추가/삭제)
│   │       └── (각 탭별 partials)
│   │
│   └── static\
│       ├── htmx.min.js          ← CDN 캐싱용 로컬 사본
│       └── style.css            ← Tailwind 외 커스텀
│
├── server_data\                 ★ 신규 (서버 작동 데이터)
│   ├── exam_results\            ← 출제된 HWP 결과 (다운로드 후 정리)
│   └── job_log\                 ← 출제 작업 로그
│
├── exercise\                    ← HWP 원본 파일 (서버 PC에 통째로 보관)
│   ├── 수능특강\
│   ├── 수능완성\
│   ├── 모의고사\
│   ├── 내신기출A\
│   └── 내신기출B\
│
├── 서버클라이언트_설계도.md     ★ 본 문서
├── 서버클라이언트_작업현황.md   ★ 진행상황
└── (기존 문서들)
```

### 실행 진입점
```bash
# 개발 모드
cd G:\문제은행\문제은행2
uvicorn server.main:app --host 0.0.0.0 --port 8000 --reload

# 프로덕션 (서버 PC)
uvicorn server.main:app --host 0.0.0.0 --port 8000 --workers 1
```

> **주의**: `--workers 1` 필수. HWP COM 인스턴스가 1쌍이라 워커 여러 개면 충돌.

---

## 5. 데이터 흐름 (주요 시나리오)

### 시나리오 A: 로그인
```
[브라우저]                    [FastAPI 서버]              [Firebase]
   │                              │                          │
   │ GET /login                   │                          │
   ├─────────────────────────────>│                          │
   │ <login.html>                 │                          │
   │<─────────────────────────────┤                          │
   │                              │                          │
   │ POST /api/auth/login         │                          │
   │ {display_id, password}       │                          │
   ├─────────────────────────────>│                          │
   │                              │ signInWithPassword       │
   │                              ├─────────────────────────>│
   │                              │      {id_token, uid}     │
   │                              │<─────────────────────────┤
   │                              │ users/{uid} 조회         │
   │                              │  → role 확인             │
   │                              ├─────────────────────────>│
   │                              │      {role, active}      │
   │                              │<─────────────────────────┤
   │ Set-Cookie: session=jwt      │                          │
   │ HX-Redirect: /               │                          │
   │<─────────────────────────────┤                          │
```

### 시나리오 B: 랜덤 출제 (대표 흐름)
```
[브라우저]                    [FastAPI]              [HWP 워커]    [Firestore]
   │                             │                       │              │
   │ GET /random                 │                       │              │
   ├────────────────────────────>│                       │              │
   │ <random_exam.html>          │                       │              │
   │ (필터 패널 + 빈 목록)       │                       │              │
   │<────────────────────────────┤                       │              │
   │                             │                       │              │
   │ hx-get /api/problems?...    │                       │              │
   ├────────────────────────────>│ get_problems(filters) │              │
   │                             ├──────────────────────────────────────>│
   │                             │            <problems list>            │
   │                             │<──────────────────────────────────────┤
   │ <partials/problem_list>     │                       │              │
   │<────────────────────────────┤                       │              │
   │                             │                       │              │
   │ POST /api/exam/random       │                       │              │
   │ {selected_ids, options}     │                       │              │
   ├────────────────────────────>│ enqueue(job)          │              │
   │                             ├──────────────────────>│              │
   │ {job_id: "abc123"}          │                       │              │
   │<────────────────────────────┤                       │              │
   │                             │                       │              │
   │ hx-get /api/exam/abc123     │                       │              │
   │ /status (every 1s)          │                       │ HWP 출제 시작│
   ├────────────────────────────>│                       │              │
   │ <progress 30%>              │                       │              │
   │<────────────────────────────┤                       │              │
   │  ...                        │                       │              │
   │                             │                       │ 출제 완료    │
   │                             │                       │ exam_results │
   │                             │                       │ /abc123.hwp  │
   │ <progress 100% + 다운로드   │                       │              │
   │  버튼>                      │                       │              │
   │<────────────────────────────┤                       │              │
   │                             │                       │              │
   │ GET /api/exam/abc123        │                       │              │
   │ /download                   │                       │              │
   ├────────────────────────────>│ FileResponse(.hwp)    │              │
   │ HWP 파일 다운로드           │                       │              │
   │<════════════════════════════┤                       │              │
```

### 시나리오 C: 동시 출제 (3명)
```
선생님 A: POST /api/exam/random  → job_001 큐 등록 (시작)
선생님 B: POST /api/exam/random  → job_002 큐 등록 (대기 1번)
선생님 C: POST /api/exam/random  → job_003 큐 등록 (대기 2번)

워커:
  job_001 처리 중 (HWP source+target 점유)
    → 완료 → job_002 시작
              → 완료 → job_003 시작

각 사용자 화면:
  job_status:
    - "queued" (대기 중, 앞에 N명)
    - "running" (출제 진행률 X%)
    - "completed" (다운로드 버튼 노출)
    - "failed" (오류 메시지)
```

---

## 6. API 명세

### 6.1 인증
| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/auth/login` | display_id+password → 세션 쿠키 발급 |
| POST | `/api/auth/logout` | 쿠키 삭제 |
| GET  | `/api/auth/me` | 현재 세션 사용자 정보 |

### 6.2 메타데이터
| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/meta/units` | 단원 트리 (대단원/중단원 위계) |
| GET | `/api/meta/schools` | 학교 목록 (NAESIN_A 기준) |
| GET | `/api/meta/years` | 연도 목록 |

### 6.3 문제 검색
| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/problems` | 필터 쿼리 → 문제 목록. 파라미터: source, unit_code, difficulty, year, school, problem_type, exclude_recent_n, limit, offset |
| GET | `/api/problems/{id}` | 단일 문제 메타 |

### 6.4 출제 (큐 기반)
| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/exam/random` | body: {problem_ids, options} → {job_id} |
| POST | `/api/exam/original` | body: {file_name, problem_ids, options} → {job_id} (Phase 4) |
| GET  | `/api/exam/{job_id}/status` | {state, progress, message, error?} |
| GET  | `/api/exam/{job_id}/download` | HWP 파일 스트리밍 (state==completed 시) |
| DELETE | `/api/exam/{job_id}` | 큐 대기 상태일 때만 취소 |

### 6.5 시험지 관리 (Phase 5)
| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/library` | 저장된 시험지 목록 (saved_tests) |
| POST | `/api/library` | 시험지 메타 저장 |
| DELETE | `/api/library/{id}` | 시험지 삭제 |

### 6.6 페이지 (HTMX)
| 경로 | 설명 |
|---|---|
| `/login` | 로그인 화면 |
| `/` | 홈 (통계, 메뉴 카드) |
| `/random` | 랜덤 출제 (탭 1) |
| `/original` | 원본 출제 (탭 2, Phase 4) |
| `/library` | 시험지 관리 (탭 3, Phase 5) |
| `/manage` | 문제 관리 (탭 5, admin, Phase 6) |
| `/register` | 문제 등록 (탭 4, admin, Phase 6) |

---

## 7. HWP COM 큐잉 설계

### 핵심 제약
- HWP COM 인스턴스 1쌍(source/target)으로 한 번에 1개 출제만 가능
- 동시 호출 시 한글 충돌 → 큐로 직렬화 필수

### 구조
```python
# server/workers/job_queue.py
class JobQueue:
    queue: asyncio.Queue
    jobs: dict[str, JobState]  # job_id → state
    
    async def enqueue(self, job: Job) -> str: ...
    def get_state(self, job_id: str) -> JobState: ...

# server/workers/hwp_worker.py
async def hwp_worker_loop(queue: JobQueue, controller_pair: HWPControllerPair):
    while True:
        job = await queue.queue.get()
        try:
            queue.jobs[job.id].state = "running"
            # 기존 hwp_generator.HWPGenerator 호출
            result = generator.generate_exam(
                problems=job.problems,
                options=job.options,
                progress_callback=lambda p: update_progress(job.id, p)
            )
            queue.jobs[job.id].state = "completed"
            queue.jobs[job.id].result_path = result.output_path
        except Exception as e:
            queue.jobs[job.id].state = "failed"
            queue.jobs[job.id].error = str(e)
```

### Job 상태 머신
```
pending(큐 진입) → queued(대기) → running(처리 중) → completed(완료)
                                              ↘ failed(실패)
                              ↘ cancelled(사용자 취소)
```

### 진행률 보고
- `hwp_generator.generate_exam` 에 `progress_callback` 인자 추가
- 콜백이 JobState.progress 값(0~100)을 갱신
- 클라이언트는 1초마다 status 폴링

### 결과 파일 관리
- 출제 결과: `server_data/exam_results/{job_id}_{timestamp}.hwp`
- 다운로드 후 24시간 뒤 자동 삭제 (cron 또는 다음 큐 처리 시 정리)

### 한글 인스턴스 생명주기
- 서버 시작 시 1쌍 생성 (`source_ctrl`, `target_ctrl`)
- 워커 루프가 점유
- 서버 종료 시 정리 (signal handler)
- COM 충돌 발생 시 자동 재시작 로직 (기존 hwp_core 의 Kill&Restart 활용)

---

## 8. 보안 / 인증

### 8.1 로그인 흐름
1. 클라이언트가 `/api/auth/login` 에 display_id+password POST
2. 서버가 Firebase Auth REST API 호출 (signInWithPassword)
3. 성공 시 id_token + uid 획득
4. 서버가 Firestore `users/{uid}` 조회 → role/active 확인
5. 서버 자체 세션 JWT 발급 → HTTPOnly+Secure 쿠키로 설정
6. 모든 후속 요청은 쿠키로 인증

### 8.2 세션 쿠키
- **HTTPOnly**: JS 접근 차단
- **Secure**: HTTPS 전용
- **SameSite=Lax**: CSRF 1차 방어
- **만료**: 24시간 (id_token refresh 와 동기화)

### 8.3 권한 체크 (의존성)
```python
# server/auth_dep.py
async def require_user(session: SessionDep) -> User: ...
async def require_admin(session: SessionDep) -> User:
    user = await require_user(session)
    if user.role != "admin":
        raise HTTPException(403)
    return user

# 사용
@router.post("/api/exam/random")
async def random_exam(_: User = Depends(require_user), ...): ...

@router.post("/api/admin/index_file")
async def index_file(_: User = Depends(require_admin), ...): ...
```

### 8.4 PC 인증 (배포용설계도.md §2) 처리
- 기존 PC GUID 잠금은 **제거** (서버 모델에선 의미 없음)
- 대신 **세션 IP 로깅** 정도만 유지 (관리 목적)

### 8.5 Rate Limiting
- 로그인: 5회/분 (brute force 방어)
- 출제 요청: 1회/30초 per user (서버 부하 보호)
- 일반 API: 60회/분 per user

### 8.6 admin SDK 키
- 서버 PC에만 보관: `server_data/firebase-key.json` (gitignore 필수)
- 환경변수 `NAEGIWANGBANK_FIREBASE_KEY` 로 경로 지정
- 클라이언트에는 절대 노출 안 됨 (서버 모델의 본질적 장점)

---

## 9. 외부 접속 (Cloudflare Tunnel)

### 9.1 구성
```
[외부 사용자] → cloudflare.com (HTTPS) → [학원 서버 PC: cloudflared] → localhost:8000
                                                                         (FastAPI)
```

### 9.2 설치 단계
1. Cloudflare 계정 생성 (무료)
2. cloudflared.exe 다운로드, 학원 서버 PC에 설치
3. `cloudflared tunnel login` → 브라우저로 인증
4. `cloudflared tunnel create naegiwangbank` → 터널 ID 발급
5. 도메인 옵션:
   - **무료**: `<tunnel-id>.cfargotunnel.com` 사용
   - **유료/소유 도메인**: Cloudflare에 도메인 추가 후 CNAME
6. `config.yml` 작성: `localhost:8000` → 터널 매핑
7. `cloudflared service install` → 윈도우 서비스로 자동 시작

### 9.3 학원 안 vs 밖 접속
- **학원 안**: 직접 LAN IP `http://192.168.x.x:8000` (빠름)
- **학원 밖**: `https://naegiwangbank.example.com` (Cloudflare 경유, HTTPS)
- 둘 다 동일 서버 동일 데이터

### 9.4 추가 보안 (선택)
- **Cloudflare Access**: 외부 접속에 이메일 OTP 추가 (무료 50명까지)
- **국가 차단**: Cloudflare WAF에서 한국 외 차단
- **IP 화이트리스트**: 특정 IP만 허용

---

## 10. 서버 PC 요구사항

### 최소 사양
| 항목 | 사양 |
|---|---|
| CPU | i5 12세대 이상 (또는 Ryzen 5 5600) |
| RAM | 16GB |
| SSD | 512GB NVMe |
| OS | Windows 11 Pro 64bit |
| 네트워크 | 유선 LAN (학원 라우터에 직결) |

### 권장 사양 (3명 동시 + 여유)
| 항목 | 사양 |
|---|---|
| CPU | i5-13500 / i5-14500 또는 Ryzen 5 7600 |
| RAM | 32GB |
| SSD | 1TB NVMe |
| OS | Windows 11 Pro 64bit |
| 추가 | UPS (정전 보호), 자동 재시작 BIOS 설정 |

### 필수 소프트웨어
- **한글 오피스 (Hancom Office) 또는 한글 단품** — 라이센스 1개 (서버 PC 전용)
- **Python 3.11+**
- **Cloudflare cloudflared.exe**
- **Git** (개발자가 코드 업데이트용)
- **NSSM** (FastAPI 윈도우 서비스 등록)

### 운영 설정
- 자동 로그인 (재부팅 후 한글 COM 즉시 사용 가능)
- 절전 모드 OFF
- Windows Update는 정기 점검일에만 (예: 매주 일요일 새벽)
- 자동 재시작 BIOS 설정 (정전 복구 시)
- 화면 보호기/잠금 OFF (한글 포커스 문제 방지)

### 백업 정책
- HWP 원본 파일: 외장 SSD에 주 1회 미러링
- 출제 결과: `server_data/exam_results/` 24시간 보관 후 자동 삭제
- 코드: GitHub private repo (별도)
- DB: Firestore가 자동 백업 (Google)

---

## 11. 개발/배포 워크플로우

### 11.1 개발 단계 (Phase 1~3)
- 로컬 개발 PC (현재 risingmath1 PC) 에서 작성
- `uvicorn --reload` 로 빠른 반복 개발
- 임시 환경에서 검증 (개발자) → 사용자에게 보고
- HWP 출제는 로컬 한글로 검증

### 11.2 배포 단계 (Phase 3 이후)
1. 서버 PC에 git clone
2. `pip install -r requirements.txt`
3. 한글 설치 + 레지스트리 등록 (스텔스 모드, 기존 hwp_registry_manager.py)
4. `firebase-key.json` 배치 + 환경변수 설정
5. NSSM 으로 FastAPI 윈도우 서비스 등록
6. cloudflared 설치 + 서비스 등록
7. 자동 시작 테스트 (재부팅 후 정상 가동 확인)

### 11.3 코드 업데이트 후 배포
1. 개발 PC에서 git push
2. 서버 PC에 RDP 접속
3. git pull
4. NSSM 으로 서비스 재시작 (`nssm restart NaegiwangbankServer`)

---

## 12. 마이그레이션 / 데이터 이동

### 12.1 HWP 원본 파일
- 현재 위치: 사용자 PC `G:\문제은행\문제은행2\exercise\` (가정)
- 이동 대상: 서버 PC 동일 경로
- 방법: 외장 SSD 또는 네트워크 공유로 1회 복사

### 12.2 인덱스 데이터
- Firestore에 이미 29,451건 마이그레이션 완료 → 추가 작업 없음
- 서버 PC의 FastAPI 가 동일 Firestore 에 연결만 하면 즉시 사용 가능

### 12.3 사용자 계정
- Firebase Auth 의 모든 계정 그대로 사용
- 신규 선생님 계정만 추가 (기존 admin이 user_admin.py 또는 GUI로)

### 12.4 saved_tests (저장된 시험지)
- Firestore 동일 컬렉션 사용 → 자동 호환

---

## 13. 출시 범위 (2026-05-05 사용자 지시 — MVP 폐기, 전 기능 동시 구현)

### 출시 시점에 99% 동등성을 갖춰야 하는 항목 — 모든 탭
- ✅ 로그인 (`/api/auth/login`, `/login`)
- ✅ 홈 페이지 (`/`) — 로고/타이틀/메뉴 카드 5종/소스별 통계/원본 폴더 설정
- ✅ **탭 1 랜덤출제** (`/random`)
  - Step 1: 단원 트리 (과목/대단원/중단원, 상호배타)
  - Step 2: 우측 패널 8개 메뉴 (소스/수준+정답률/형식/문항선호도/년도/제외/배분/상세필터) + 좌측 단원별 행
  - Step 3: 출제 문항 확인 (정렬/랜덤추가/정렬취소/삭제) ← 로컬과 동등
  - 출제 → 큐 → 진행률 → 다운로드 + 누락 경고
- ✅ **탭 2 원본출제** (`/original`) — 소스→파일→문제, 검색/필터, 출제 추가옵션
- ✅ **탭 3 시험지관리** (`/library`) — 폴더 트리 CRUD, 시험지 CRUD, 출제 추가옵션, 오답 만들기
- ✅ **탭 4 문제등록** (`/register`, admin) — HWP 인덱싱 워크플로
- ✅ **탭 5 문제관리** (`/manage`, admin) — 검색/편집/일괄/진단/재인덱싱
- ✅ **탭 6 사용자관리** (`/users`, admin) — 계정 CRUD, 권한 변경
- ✅ HWP COM 큐잉 (3명 동시 처리)
- ✅ Cloudflare Tunnel 외부 접속
- ✅ 서버 PC 셋업 + 자동 시작

### 부분 출시(랜덤만 먼저) 정책 — 폐기
- 새 선생님이 사용 시점에 모든 흐름이 동작해야 한다 (특정 탭만 동작하면 업무 불가).
- 기존 선생님 .exe 병행은 5/30까지 유지(15장 단종 로드맵 참조).

### 병행 운영
- 윈도우 선생님은 5/30까지 .exe 계속 사용 (모든 기능 web 동등 시점부터 web 권장).
- 새 선생님(맥)은 5/9 부터 web — 모든 흐름 동작 전제.
- Firestore 가 단일 진실 출처 → 데이터 일관성 자동 보장.

---

## 14. 결정 완료 항목 (2026-05-02)

- [x] **도메인**: 무료 `*.cfargotunnel.com` 으로 시작 (안정화 후 자체 도메인 검토)
- [x] **세션 JWT 비밀키**: 환경변수 `NAEGIWANGBANK_SESSION_SECRET`
- [x] **HWP 워커**: 1쌍 (3명 동시 사용 큐 대기 견딜만함)
- [x] **출제 결과 보관 기간**: 24시간 자동 삭제
- [x] **로그 보관 정책**: 서버 PC `server_data/logs/` 30일 회전

---

## 15. .exe 단종 로드맵 (2026-05-02 결정)

### 핵심 결정
- **두 시스템 영구 병행 X** → 단계적 폐기로 단일 시스템 운영
- 이유: 코드 이중 유지 부담, 기능 동기화 비용, 데이터 일관성 위험

### 단종 일정
| 단계 | 날짜 | 행동 |
|---|---|---|
| 1차 출시 (전 기능 동등) | 2026-05-09 | 새 선생님 웹 시작, 윈도우 선생님은 병행 가능 |
| 시범 운영 | 2026-05-10 ~ 05-29 | 모든 선생님 웹 시범, .exe 병행 가능 |
| **.exe 로그인 차단** | **2026-05-30 (금)** | Firestore 플래그로 .exe 차단 |

### 5/22 이후 정책 (Phase 6 완료~5/29)
- ❌ .exe 신규 기능 추가 금지
- ❌ .exe 버그 수정 금지 (web 우선)
- ✅ 모든 신규 작업은 web 전용

### 5/30 차단 메커니즘
- Firestore 에 `system/config` 문서: `legacy_exe_disabled: true` 추가
- .exe 부팅 시 첫 Firestore read 에서 체크
- True 면 다이얼로그 표시:
  ```
  내기왕 문제은행 시스템이 웹으로 전환되었습니다.
  주소: https://<tunnel-url>
  로그인 ID/PW 는 동일합니다.
  ```
  → `sys.exit(0)` 으로 종료
- 코드 변경 1시간, 배포 .exe 1회 추가 빌드 필요 (체크 로직 삽입)

### 폐기 후 정리 (5/30 이후)
- `backend/main_gui.py` (6,902줄) — 보존하되 deprecated 표시
- `dist/` 폴더 — 외장 백업 후 삭제
- 기존 .exe 빌드 시스템 (`build.bat`, `naegiwangbank.spec`, `_bump_version.py`) — 삭제
- `system_architecture.md` 갱신 — 서버-클라이언트 모델로 전면 개편

---

## 16. 운영 정책 정리 (2026-05-02 추가)

### PC 잠금 정책
- **.exe**: 기존 정책 유지 (PC GUID 잠금, admin 면제)
- **웹**: PC 잠금 면제 (브라우저는 IP/디바이스 식별 불안정 + 어차피 5/30 단종)

### 맥에서 HWP 열기
- **기본**: .hwp 그대로 다운로드 → 새 선생님이 한컴 맥 버전 또는 한글뷰어 사용
- **fallback**: 맥에서 .hwp 못 열면 **Phase 4에 PDF 다운로드 옵션 추가** (한글 자체 PDF 저장 기능 활용)

### 5/9 첫날 백업 플랜
- 새 선생님이 웹 접속 안 되거나 출제 실패 시:
  1. 카톡으로 요청사항 받기 (단원/난이도/문항수)
  2. 학원 PC에서 기존 .exe로 출제 → 카톡으로 .hwp 전송
- 5/10 ~ 5/14 안정화 모니터링: 출제 실패율, 응답 시간 측정

### 양방향 호환 (Firestore 동기화)
- Phase 3 통합 테스트에 포함:
  - 웹에서 시험지 저장 → .exe 시험지 관리에서 보임 ✓
  - .exe 출제 → 웹 활동 로그에 보임 ✓
  - saved_tests, problems, exclusion_settings 양방향 정합성
