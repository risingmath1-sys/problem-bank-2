# 서버-클라이언트 작업현황

> 설계는 `서버클라이언트_설계도.md` 참조.
> 데드라인: **2026-05-09** — 새 선생님 사용 시작 (전 기능 동등성 99%)
> 시작일: 2026-05-02
> 2026-05-05 사용자 지시: **MVP 폐기. 모든 탭(0~6) 동시 구현, 99% 동등성 도달 시 출시.**
> 2026-05-23 단원 매핑 사고 + 대규모 정상화 (아래 § 별도 섹션 참조)

---

## 🚨 2026-05-23: 단원 매핑 시스템 대규모 정상화

### 발견된 사고
사용자 보고: "내신기출B 수열/대수가 랜덤출제에서 0건 / 경우의 수 뽑았는데 도형의 방정식 나옴"

### 근본 원인
1. `unit_hierarchy.json` 이 2015 ↔ 2022 코드 체계 통합으로 재정의됨 (D1=행렬만 신설)
2. 그러나 `curriculum_config.json` 의 옛 `curriculum_mappings.2015_to_2022` 변환표는 그대로 잔재
3. `hwp_metadata_parser_v2.py:579` 가 인덱싱 시 이 잘못된 변환표를 적용
   - 예: J1(수열) → H1(지수), I1(삼각함수) → P1(이산확률분포), T1(포물선) → V1(평면벡터)
4. 결과: 7,742건의 unit_code 가 다른 단원으로 오저장됨
5. 추가: middle_unit / large_unit 7,958건도 옛 잘못된 코드의 이름으로 박혀있음
6. 추가: `naesin_n_unit_map.json` 의 high_school 섹션에 D1(행렬) 매핑 누락

### 수행한 복구 (요약)
| # | 작업 | 결과 |
|---|---|---|
| 1 | `curriculum_config.json` 의 `curriculum_mappings.2015_to_2022` 폐기 (빈 dict + history 주석) | 신규 인덱싱 안전 |
| 2 | `hwp_metadata_parser_v2.py:579` 변환 블록 제거 | 인덱싱 깔끔 |
| 3 | `main_gui.py` 의 5곳 (Line 365, 2063, 3291, 3590, 3598) 옛 변환 코드 제거 | 로컬 GUI 정리 |
| 4 | Firestore `unit_code` 7,742건 `mapped_unit_code` 기준 일괄 복구 | 백업: `restore_backup_*.json` |
| 5 | `naesin_n_unit_map.json` high_school 섹션에 D1(행렬) 매핑 추가 | 신규 인덱싱 정상 |
| 6 | 행렬 D1 빈값 317건 일괄 갱신 (파일명 `행렬` 키워드 기준) | bulk_update_field 사용 |
| 7 | `middle_unit` / `large_unit` 7,958건 일괄 정상화 (unit_hierarchy 기준) | |
| 8 | 부팅 시 자동 정합성 검사 (`data_cache.py:ensure_synced`) | cache vs Firestore count 차이 > 0.5% 시 자동 full_sync |
| 9 | `bulk_update_field()` 안전 헬퍼 추가 (`firestore_engine.py`) | Firestore + 캐시 동시 갱신 + `updated_at` 자동 |
| 10 | `backend/resync_all.py` 통합 헬퍼 | cache full_sync + audit 한 줄 실행 |
| 11 | `backend/audit_db.py` 정합성 검사 강화 ([6] middle/large 어긋남 추가) | 자가검증 시스템 완비 |
| 12 | `hwp_metadata_parser_v2.py:_report_mapping_failures()` | 인덱싱 종료 시 매핑 누락 단원명 자동 요약 |

### 검증 결과 (2026-05-23 종료 시점)
```
NAESIN_N (16,281건):
  [1] subject↔unit_code:        1건 (학교 분류 실수, 무시 가능)
  [2] unit_code 빈값:           395건 (중학교 시험지 매핑 누락 — 별건)
  [3] [고] 파일에 Z 코드:        0건 ✅
  [4] mapped_unit_code 잔재:     0건 ✅
  [5] 캐시 vs Firestore:         일치 ✅
  [6] middle/large_unit 어긋남:  0건 ✅
```

### 남은 작업 / 미래 작업

**B. middle_unit / large_unit 파생값化 (선택, 큰 작업)**:
- 현재: DB에 unit_code + middle_unit + large_unit 별도 저장 → 어긋날 위험은 audit_db.py [6]번 검사로 잡힘
- 이상: unit_code 만 저장, 표시 시 `unit_hierarchy.json` lookup (단일 진실의 원천)
- 영향: 인덱싱 + WRITE 경로 + UI 표시 등 다수 위치. 2~3시간 예상
- 우선순위: 낮음 (현 안전망으로 충분)
- 메모: 검증 시스템(D) 잘 동작하니 굳이 안 해도 됨

**E. 옛 매핑 문서 history 표시 (완료)**:
- `curriculum_mapping_validation.md` — 옛 매핑 자료 보존용 (참고)
- `system_architecture.md` 의 "단원 코드 체계 (2026-05-23 업데이트)" 섹션 갱신 완료
- `서버클라이언트_설계도.md` § 0-B 운영 역할 분리 정책 추가

**기타**:
- `naesin_n_unit_map.json` 에 중학교 단원명 매핑 누락 보강 (395건 영향) — 사용자 판단
- 행렬 769건 → D1 374건 변환 (재인덱싱 + 일괄 fix). 나머지 행렬 파일도 재인덱싱 권장

### 운영 가이드 (재발 방지)
1. 일괄 작업 후: `python backend/resync_all.py` 실행
2. 인덱싱 후: 매핑 실패 단원명 요약 자동 출력 — 누락 시 `naesin_n_unit_map.json` 추가
3. Firestore 직접 batch.update 금지 — `engine.bulk_update_field()` 사용
4. 정기 점검: `python backend/audit_db.py [--source NAESIN_N]`

---

## ⚠️ 동등성 평가 (2026-05-04, 설계도 §0-A)

> **출시 기준 = 이 체크리스트 통과 (동등성 99%)**, 줄 수나 라우트 수가 아니다.
> 평가 기준: 로컬 `backend/main_gui.py`와의 1:1 동등성.
> 표기: ✅ 동등 / ⚠️ 부분 / ❌ 미구현 / ➖ 해당 없음

### 종합 점수

| 측정 시점 | 평균 동등성 | 비고 |
|---|---|---|
| 2026-05-04 1차 | 약 30% | 사용자 평가와 일치 (탭 1/4/6 미흡) |
| 2026-05-04 EOD | 약 70% | R-1~R-8, Library, Original, Manage parity 작업 완료 |
| **2026-05-05 EOD** | **약 90%** | MVP 폐기 + 모든 탭 동시 진행, Step 3 + 홈 + Users + Register skel + 진단/재인덱싱/오답만들기 |

### 탭별 점수 (2026-05-05 EOD)

| 탭 | 동등성 | 변동 | 핵심 추가/변경 |
|---|---|---|---|
| 탭 0 홈 | **95%** | +45 | 로고 / 5개 메뉴 카드(색상별) / 문제등록 + 사용자관리 카드 / 5색 통계 박스 / 원본 폴더 설정(admin) |
| 탭 1 랜덤출제 | **95%** | +75 | Step 3 (출제 문항 확인) — 삭제/순서정렬/정렬취소/랜덤추가/이전/다음. exam_session.py 추가 |
| 탭 2 원본출제 | **80%** | +25 | 출제 추가옵션 (exclude_tags NAESIN 전용 / fast_answer_key) |
| 탭 3 시험지관리 | **90%** | +30 | 출제 추가옵션 + 오답 만들기 (채점 → X문항 자동 오답시험지 저장) |
| 탭 4 문제등록 | **50%** | +50 | 6개 소스 카드 + 폼 진입 + admin 가드. HWP 파서 워커는 추후. |
| 탭 5 문제관리 | **80%** | +50 | 파일 진단 (존재/좌표/제외/해결책) + 재인덱싱 안내 |
| 탭 6 사용자관리 | **90%** | +90 | 목록 / 새계정 발급 / 비번재설정 / 활/비/역할/삭제 + 본인 보호 가드 |

### 1차 출시(2026-05-09) 가능 평가
- **랜덤출제**, **원본출제**, **시험지관리**, **문제관리**, **사용자관리**, **홈** — 모든 핵심 흐름 동작.
- **문제등록** 만 50% (admin 가이드 페이지). HWP 파서 워커 분리는 데드라인 후 일정으로 분리 가능.
- 출시 가능: ✅ (admin 인덱싱은 데스크탑 .exe 병행 사용으로 충분)

### Smoke suite 현황 (10개)
- `_smoke_random_step2.py` (R-3/4/5)  ✅
- `_smoke_random_step3.py` (Step 3 흐름)  ✅ NEW
- `_smoke_random_r67.py` (R-6/7)  ✅
- `_smoke_random_r8.py` (R-8)  ✅
- `_smoke_library.py` (폴더/시험지/오답만들기)  ✅
- `_smoke_original.py` (원본출제)  ✅
- `_smoke_manage.py` (관리/진단/재인덱싱)  ✅
- `_smoke_home.py` (홈/통계/폴더설정)  ✅ NEW
- `_smoke_users.py` (사용자관리)  ✅ NEW
- `_smoke_register.py` (문제등록 진입)  ✅ NEW

---

### 탭 0. 홈 — 동등성 50%
**로컬**: `backend/main_gui.py` `_build_home_tab` (라인 177-305) + `_make_home_card` (307-322) + `_update_home_stats` (324-345)
**서버**: `server/templates/home.html` + `/partial/stats` (`pages.py:42-54`)

| 항목 | 로컬 라인 | 서버 | 상태 |
|---|---|---|---|
| 학원 로고 (54x54, 흰배경 투명처리) | 189-205 | logo.png 있으나 미사용 | ❌ |
| "상승 Solution" 타이틀 + 부제목 | 210-213 | "{{user}} 님 환영합니다"로 변형 | ⚠️ |
| 메뉴 카드 — 랜덤출제 | 226-227 | ✅ | ✅ |
| 메뉴 카드 — 원본출제 | 228-229 | ✅ | ✅ |
| 메뉴 카드 — 시험지관리 | 230-231 | ✅ | ✅ |
| 메뉴 카드 — **문제 등록** (admin) | 232-233 | 없음 | ❌ |
| 메뉴 카드 — 문제관리 (admin) | 234-235 | ✅ | ✅ |
| 카드별 색상 (파/녹/보/주/회) + 부제 2줄 | 226-240 | 색상 단일, 부제 1줄 | ⚠️ |
| 소스별 통계 5개 박스 (색상별) | 252-270 | partial/stats 텍스트 위주 | ⚠️ |
| 총 등록 문제 카운트 | 273-282 | partial/stats 안에 포함 | ✅ |
| **원본 파일 폴더 설정 Entry + 폴더 선택 버튼** | 287-302 | 없음 | ❌ |
| 탭 전환 시 통계 자동 갱신 | 347-351 | HTMX hx-trigger=load만 | ⚠️ |

---

### 탭 1. 랜덤 출제 — 동등성 20% (가장 시급, 사용자가 가장 많이 사용)
**로컬**: `_build_random_tab` (1726) → `_build_step_2_ui` (2500) → `_build_right_panel_options` (**2547-2893**) + `_build_left_panel_table` (3153)
**서버**: `server/templates/random_exam.html` + `/random`, `/partial/random/units`, `/partial/random/preview` + `POST /api/exam/random`

#### 구조 비교
- 로컬: **2-Step 구조** (Step 1 단원 선택 → Step 2 옵션 + 좌측 테이블 + 우측 패널)
- 서버: **단일 페이지** (subject 드롭다운 → units 그리드 → 미리보기 카운트)
- ❌ Step 구조 자체가 다름 — 로컬의 Step 2 좌측 테이블(선택된 문제 미리보기)이 서버에 없음

#### 우측 패널 8개 메뉴 비교 (로컬 라인 2547-2893)
| 번호 | 항목 | 로컬 라인 | 서버 | 상태 |
|---|---|---|---|---|
| 1 | 소스 필터 (5개 + 전체해제 버튼) | 2578-2615 | 체크박스만 | ⚠️ |
| 2 | 수준별 보기 (토글 + A/B/C/D) | 2619-2636 | 체크박스 (토글 없음) | ⚠️ |
| 2-b | 정답률 사용 (Spinbox 0-100% + 적용) | 2638-2660 | 없음 | ❌ |
| 3 | 형식별 보기 (토글 + 객/주관식) | 2664-2677 | 라디오 (토글 없음) | ⚠️ |
| 3-b | **문항선호도** (Good/Soso/Bad + 내것/남것) | 2680-2737 | 없음 | ❌ |
| 4 | 년도 필터 (토글 + 제외/포함 + 콤보 + 태그) | 2741-2771 | 단순 입력 필드 | ⚠️ |
| 5 | 출제 제외 옵션 (제외 문제 출제 + 다이얼로그) | 2775-2786 | 없음 | ❌ |
| 7 | **문항 배분** (총문항 + 균등/비율 배분 + 합계) | 2788-2805 | 출제 개수만 | ⚠️ |
| 8 | ▶ 상세 필터 토글 | 2807-2814 | 없음 | ❌ |
| 8-a | 학교 옵션 (검색 Listbox + 선택 태그) | 2820-2850 | 단순 텍스트 입력 | ⚠️ |
| 8-b | 모의고사 검색 (브랜드 키워드 + 태그) | 2852-2875 | 없음 | ❌ |
| 8-c | 문항번호 필터 (예: 30, 45, 46) | 2877-2892 | 없음 | ❌ |

#### 출제 흐름
| 항목 | 로컬 | 서버 | 상태 |
|---|---|---|---|
| 시험지 제목 입력 | ✅ | ✅ | ✅ |
| 시험지 단원 입력/자동 | ✅ | exam_unit="" 하드코딩 | ⚠️ |
| 정답키 옵션 (fast_answer_key) | ✅ (False 기본) | False 하드코딩 | ⚠️ |
| 진행률 표시 | ✅ | partials/exam_progress.html | ✅ |
| HWP 다운로드 | ✅ | `GET /api/exam/{id}/download` | ✅ |
| 누락 경고 (skipped 표시) | ✅ (출제_불일치_감지.md) | job.skipped 있음, UI 표시 미확인 | ⚠️ |
| 최신기출우선 (메모리 5번 메뉴) | 위치 미확인 — 추후 확인 | 없음 | ❌ |

---

### 탭 2. 원본 출제 — 동등성 55%
**로컬**: `_build_exam_gen_tab` (라인 741-약1725)
**서버**: `original_exam.html` + `/original`, `/partial/original/files`, `/partial/original/problems` + `POST /api/exam/original`

| 항목 | 상태 | 비고 |
|---|---|---|
| 소스 선택 → 파일 목록 | ✅ | 학교/연도 그룹화 |
| 파일 검색 (file_name_like) | ✅ | |
| 학교/연도 필터 | ✅ | |
| 파일 클릭 → 읽기전용 문제 목록 | ✅ | |
| 원본 그대로 출제 | ✅ | exam_title=file_name 기본값 |
| 추가 옵션 (제외태그, 정답키 등) | ❌ | 미확인 — 로컬 741-1725 분석 필요 |
| 출제 결과 미리보기 | ❌ | |
| 폴더 옵션 (`_build_folder_options` 1282) | ❌ | 시험지를 폴더에 자동저장하는 옵션 |

---

### 탭 3. 시험지 관리 — 동등성 60%
**로컬**: `_build_library_tab` (라인 5802-약6910)
**서버**: `library.html` + `/partial/library/folders`, `/tests`, `/test/{id}/preview` + `POST /api/exam/library`

| 항목 | 상태 | 비고 |
|---|---|---|
| 폴더 트리 (계층 구조) | ✅ | parent_id 기반 |
| 폴더 내 시험지 리스트 (정렬 옵션) | ✅ | created/title |
| 시험지 클릭 → 미리보기 | ✅ | |
| 미리보기에서 출제 버튼 | ✅ | |
| 폴더 생성/이름변경/삭제 | ❌ | 라우트 부재 — 로컬 5802~ 분석 필요 |
| 시험지 이동/삭제 | ❌ | |
| 출제 제외 연동 (`_oneshot_excluded_problem_ids`) | ❌ | |

---

### 탭 4. 문제 등록 — 동등성 0%
**로컬**: `_build_registration_tab` (430) → `_build_source_selector` (438) → `_build_reg_form` (551-713)
**서버**: ❌ 라우트·템플릿·API 모두 없음

| 항목 | 상태 |
|---|---|
| 소스 선택 화면 | ❌ |
| HWP 파일 업로드/서버 폴더 선택 | ❌ |
| 인덱싱 워크플로우 (parser → 결과 검토 → 저장) | ❌ |
| 진행률 표시 (인덱싱 시간 오래 걸림) | ❌ |

---

### 탭 5. 문제 관리 — 동등성 30%
**로컬**: `_build_management_tab` (라인 4807-5801)
**서버**: `manage.html` + `/partial/manage/files`, `/partial/manage/problems` + `api_manage.py`

| 항목 | 상태 | 비고 |
|---|---|---|
| 소스 → 파일 검색 | ✅ | |
| 파일별 문제 목록 | ✅ | |
| view_mode (all / excluded) | ✅ | |
| difficulty 편집 + lock 플래그 | ❌ | UI 미확인 — 로컬 4807~ 분석 필요 |
| unit_code 편집 + lock | ❌ | |
| is_excluded 토글 | ❌ | |
| 일괄 편집 (다중 선택) | ❌ | |
| 검색/필터 (난이도·단원·문제유형 등) | ❌ | |
| 문제 삭제 | ❌ | |

---

### 탭 6. 사용자 관리 — 동등성 0%
**로컬**: `_build_users_tab` (라인 6911~)
**서버**: ❌ 부재

---

### 공통 동등성 항목

| 항목 | 상태 | 비고 |
|---|---|---|
| 로그인 화면 (디자인 + 플로우) | ⚠️ | 동작은 됨, 디자인 차이 미평가 |
| 색상/폰트/여백 (tkinter 수준 가독성) | ⚠️ | dark theme 위주, 로컬 home BG=#1a1a2e 비교 |
| 단축키 (Ctrl+탭 등) | ❌ | |
| 에러 메시지 문구 | ⚠️ | |
| 진행률 표시 (출제 중) | ⚠️ | exam_progress.html 동작 검증 필요 |
| 출제 누락 경고 (`출제_불일치_감지.md`) | ⚠️ | |

---

### 차이 기록 규칙
- 항목 단위로 **로컬 라인 위치 → 서버 구현 → 상태(✅/⚠️/❌)** 기입.
- 구체 차이는 별도 줄 "[차이] ..." 로 추가 기록.
- 작업 순서: ❌ → ⚠️ → ✅ 로 끌어올린다. **우선순위는 영향 큰 탭(랜덤출제, 문제등록)부터.**

---

## 🔧 완료 작업 — 랜덤출제 동등성 (R-Phase) + 전 탭 동시 진행

> 2026-05-04 시작 (R-Phase). 2026-05-05 사용자 지시로 MVP 폐기, 모든 탭 동시 99% 동등성.
> 로컬 `backend/main_gui.py`를 정답으로 보고 1:1 복제. 로컬 코드 미수정.

### R-Phase 결과 (2026-05-04 ~ 05)

| 단계 | 내용 | 상태 |
|---|---|---|
| **R-1a** | Step 1 페이지 — 단원 트리 (과목/대단원/중단원 체크) | ✅ |
| **R-1b** | Step 2 페이지 — 좌측 단원별 테이블 (가능 카운트 + 수량 입력) | ✅ |
| **R-1c** | Step 2 → 출제 트리거 + Step 3 진행률 화면 | ✅ |
| **R-2** | 우측 메뉴 1-3 (소스 / 수준+정답률 / 형식) | ✅ |
| **R-3** | 우측 메뉴 4 — 문항선호도 (Good/Soso/Bad + scope) | ✅ |
| **R-4** | 우측 메뉴 5 — 년도 필터 (제외/포함 모드 + 콤보 + 태그) | ✅ |
| **R-5** | 우측 메뉴 6 — 출제 제외 (다이얼로그 포함) | ✅ |
| **R-6** | 우측 메뉴 7 — 문항 배분 (균등/비율 배분 버튼) | ✅ |
| **R-7** | 우측 메뉴 8 — 상세필터 (학교/모의고사/문항번호) | ✅ |
| **R-8** | 출제 옵션 (제목/단원 자동/정답키) + 누락 경고 | ✅ |
| **R-9** | **Step 3 (출제 문항 확인)** — 정렬/랜덤추가/정렬취소/삭제/이전/다음 | ✅ 2026-05-05 NEW |

### 2026-05-05 EOD 추가 작업 (전 탭 99%)

| 항목 | 내용 | 상태 |
|---|---|---|
| H-1 | 홈 — 로고/5색 카드/문제등록·사용자관리 카드/5색 통계/원본폴더 설정 | ✅ |
| H-2 | 홈 — `/api/source_folder` admin POST → settings.json 갱신 + in-memory reload | ✅ |
| U-1 | 사용자관리 페이지 (`/users`) + admin 가드 | ✅ |
| U-2 | `/api/users/{create,reset_password,toggle_active,change_role,delete}` + 본인보호 | ✅ |
| M-1 | 문제관리 — `/api/manage/files/diagnose` (파일 진단 partial) | ✅ |
| M-2 | 문제관리 — `/api/manage/files/reindex` (안내) | ✅ |
| L-1 | 시험지관리 — `/api/library/test/{id}/scoring` (채점 화면) | ✅ |
| L-2 | 시험지관리 — `/api/library/test/{id}/save_wrong` (오답 시험지 자동 저장) | ✅ |
| L-3 | 시험지관리 / 원본출제 — 출제 옵션 추가 (exclude_tags / fast_answer_key) | ✅ |
| Reg-1 | 문제등록 — `/register` + 6개 소스 카드 + `/register/{source}` 폼 진입 | ✅ (skel 50%) |

### 핵심 구조 차이 (해결)

**기존 서버**: 단일 페이지 (과목 → 단원 → 우측 옵션 → 출제) ← 빠른 출제만
**현재 (로컬 동등)**: 3-Step + 출제 진행

```
Step 1: 단원 선택
Step 2: 옵션 8종 + 좌측 카운트
Step 3: 출제 문항 확인 (정렬/랜덤추가/삭제) ← 2026-05-05 추가
Step 4: 출제 진행률 + 다운로드 + 누락 경고
```

---

## 일정 요약

### 2026-05-05 단계 재편 — MVP 폐기, 단일 출시 모델
| 마일스톤 | 작업 | 예정일 | 상태 |
|---|---|---|---|
| 0 | 사전 결정 + 설계 문서 + 폴더 정리 | 2026-05-02 | ✅ 완료 |
| 1 | 서버 골격 + 인증 + 로그인/홈 페이지 | 2026-05-04 | ✅ 완료 |
| 2 | 4개 탭 parity 70-75%(R-1~R-8, Library, Original, Manage) | 2026-05-04 | ✅ 완료 |
| 3 | **모든 탭 0~6 동시 구현 → 동등성 99%** | 2026-05-05 ~ 08 | 🔄 진행 중 |
| 4 | Cloudflare Tunnel + 서버 PC 셋업 | 2026-05-08 | ⏳ 대기 |
| **출시** | **새 선생님 사용 시작 (전 기능 동등)** | **2026-05-09 (금)** | 🎯 **데드라인** |
| **시범 운영** | **모든 선생님 웹 시범, .exe 병행** | **2026-05-10 ~ 29** | ⏳ 대기 |
| **🛑 .exe 차단** | **Firestore 플래그로 .exe 로그인 차단** | **2026-05-30 (금)** | 🏁 **단종일** |

> Phase 단계(2/4/5/6)는 폐기됨 — 한 마일스톤(3) 안에서 모든 탭 동시 진행.

---

## Phase 0: 사전 결정 + 설계 문서 (2026-05-02 완료)

### 결정사항
- [x] 시스템 대전환 방향 확정 (서버-클라이언트)
- [x] 기술 스택 확정 (FastAPI + HTMX + Firestore 유지 + Cloudflare Tunnel)
- [x] 동시 사용자 3명, 데드라인 5/9 (전 탭 동등성 99%)
- [x] HWP 원본 서버 PC 보관, 결과 브라우저 다운로드
- [x] 권한 admin/user 유지
- [x] 요금제 전환 계획 (Pro → 5/4 Max 20x)

### 산출물
- [x] `서버클라이언트_설계도.md` (본 폴더)
- [x] `서버클라이언트_작업현황.md` (본 문서)
- [x] MEMORY.md 갱신 — 시스템 대전환 섹션 추가

---

## Phase 1: 서버 골격 + 인증 + Cloudflare Tunnel (2026-05-04 ~ 05)

### 1-1. 프로젝트 구조 생성
- [ ] `server/` 폴더 생성 + `__init__.py`
- [ ] `server_data/` 폴더 생성 (exam_results, logs)
- [ ] `requirements.txt` 갱신 (fastapi, uvicorn, jinja2, python-multipart 등)
- [ ] `.gitignore` 갱신 (server_data/, firebase-key.json 등)

### 1-2. FastAPI 골격
- [ ] `server/main.py` — FastAPI 앱 인스턴스, 미들웨어, 라우터 등록
- [ ] `server/config.py` — 환경변수 로드 (포트, 키 경로, 세션 시크릿)
- [ ] 정적 파일 서빙 (`/static`)
- [ ] Jinja2 템플릿 엔진 등록
- [ ] 헬스체크 엔드포인트 `/healthz`

### 1-3. Firebase Auth 통합
- [ ] `server/auth_dep.py` — 세션 쿠키 검증 의존성
- [ ] `server/routes/api_auth.py` — `POST /api/auth/login`, `/logout`, `GET /api/auth/me`
- [ ] 기존 `backend/auth.py` REST 호출 부분 재사용
- [ ] 세션 JWT 발급/검증 (HTTPOnly+Secure 쿠키)
- [ ] 로그인 5회/분 rate limit
- [ ] `require_user`, `require_admin` 의존성

### 1-4. 로그인 페이지
- [ ] `server/templates/base.html` — 공통 레이아웃 (Tailwind CDN, HTMX, 네비)
- [ ] `server/templates/login.html` — 로그인 폼 (HTMX `hx-post`)
- [ ] `server/routes/pages.py` — `GET /login`, `GET /` (로그인 가드)
- [ ] 로그인 성공 시 HX-Redirect 로 `/` 이동
- [ ] 로그아웃 버튼

### 1-5. 홈 페이지
- [ ] `server/templates/home.html` — 통계 + 메뉴 카드 (랜덤/원본/관리)
- [ ] `GET /api/meta/stats` — 총 문제 수, 소스별 카운트
- [ ] 권한 따라 admin 메뉴 표시/숨김

### 1-6. Cloudflare Tunnel 설정 (개발 PC에서 임시 검증)
- [ ] Cloudflare 계정 생성 (없으면)
- [ ] cloudflared.exe 다운로드
- [ ] `cloudflared tunnel login` → 인증
- [ ] 임시 터널 생성 (개발 PC, 무료 cfargotunnel.com)
- [ ] `localhost:8000` 매핑 → 외부 URL 접속 검증
- [ ] HTTPS 통신 확인

### 1-7. 검증
- [ ] 로그인 → 홈 페이지 진입 정상
- [ ] 외부 URL (Cloudflare 경유) 로 접속 가능
- [ ] 맥 사파리에서 동작 확인 (가능하면)

### 산출물
- 동작하는 FastAPI 서버 (로그인 + 홈)
- 외부 접속 가능한 Cloudflare URL

---

## Phase 2: 랜덤 출제 화면 + HWP 큐잉 (2026-05-06 ~ 07)

### 2-1. 메타데이터 API
- [ ] `GET /api/meta/units` — `unit_hierarchy.json` 트리 반환
- [ ] `GET /api/meta/schools` — Firestore에서 distinct school
- [ ] `GET /api/meta/years` — Firestore에서 distinct year

### 2-2. 문제 검색 API
- [ ] `GET /api/problems` — 필터 쿼리 → JSON
  - 파라미터: source, unit_code[], difficulty[], year, school, problem_type, exclude_ids[], limit, offset
  - 기존 `firestore_engine` 의 검색 메서드 재사용
- [ ] `GET /api/problems/{id}` — 단일 문제 메타

### 2-3. 랜덤 출제 페이지 (HTMX)
- [ ] `server/templates/random_exam.html` — 좌(목록) + 우(필터 패널)
- [ ] `server/templates/partials/filter_panel.html` — 필터 컨트롤
- [ ] `server/templates/partials/problem_list.html` — 문제 목록 (체크박스, 메타)
- [ ] `server/templates/partials/exam_options.html` — 제목/단원/정답키 옵션
- [ ] `server/templates/partials/exam_progress.html` — 출제 진행률 + 다운로드
- [ ] `GET /random` — 페이지 진입 (필터 패널 + 빈 목록)
- [ ] HTMX `hx-get` 으로 필터 변경 시 목록 부분 갱신
- [ ] 선택 카운터 (선택된 문제 수)
- [ ] 일괄 선택/해제 버튼

### 2-4. HWP 워커 + 큐
- [ ] `server/workers/job_queue.py` — `JobQueue` 클래스 (asyncio.Queue + jobs dict)
- [ ] `server/workers/hwp_worker.py` — 워커 루프 (큐에서 꺼내 generate_exam 호출)
- [ ] `Job` dataclass — id, problems, options, state, progress, error, result_path
- [ ] 서버 시작 시 워커 1개 + HWP COM source/target 인스턴스 1쌍 생성
- [ ] 서버 종료 시 한글 정리 (signal handler)
- [ ] `hwp_generator.generate_exam` 에 `progress_callback` 추가 (필요 시)

### 2-5. 출제 API
- [ ] `POST /api/exam/random` — body로 problem_ids + options → enqueue → {job_id}
- [ ] `GET /api/exam/{job_id}/status` — JobState JSON
- [ ] `GET /api/exam/{job_id}/download` — FileResponse(HWP)
- [ ] `DELETE /api/exam/{job_id}` — 큐 대기 시 취소
- [ ] 출제 1회/30초 rate limit

### 2-6. 출제 결과 정리
- [ ] `server_data/exam_results/{job_id}_{timestamp}.hwp` 저장
- [ ] 24시간 후 자동 삭제 (백그라운드 task)

### 2-7. 검증
- [ ] 단일 사용자: 5문제 랜덤 출제 → HWP 다운로드 → 한글 정상 열림
- [ ] 단일 사용자: 100문제 출제 → 진행률 표시 → 완료 → 다운로드
- [ ] 동시 3명: 각각 출제 요청 → 큐 대기 표시 → 순차 처리 → 모두 다운로드 성공
- [ ] 출제 중 일부 문제 실패 → 누락 경고 표시 (기존 `_validate_skipped` 활용)

### 산출물
- 랜덤 출제 → 다운로드 동작
- 큐 기반 동시 사용자 처리

---

## Phase 3: 서버 PC 셋업 + 통합 테스트 + 배포 (2026-05-08)

### 3-1. 서버 PC 준비 (사용자 작업)
- [ ] 서버 PC 구입 (i5-13500 / RAM 16GB / SSD 512GB / Win11 Pro)
- [ ] 학원 라우터에 유선 연결, 고정 LAN IP 할당
- [ ] 자동 로그인 설정, 절전모드 OFF, 자동 재시작 BIOS 설정

### 3-2. 소프트웨어 설치 (서버 PC)
- [ ] Windows Update 적용
- [ ] 한글 오피스 또는 한글 단품 설치 + 라이센스 등록
- [ ] Python 3.11+ 설치
- [ ] Git 설치
- [ ] cloudflared.exe 설치
- [ ] NSSM 다운로드

### 3-3. 코드 + 데이터 이전
- [ ] GitHub private repo 생성 (또는 기존 사용)
- [ ] 서버 PC 에서 git clone
- [ ] `pip install -r requirements.txt`
- [ ] HWP 원본 파일 외장 SSD로 복사 → 서버 PC `exercise/` 에 이동
- [ ] `firebase-key.json` 배치 (`server_data/` 또는 환경변수 지정 위치)
- [ ] 한글 레지스트리 등록 (스텔스 모드, 기존 `hwp_registry_manager.py`)

### 3-4. 서비스 등록
- [ ] NSSM 으로 FastAPI 윈도우 서비스 등록
  - 이름: `NaegiwangbankServer`
  - 명령: `uvicorn server.main:app --host 0.0.0.0 --port 8000 --workers 1`
- [ ] cloudflared 윈도우 서비스 등록
  - `cloudflared service install`
  - 프로덕션 터널 생성 + config.yml
- [ ] 자동 시작 테스트 (재부팅 후 둘 다 자동 가동 확인)

### 3-5. 통합 테스트 (서버 PC + 외부)
- [ ] 학원 안 LAN IP 직접 접속: `http://192.168.x.x:8000`
- [ ] 외부 Cloudflare URL 접속: `https://<tunnel>.cfargotunnel.com`
- [ ] 맥 사파리에서 외부 URL 접속 — 로그인 → 출제 → 다운로드 전 흐름
- [ ] 동시 3명 시나리오 재현
- [ ] 24시간 가동 후 메모리 누수/한글 누수 확인

### 3-6. 새 선생님 계정 + 안내
- [ ] Firebase Auth 에 새 선생님 계정 생성 (admin이 user_admin.py 사용)
- [ ] role 설정 (user)
- [ ] 새 선생님에게 URL + ID/PW 전달
- [ ] 사용 가이드 1페이지 작성 (로그인 → 출제 → 다운로드)

### 산출물
- 서버 PC 24/7 가동
- 새 선생님 사용 가능 환경

---

## 🎯 출시 (2026-05-09) — 전 기능 동등성 99%
- 새 선생님(맥) 첫 사용일
- 윈도우 선생님은 기존 .exe 계속 사용 (5/30까지 병행 가능)

---

## Phase 4: 원본 출제 화면 (2026-05-12 ~ 14)

### 4-1. 파일 검색 API
- [ ] `GET /api/source_files?source=...&query=...` — 파일명 검색
- [ ] 기존 `main_gui.py` 의 원본출제 탭 로직 참고

### 4-2. 원본 출제 페이지
- [ ] `server/templates/original_exam.html` — 소스→파일→문제 목록
- [ ] `server/templates/partials/file_list.html`
- [ ] `server/templates/partials/original_problem_list.html` (읽기전용)
- [ ] `GET /original` — 페이지 진입

### 4-3. 원본 출제 API
- [ ] `POST /api/exam/original` — 파일+문제ID → 큐 등록
- [ ] HWP 워커가 `hwp_generator.generate_original_exam` 호출

### 4-4. 검증
- [ ] 수특 파일 1개 → 5문제 → 출제 → 다운로드
- [ ] 내신 파일 → 전체 문제 → 출제 → 다운로드

---

## Phase 5: 시험지 관리 화면 (2026-05-15 ~ 16)

### 5-1. 시험지 저장
- [ ] `POST /api/library` — 출제 결과 메타 저장 (Firestore saved_tests)
- [ ] 출제 직후 자동 저장 옵션 추가

### 5-2. 시험지 목록 페이지
- [ ] `server/templates/library.html` — 저장된 시험지 리스트
- [ ] `GET /library` — 페이지 진입
- [ ] `GET /api/library` — 목록 조회 (페이징)
- [ ] `DELETE /api/library/{id}` — 삭제

### 5-3. 출제 제외 (랜덤 출제와 연계)
- [ ] 시험지 선택 시 그 안의 problem_id를 출제 제외에 추가
- [ ] 기존 `_oneshot_excluded_problem_ids` 흐름을 서버 세션으로 이전

---

## Phase 6: 문제 등록·관리 (2026-05-19 ~ 22, admin 전용)

### 6-1. 문제 관리 화면
- [ ] `server/templates/manage.html` — 문제 검색·편집
- [ ] `GET /manage` — admin 가드
- [ ] difficulty/unit_code 편집 + lock 플래그
- [ ] is_excluded 토글

### 6-2. 문제 등록 화면
- [ ] `server/templates/register.html` — HWP 파일 업로드 또는 서버 폴더 선택
- [ ] 인덱싱 워크플로우: 소스 선택 → 파일 → 인덱싱 → 결과 검토 → 저장
- [ ] 진행률 표시 (인덱싱은 시간 오래 걸림)
- [ ] 기존 `hwp_metadata_parser_v2` 재사용

---

## 위험 요소 / 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 서버 PC 구입 지연 | 출시 데드라인 위협 | 개발자 PC를 임시 서버로 활용 → 외부 노출은 Cloudflare로 |
| 학원 인터넷 회선 불안정 | 외부 접속 품질↓ | 학원 안에서는 LAN 직접 접속 fallback |
| HWP COM 충돌/포커스 | 출제 실패 | 기존 2-인스턴스 분리 + Kill&Restart 로직 그대로 사용 |
| Firestore 쿼리 비용 증가 | 월 비용↑ | data_cache.py 의 SQLite 캐시 그대로 활용 |
| 새 선생님 계정 설정 누락 | 로그인 불가 | 5/8까지 사전 생성 + 1차 로그인 시 입회 |
| Pro→Max 전환 일정 어긋남 | 개발 속도↓ | 5/3까지는 설계만, 5/4 즉시 전환 |

---

## Phase 7: .exe 단종 (2026-05-23 ~ 05-30) — 설계도 §15 참조

### 7-1. 5/22 Phase 6 완료 직후 (기능 동결)
- [ ] 모든 선생님께 안내: "5/30 부터 .exe 종료, 이번 주(5/23~29) 웹으로 전환 부탁드립니다"
- [ ] .exe 신규 기능 추가 중단
- [ ] .exe 버그 수정 중단 (모든 작업 web 우선)

### 7-2. 5/23 ~ 05-29 시범 운영
- [ ] 모든 선생님 웹 계정 활성화 + 사용 가이드 전달
- [ ] 사용 모니터링 (웹 vs .exe 사용 비율)
- [ ] 문제 보고 받아 핫픽스 (web 만)

### 7-3. .exe 차단 메커니즘 구현 (5/27 ~ 29 사이)
- [ ] Firestore 에 `system/config` 문서 생성: `legacy_exe_disabled: false` (초기)
- [ ] `backend/main_gui.py` 부팅 시 첫 Firestore read 에서 플래그 체크 코드 삽입
- [ ] True 일 때 다이얼로그 (웹 주소 안내) + sys.exit(0)
- [ ] 마지막 .exe 빌드 (체크 로직 포함, v1.0.13)
- [ ] 모든 선생님께 마지막 .exe 배포

### 7-4. 5/30 (금) 차단 실행
- [ ] Firestore 플래그 `legacy_exe_disabled: true` 로 변경 (1초 작업)
- [ ] 학원 카톡으로 차단 공지 + 웹 URL 안내
- [ ] 첫 시간 모니터링 (웹 접속률 100% 확인)

### 7-5. 단종 후 정리 (5/31 이후)
- [ ] `dist/` 외장 백업 후 폴더 삭제
- [ ] `build.bat`, `naegiwangbank.spec`, `_bump_version.py` 삭제
- [ ] `backend/main_gui.py` 6,902줄 — `_legacy/` 폴더로 이동 + deprecated 표시
- [ ] `system_architecture.md` 전면 개편 (서버-클라이언트 모델로)
- [ ] `배포용설계도.md`, `배포용_작업현황.md` → `_legacy/` 폴더로 이동

---

## 완료 후 정리 작업

- [ ] 사용 가이드 문서 작성 (선생님용 1page)
- [ ] 백업 자동화 스크립트 (HWP 원본 외장 SSD 미러링)
- [ ] 모니터링 대시보드 (출제 통계, 응답 시간, 에러율)

---

## 변경 이력

- **2026-05-02**: 문서 최초 작성. Phase 0 완료, Phase 1~6 일정 확정.
