# 배포용 작업현황

> 설계는 `배포용설계도.md` 참조.

---

## Phase 0: 사전 결정 (2026-04-30 완료)
- [x] 1년 만료 → PC 인증으로 대체
- [x] PC 1대 / 계정 1개 정책 확정
- [x] PC 변경 = 관리자 수동 reset
- [x] PC 식별 = Windows Machine GUID
- [x] 첫 로그인 자동 등록
- [x] 관리자 PC 잠금 면제
- [x] 빌드 = `build.bat` 1버튼
- [x] 버전 = 자동 +1
- [x] 업데이트 = 수동 배포
- [x] 캐시 = `%APPDATA%\naegiwangbank\`
- [x] 코드 난독화 = PyArmor 무료판 (강도 중)

## Phase 1: PC 인증 구현 (2026-04-30 완료)
- [x] `pc_identity.py` 작성 — Machine GUID 읽기 (HKLM\SOFTWARE\Microsoft\Cryptography\MachineGuid)
- [x] `verify_or_register_device(uid, role)` — admin 면제, user 첫 등록/검증/차단
- [x] `auth.login()` 에 device 검증 step 삽입
- [x] `auth.load_cached_session()` 에도 device 검증 (캐시 복사 공격 차단)
- [x] 단위 테스트: admin 면제 ✅, 미등록 user → DeviceError ✅, Machine GUID 정상 추출 ✅
- [x] 관리자 reset 가이드 작성 ↓

### 관리자(선생님) 기기 reset 절차
**상황**: 사용자가 컴퓨터를 바꿨거나 포맷한 후 "다른 컴퓨터에서 사용 중" 메시지가 나올 때
1. Firebase Console → Firestore Database → `users` 컬렉션 열기
2. 해당 사용자 doc 클릭 (display_id 로 검색)
3. `device_guid` 필드 우측 점3개 메뉴 → **"필드 삭제"**
4. (선택) `device_registered_at` 도 삭제
5. 사용자에게 "이제 새 PC 에서 다시 로그인하세요" 안내
6. 사용자 다음 로그인 시 새 PC GUID 가 자동 등록됨

**주의**: device_guid 를 직접 새 값으로 덮어쓰지 말고 **삭제(null)** 로 만들어야 첫 로그인 자동 등록 흐름이 동작.

## Phase 2: admin SDK 키 분리 (2026-05-01 진행 중)

### 설계 절충 결정 (2026-05-01)
- 원래 설계도 §4: "클라이언트 = Web SDK, admin SDK 키는 선생님 PC만"
- **실제 채택**: 클라이언트도 admin SDK 사용 + **키만 .exe 번들에서 분리** (절충안)
  - 이유: Web SDK 전환은 firestore_engine.py 전면 재작성 (4-A/B/C/D/Phase5 모두) → 별도 Phase 로 분리
  - 본 절충안의 보호: .exe 단독 유출 시 admin 권한이 같이 새지 않음 (키 파일은 별도 채널)
- 완전 Web SDK 전환 = **Phase 6** (firebase_init.py 주석에 명시) — 추후 작업

### 코드 현황 점검 (2026-05-01 분석)
- [x] firestore_engine 사용 패턴 분석 — admin SDK 통일 사용 (READ/WRITE 모두). user vs admin 분리는 **미구현**, Security Rules 만이 분리선
- [x] **이미 구현되어 있던 것들** (작업현황 stale 였음):
  - [x] `backend/firebase_init.py` — 키 외부 탐색 (env → .exe 옆 → %APPDATA% → backend/) + `KeyNotFoundError`
  - [x] `naegiwangbank.spec` line 26-28 — admin 키 번들 제거 ("Phase 2 완료" 주석)
  - [x] `backend/main_gui.py` line 7660-7673 — `init_admin_sdk()` + 누락 시 다이얼로그
  - [x] `firestore.rules` — admin/user 분리 규칙 작성됨

### 검증 진행 (2026-05-01)
- [x] **키 위치 정리 완료**:
  - `backend/naegiwangbank-firebase-adminsdk-*.json` — 이미 제거됨 (없음)
  - `%APPDATA%\naegiwangbank\firebase-key.json` — 활성 키 (2026-04-30 이전됨)
  - `_archive_admin_key_DO_NOT_DISTRIBUTE.json` — 프로젝트 루트 백업 (배포 제외 필수)
- [x] dev 모드 `firebase_init.find_key_path()` 동작 확인 → `%APPDATA%\naegiwangbank\firebase-key.json` 발견 ✓
- [x] Firestore Rules 라이브 릴리스 확인 (deploy_firestore_rules.py --status):
  - `projects/naegiwangbank/releases/cloud.firestore` updateTime=2026-04-27
  - 현재 `firestore.rules` 파일 dry-run 파싱 통과 ✓
- [x] **deploy_firestore_rules.py 패치**: 하드코딩 SERVICE_ACCOUNT 경로 → `firebase_init.find_key_path()` 위임
- [x] **dist/v1.0.3 폐기**: `backups/discarded/naegiwangbank_v1.0.3_HAD_ADMIN_KEY.exe` 로 이전 (당시 spec에 키 임시 포함됐었음)
- [x] **v1.0.4 재빌드 성공**: `dist/naegiwangbank_v1.0.4.exe` (85MB)
  - 빌드 로그(`build_logs/build_v1.0.4.log`) 에서 `adminsdk`/`firebase-key` 검색 결과 0건 → 키 미포함 확인
- [x] **검증 1 완료**: 키 quarantine 후 .exe 실행 → "Firebase 키 누락" 다이얼로그 정상 표시 (탐색 6곳 안내 + "관리자에게 요청" 안내)
- [x] **검증 2 완료**: `dist\firebase-key.json` 배치 후 .exe 실행 → 키 발견 → 로그인 다이얼로그 → 메인 GUI 정상 부팅
- [x] **배포 시나리오 확정**: `dist\naegiwangbank_v1.0.4.exe` + `dist\firebase-key.json` 두 파일을 함께 zip → 사용자에게 전달 (firebase_init 첫 번째 후보 = `.exe 옆`)

### 알려진 미해결 이슈 (Phase 2 종료 후 추후 처리)
- **`%APPDATA%\naegiwangbank\firebase-key.json` 후보가 frozen .exe 에서 1회 미발견**:
  - dev 모드에서는 `find_key_path()` 가 정상적으로 그 경로를 반환
  - frozen .exe 에서 같은 시점, 같은 파일에 대해 `os.path.exists()` False 반환 (다이얼로그에 tried 6개 모두 노출됨)
  - 환경변수/파일 권한/파일명 모두 정상 확인 → 원인 미상
  - 우회: `.exe 옆`(첫 번째 후보) 에 키 두는 정통 배포 시나리오로 진행 → 문제 없음
  - 후속: 다른 PC 검증 시 이 케이스도 같이 모니터 (Phase 3 잔여)

### Phase 2 결과 산출물
- `backend/firebase_init.py` — 키 외부 탐색 + 누락 시 KeyNotFoundError
- `naegiwangbank.spec` — 키 번들 제외 (line 26-28)
- `backend/main_gui.py:7660-7673` — 진입 시 `init_admin_sdk()` + 누락 다이얼로그
- `firestore.rules` — admin/user 분리 (Firebase 라이브 릴리스 동일, 2026-04-27 갱신)
- `deploy_firestore_rules.py` — SERVICE_ACCOUNT 경로를 `firebase_init.find_key_path()` 위임
- `dist/naegiwangbank_v1.0.4.exe` (88 MB) — 검증 통과
- `backups/discarded/naegiwangbank_v1.0.3_HAD_ADMIN_KEY.exe` — 키 포함됐던 구 빌드 격리

### Phase 2 종료 (2026-05-01)
- ✅ 키 .exe 번들 분리 + 외부 탐색 + 누락 안내 + 정상 부팅 모두 검증
- ✅ 작업현황의 "임시로 키 spec 포함됨" 우려 해소
- ⚠ "클라이언트 = Web SDK" 완전 전환은 Phase 6 로 분리 (절충안 채택, 본 문서 상단 참조)

## Phase 3-bis: 배포 zip 자동 패키징 (2026-05-01)

### 동기
- `build.bat` 은 `.exe` 빌드까지만 — 매 빌드마다 `firebase-key.json` 수동 복사 + zip 수동 생성 필요했음
- 1버튼 배포로 끌어올리기

### 변경 (`build.bat`)
- 단계 [4/5]: `firebase_init.find_key_path()` 호출로 활성 키 자동 위치 찾아서 `dist\firebase-key.json` 으로 복사
- 단계 [4/5] 후속: PowerShell `Compress-Archive` 로 `dist\naegiwangbank_v{ver}.zip` 생성 (.exe + key.json)
- 단계 [5/5]: 완료 메시지에 `OK deploy zip: dist\...zip <-- this is what you ship` 명시

### 산출물 (정상 빌드 시)
```
dist\
  naegiwangbank_v{ver}.exe        ← 중간 산출
  firebase-key.json               ← 중간 산출
  naegiwangbank_v{ver}.zip        ← ★ 외부 배포용
```

### 운영 주의
- **zip 자체에 admin SDK 키 포함** → USB/메일/공유폴더 전달 채널의 통제 필요
- zip 만 외부에 나가야 함. `dist\` 내부 파일을 따로 보내지 말 것

### 검증 (2026-05-01)
- [x] PyInstaller 단독 호출로 v1.0.5 빌드 → `dist\naegiwangbank.exe` 생성
- [x] build.bat [3/5][4/5] 단계 시뮬레이션:
  - rename → `naegiwangbank_v1.0.5.exe`
  - `firebase_init.find_key_path()` → `%APPDATA%\naegiwangbank\firebase-key.json` 자동 복사
  - PowerShell `Compress-Archive` → `naegiwangbank_v1.0.5.zip` 생성
- [x] zip 내용 검증 (python `zipfile`):
  - `naegiwangbank_v1.0.5.exe` (88,771,014 B)
  - `firebase-key.json` (2,379 B)
  - 총 2 entries — 불필요 파일 없음 ✓
- [x] 직전 v1.0.4.exe 는 폐기 (dist 정리)

### Phase 3-bis 종료
- ✅ 1버튼 빌드 → 1버튼 zip 패키징
- ✅ build.bat 다음 호출(v1.0.6) 부터는 [1/5]~[5/5] 자동 일괄 실행
- 다음 빌드: 그냥 `build.bat` 더블클릭 → `dist\naegiwangbank_v1.0.6.zip` 생성됨

## Phase 3: 빌드 자동화 (2026-05-01 완료 — 이 PC 한정)
- [x] `version.txt` 생성 (시작 v1.0.0)
- [x] `_bump_version.py` 헬퍼 (PATCH +1)
- [x] `naegiwangbank.spec` 작성 (PyInstaller)
- [x] `build.bat` 작성 (1버튼 빌드)
- [x] 첫 빌드 스모크 테스트 (.exe 정상 실행) — v1.0.3 GUI 진입 OK
- [x] **build.bat end-to-end 검증** — v1.0.12 까지 1버튼 통과 (자동 검증 섹션 참조)
- [ ] 다른 PC 에서 동작 검증

### Phase 3 중 발견·수정한 동결모드 버그 (배포 차단급)
1. **`base_dir` 두 단계 위로 가는 문제** (`backend/main_gui.py`)
   - dev: `__file__` = `backend/main_gui.py` → 두 단계 위 = 프로젝트 루트 ✓
   - frozen: `__file__` = `_MEIPASS/main_gui.py` → 두 단계 위 = `Temp/` ✗
   - **수정**: `sys.frozen` 분기 도입, `base_dir`(쓰기) + `resource_dir`(읽기) + `_backend_dir`(번들 backend/) 분리
2. **번들 backend/* 리소스 미발견** (curriculum_config.json / unit_hierarchy.json / logo.png / firebase admin SDK key)
   - frozen 에서 main_gui 의 `__file__` 폴더는 `_MEIPASS` 인데 데이터는 `_MEIPASS/backend/` 안에 번들됨
   - **수정**: 경로 6 곳을 `_backend_dir` 로 변경, spec 에 logo.png 추가
3. **Firestore→SQLite 증분 동기화 list 바인딩 실패** (`backend/data_cache.py`)
   - Firestore 일부 doc 의 `pos_start`/`pos_end` 가 list 타입으로 저장되어 SQLite 바인딩 시 ProgrammingError
   - **수정**: `_doc_to_row` 에서 list/dict 값 자동 JSON 직렬화

### 산출물
- `dist/naegiwangbank_v1.0.3.exe` (85 MB, console=False)
- `build_logs/build_v1.0.3.log`

### 빌드 파일 구성
- `version.txt` — `MAJOR.MINOR.PATCH` (빌드 시 PATCH 자동 +1)
- `_bump_version.py` — 버전 증가 헬퍼 (build.bat 가 호출)
- `naegiwangbank.spec` — PyInstaller 설정 (datas/hiddenimports)
- `build.bat` — 1버튼 진입점 (버전 증가 → PyInstaller → rename → 로그)
- `dist/내기왕문제은행_v{ver}.exe` — 산출물
- `build_logs/build_v{ver}.log` — 빌드 로그

### 사용법
```
build.bat 더블클릭
  → 1~3분 대기
  → dist\내기왕문제은행_v{ver}.exe 생성
  → 사용자에게 그 .exe 한 파일만 전달하면 됨
```

### 주의
- `naegiwangbank.spec` 의 datas 에 `naegiwangbank-firebase-adminsdk-*.json` 가 **임시로 포함**됨
- → Phase 2 (admin 키 분리) 작업 완료 시 spec 의 해당 줄을 제거해야 함

## Phase 4: 코드 난독화 — ❌ 스킵 결정 (2026-04-30)
- 사용자 결정: PyInstaller 단독 보호로 진행. PyArmor 도입 안 함.
- 이유: 무료판은 큰 파일(main_gui 6,902줄, hwp_core 1,626줄) 거부, 부분 적용은 보호 효과 약함.
- PyInstaller 자체 번들 = 일반 사용자 기준으로는 충분한 진입장벽.

## Phase 5: 사용자 배포 준비
- [ ] 사용자 매뉴얼 작성
- [ ] 첫 실행 가이드 (Firebase 로그인, HWP 설치 안내)
- [ ] 시범 배포 (2~3명 베타)

---

## build.bat end-to-end 자동 검증 (2026-05-01 ✅ 완료)

### 결론
**build.bat 1버튼이 [1/5]~[5/5] 전 단계 끝까지 RC=0 통과 (93초). 산출물: `dist\naegiwangbank_v1.0.12.exe` + `dist\naegiwangbank_v1.0.12.zip`.**

### 검증 과정에서 발견·수정한 build.bat 자체 버그 3건
실제로는 **build.bat 자체에 잠재 버그가 3개** 있었음. 더블클릭으로 돌렸을 때도 zip은 결국 안 나왔을 것 (=Phase 3-bis 가 사실상 미완성이었음). 자동 호출 시도 덕분에 발견됨.

#### 버그 1: 줄바꿈이 LF (Unix) → CRLF 변환
- **증상**: cmd 가 build.bat 전체를 한 줄로 읽어 모든 토큰을 명령으로 해석 (`-`, `3.`, `on`, `delayedexpansion`, `ING`, `ller`...)
- **원인**: Edit 도구가 LF로 저장 → Windows cmd 는 .bat 파일에 CRLF 필수
- **수정**: 117 줄 모두 CRLF (`\r\n`) 로 변환

#### 버그 2: PowerShell 인자에서 `\f` 가 form-feed 로 해석
- **증상**: `Compress-Archive -Path 'dist\firebase-key.json'` 가 `'distirebase-key.json'` 으로 해석됨 → 파일 못 찾음
- **원인**: cmd 가 PowerShell -Command 인자를 single-string 으로 넘길 때, PowerShell C-runtime 파싱 단계에서 `\f` (form feed = 0x0C) 이스케이프 처리 → 백슬래시+f 가 단일 0x0C 바이트로 변환되며 "f" 가 사라짐
- **수정**: build.bat:98 의 PowerShell 명령에서 `\` → `/` (Windows PowerShell 은 forward slash 정상 처리)
  ```diff
  - powershell -NoProfile -Command "Compress-Archive -Path 'dist\%OUTNAME%','dist\firebase-key.json' -DestinationPath 'dist\%ZIPNAME%' -Force"
  + powershell -NoProfile -Command "Compress-Archive -Path 'dist/%OUTNAME%','dist/firebase-key.json' -DestinationPath 'dist/%ZIPNAME%' -Force"
  ```

#### 버그 3: `if errorlevel 1 (` 블록 내부의 `(.exe is OK ...)` 가 블록을 조기 종료
- **증상**: python -c 가 errorlevel 0 (성공) 으로 끝나도 build.bat 가 항상 RC=1 로 끝남. zip 단계 진입 못 함.
- **원인**: build.bat:91 의 `echo (.exe is OK at dist\..., but you must add key manually)` 안에 escape 안 된 `(`/`)` 가 있어서, cmd 의 if 블록 파서가 그 자리에서 닫는 `)` 를 읽고 조기 종료. 결과적으로 `exit /b 1` 이 if 바깥에 있는 것처럼 무조건 실행됨.
- **수정**: 괄호 escape — `^(...^)`
  ```diff
  -     echo         (.exe is OK at dist\%OUTNAME%, but you must add key manually)
  +     echo         ^(.exe is OK at dist\%OUTNAME%, but you must add key manually^)
  ```

### 자동 호출 노하우 (한글 경로 + 비대화형 셸)
이전엔 "자동 호출 자체가 안 됨" 으로 결론 내렸으나, 결국 **Python subprocess** 가 답이었음:
```python
import subprocess, os
os.environ['CI'] = '1'
os.environ.pop('NoDefaultCurrentDirectoryInExePath', None)  # 시스템 default=1 인데 cmd 가 cwd 를 PATH 에서 빼버림
os.chdir(r'G:\문제은행\문제은행2')
subprocess.run(['cmd', '/c', r'G:\문제은행\문제은행2\build.bat'])
```
- 핵심: bash/PowerShell 은 한글 경로를 cmd 에 넘길 때 인코딩이 깨짐. **Python 의 subprocess 는 CreateProcessW (Unicode API) 를 쓰므로 한글 경로 정상 전달**.
- 또 하나: Windows 시스템에 `NoDefaultCurrentDirectoryInExePath=1` 가 켜져 있으면 cmd 가 cwd 를 검색 안 함 → 절대 경로로 build.bat 를 호출하거나 환경변수 제거 필요. 더블클릭은 Explorer 가 처리해서 무관.

### 산출물 (이 PC 검증 완료)
- `dist\naegiwangbank_v1.0.12.exe` (88 MB)
- `dist\naegiwangbank_v1.0.12.zip` (84 MB) ← **이걸 배포하면 됨**
- `dist\firebase-key.json`
- `build_logs\build_v1.0.12.log`
- `version.txt` = `1.0.12` (다음 빌드 → v1.0.13 자동 +1)

### 다음 PC 에서 검증할 내용 (Phase 3 마지막 체크)
- v1.0.12.zip 풀고 `naegiwangbank_v1.0.12.exe` 더블클릭 → 로그인 → 정상 진입
- 첫 로그인 시 PC GUID 등록 확인 (Firestore Console `users/{uid}.device_guid` 채워짐)
- 두 번째 로그인 시 캐시된 세션 + GUID 검증 동작 확인
