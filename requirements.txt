# 내기왕 문제은행 서버 의존성 (2026-05-02)
# 설치: pip install -r requirements.txt

# --- 웹 프레임워크 ---
fastapi==0.128.0
uvicorn==0.40.0
python-multipart==0.0.21       # form-data 파싱
jinja2==3.1.6                   # 템플릿 엔진

# --- 인증 / 세션 ---
PyJWT==2.10.1
requests==2.32.5                # Firebase Auth REST 호출

# --- Firebase / Firestore ---
firebase-admin==7.1.0
google-cloud-firestore==2.22.0

# --- HWP COM 자동화 (윈도우 전용) ---
pywin32==311
pyhwpx==1.6.8

# --- 기타 (기존 backend/ 에서 사용) ---
# 필요 시 추가: pillow, pandas 등은 기존 코드에서 import 시점에 별도 명시
