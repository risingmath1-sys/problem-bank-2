# 🛠 로그인시스템 작업현황

> **이 파일은 작업이 끊기더라도 다음 세션이 이어갈 수 있도록 모든 단계를 기록합니다.**
> 매 작업 시작/종료 시 갱신.
> 설계 원본 = `로그인시스템설계도.md`

---

## 진척 요약

| Phase | 상태 | 일자 |
|---|---|---|
| 0. 설계 | ✅ 완료 | 2026-04-26 |
| 1. problems 이주 | ✅ 완료 | 2026-04-26 |
| 2. 로그인 시스템 | ✅ 완료 | 2026-04-26 ~ 2026-04-27 |
| 3. 개인 데이터 이주 | ✅ 완료 | 2026-04-27 |
| 4. GUI 데이터 소스 전환 | ✅ 완료 (4-A·4-B·4-B+·4-C·4-D 완료) | 2026-04-27 ~ 2026-04-28 |
| 5. 인덱서 직접 쓰기 | ✅ 완료 | 2026-04-28 |
| ★ 데이터 최적화 (1·2순위) | ✅ 완료 | 2026-04-28 |
| ★ Firestore 순정화 (default 전환 + Dialog 와이어링 잔여) | ✅ 완료 | 2026-04-28 ~ 2026-04-29 |
| ★ 코드 위생 (AUDIT 중복 제거 + filter API 마이그) | ✅ 완료 | 2026-04-30 |
| ★ 데이터 잔존 표기 정리 (NAESIN_ANG·MOCK_EXAM 단원코드) | ✅ 완료 | 2026-04-30 |

---

## ★ 데이터 잔존 표기 정리 (2026-04-30)

### 동기
오래된 메모리(2026-04-01)에 남아 있던 두 가지 "미완료 항목"을 실제 DB 와 대조 → 둘 다 사실상 결정 완료된 상태였음. 표기만 정합화.

### 점검 결과

#### 1) NAESIN_ANG → NAESIN_A 마이그레이션
- 메모리 기록: "1,228건 구 소스타입 잔존, 마이그레이션 필요"
- **실측 (2026-04-30)**:
  - SQLite `problem_bank.db`: NAESIN_ANG **0건**, NAESIN_A 6,500건
  - Firestore `problems`: NAESIN_ANG **0건**, NAESIN_A 6,500건
- **결론**: 이미 통합 완료. 메모리/문서 표기만 stale.

#### 2) MOCK_EXAM unit_code 984건 빈값
- 메모리 기록: "인덱싱 미완료"
- **실측 (2026-04-30)**:
  - SQLite/Firestore 양쪽 — NULL 0건, 빈 문자열("") 984건. 형태는 이미 통일.
- **선생님 결정 (2026-04-30)**: 단원분류 안 함, **의도적으로 빈 값으로 박아둠**. 추가 인덱싱/채움 작업 안 함.

### 처리
- 코드/DB 변경 **없음** (이미 깨끗하므로).
- `MEMORY.md` 갱신:
  - "DB 현재 상태" 섹션 (2026-04-01 → 2026-04-30, 실측치 반영)
  - "소스 타입" 에서 NAESIN_ANG 를 통합 완료로 명시
  - "현재 미완료 작업" 분리 → "의도적 빈값 / 결정 완료" 섹션 신설 + MOCK_EXAM 984건은 의도적 빈값 명시

### 변경/유지
- **변경**: `MEMORY.md` (3개 섹션) / `로그인시스템_작업현황.md` 본 문단.
- **유지**: 모든 코드, 모든 DB 도큐먼트.

---

## ★ 코드 위생: 잔존 노이즈 정리 (2026-04-30)

### 동기
이전 단계에서 발견된 두 가지 사소한 잔존 이슈 — Firestore 모드의 AUDIT 로그 중복(4-D 메모) 과 `firestore_v1` positional `where()` UserWarning(4-B/4-C/4-D 공통) — 일괄 처리. 기능 변화 없음. 운영 노이즈 제거 + SDK 미래 호환성 확보.

### 1) AUDIT 로그 중복 제거
- 증상: Firestore 모드의 `set_exclusion_status` 호출 시 `[EXCL_AUDIT ... via=Firestore]` 가 1회, 직후 cache 위임으로 `LocalDBEngine.set_exclusion_status` 가 다시 1회 → 같은 작업이 두 줄.
- 처리:
  - `backend/db_engine.py:set_exclusion_status` — `os.environ.get("EXCL_AUDIT_SUPPRESS") == "1"` 일 때 AUDIT 블록 스킵.
  - `backend/firestore_engine.py:set_exclusion_status` — cache 위임 직전 환경변수 set, finally 에서 원복.
- 결과: Firestore 모드에서도 한 호출에 한 줄만 기록. SQLite 단독 모드는 동작 무변.

### 2) `where()` positional API → `filter=FieldFilter(...)` 키워드 마이그
- 증상: `firestore_v1` positional `where(field, op, value)` 가 deprecation UserWarning 다발. 코드 중간 출력 노이즈 + 차후 SDK 메이저 업그레이드 시 제거 예상.
- 처리: `from google.cloud.firestore_v1.base_query import FieldFilter` import 추가 + `.where(filter=FieldFilter(field, op, value))` 로 일괄 교체.
- 범위: `backend/data_cache.py` 1곳 + `backend/firestore_engine.py` 28곳 = **총 29곳**.
- 검증: `python -W error::UserWarning` 로 직접 호출 → folders/tests/scores 정상, 경고 0건.

### 검증 (회귀)
| 검증 | 결과 |
|---|---|
| `tests/test_engine_parity.py` (4-A) | **20/20 PASS** |
| `tests/test_4b_plus_smoke.py` (4-B+) | **20/20 PASS** |
| `tests/test_4c_write_smoke.py` (4-C) | **40/40 PASS** |
| `tests/test_4d_write_smoke.py` (4-D) | **27/27 PASS** |
| `tests/test_5_indexer_smoke.py` (Phase 5) | **29/29 PASS** |
| `tests/test_engine_parity_4b.py` | **30/30 PASS** |
| `python -W error::UserWarning` 호출 (folders/tests/scores) | **NO USERWARNINGS** |

총 **166/166 PASS** + UserWarning 0건.

### 변경/유지
- **변경**: `backend/db_engine.py`(AUDIT 가드 1곳) / `backend/firestore_engine.py`(import + 28 where + AUDIT suppress 토글) / `backend/data_cache.py`(import + 1 where).
- **유지**: 메서드 시그니처 / 캐시 스키마 / 테스트 ID 시퀀스 / GUI 호출부.

---

## ★ Firestore 순정화 (2026-04-28 시작 → 2026-04-29 마감)

### 동기
- 로컬 DB 폴백을 끊고 평소 부팅이 Firestore 를 정 값으로 쓰게 만든다.
- 4-B+ 와이어링이 `ModernProblemScannerGUI` 28곳만 잡아주고, 그 클래스 밖에 사는 다이얼로그는 누락된 상태였음 → 채점/제외 흐름 진입 시 즉시 `AttributeError` 로 폭발.

### 1) data_engine.py 기본값 전환 (2026-04-28 22:10)
- `get_engine_mode()` 의 default 를 `"sqlite"` → `"firestore"` 로 변경.
- `make_engine()` 에서 알 수 없는 값/미지정 → Firestore 로 폴백 (sqlite 는 `DATA_ENGINE=sqlite` 명시 시에만 rollback 모드).

### 2) Dialog 클래스 `current_user_id` 와이어링 (2026-04-29)
- 증상: `ScoringDialog._open_save_dialog()` 진입 시
  ```
  AttributeError: 'ScoringDialog' object has no attribute 'current_user_id'
  ```
  (debug_console.log 끝부분 동일 traceback 3회)
- 원인: 두 다이얼로그 클래스가 `self.gui` (메인 GUI 참조) 는 들고 있는데 `self.current_user_id` 로 잘못 접근.
- 수정: `self.current_user_id` → `self.gui.current_user_id` (총 10곳)
  - `ScoringDialog` (line 7001~) 5곳 — 7189, 7197, 7205, 7207, 7237
  - `ExclusionSettingDialog` (line 7306~) 5곳 — 7520, 7521, 7555, 7579, 7608
- 호출부 양쪽 다 `gui_instance=self` / `gui=self` 정상 전달 확인.

### 검증
| 검증 | 결과 |
|---|---|
| `python -c "import ast; ast.parse(...)"` (syntax) | OK |
| `inspect.getsource()` 로 두 다이얼로그에 `self.current_user_id` 잔존 0건 | OK |
| `tests/test_4b_plus_smoke.py` (양 모드 회귀) | **20/20 PASS** |

### 변경/유지
- **변경**: `backend/data_engine.py` default mode, `backend/main_gui.py` 다이얼로그 와이어링 10곳.
- **유지**: 메서드 시그니처 / DB 엔진 / 캐시 구조 / Phase 4-D 까지의 모든 산출물.

### 3) Phase 3 이후 SQLite-only 누락 데이터 보충 (2026-04-29)
- 증상: SQLite 에는 김정환 폴더(id=32) + "모의기출 B -1" 시험지(id=219) 가 있는데 Firestore 에 없음.
- 원인: Phase 3 이주(2026-04-27) 이후 ~ 4-C 완료(2026-04-28) 사이에 SQLite 모드로 작성된 시간차 데이터.
- 처리: `migrate_user_data_to_firestore.py` 의 `doc_*` 변환 함수를 import 하여 단건 set.
  - `user_directories/32` (김정환, parent=15) → 추가
  - `saved_tests/219` (모의기출 B -1, dir=32, 문제 50건) → 추가
  - 관련 `problem_scores` (test_id=219): SQLite 0건이라 옮길 것 없음.
- 검증: `db.get().exists == True`, name/title/문제수 일치.
- 부수 점검:
  - Firestore-only saved_tests `169`/`224` 는 GUI 가 Firestore 모드로 직접 작성한 정상 데이터 → 보존.
  - `problem_scores` Firestore 2331 vs SQLite 2131 (200건 차) = 채점 작업분이 Firestore-only → 정상.

### 5) 신규 사용자 '오답' 보호 폴더 자동 발급 (2026-04-29)
- 동기: 4) 에서 default uid 부트스트랩을 제거 → 신규 사용자가 보호 폴더 없이 발급됨 → 채점 다이얼로그가 default 폴더(=root)로 떨어짐.
- 처리:
  - `firestore_engine.py` 모듈 함수 추가:
    - `next_int_doc_id(fs, collection)` — `_FirestoreEngine._next_int_id` 와 동일 채널, 인스턴스 없이 호출.
    - `ensure_protected_folder(fs, uid, name="오답")` — 멱등. 이미 있으면 그 id, 없으면 발급.
  - `user_admin.create_user()` — Auth+Firestore users/{uid} 생성 직후 `ensure_protected_folder(db, uid, "오답")` 호출. 실패해도 사용자 발급 자체는 성공.
- 채점 → 오답 폴더 기본 선택은 `ScoringDialog._open_save_dialog`(main_gui.py:7187~) 가 이미 구현 (combobox + `get_protected_folder_id`); 사용자가 다른 폴더로 변경 가능.
- 검증 (smoke):
  - 기존 admin '오답' (id=8) → 8 반환 ✓
  - 신규 보호 폴더 생성 → counter 채번 ✓
  - 동일 이름 재호출 → 같은 id 반환 (idempotent) ✓
  - doc 필드 (`user_id`, `is_protected=True`, `parent_id=None`) 일치 ✓
- 회귀: parity_4b 30/30, 4b_plus 20/20, 4c_write 40/40, 4d_write 27/27, parity 20/20 — 전부 PASS.

### 4) `default` uid 자동 부트스트랩 제거 + parity_4b 테스트 재작성 (2026-04-29)
- 증상: SQLite `user_directories/31` ("오답", uid=`default`) 가 admin 마이그에서 누락. `LocalDBEngine.init_user_tables` 가 매 부팅마다 default 사용자 기준으로 보호 폴더를 자동 생성하던 잔재.
- 처리:
  - `db_engine.py:1068-1073` default 부트스트랩 코드 제거 (4-B+ 이후 admin uid 기준 — `user_admin.create_user()` 에서만 폴더 생성).
  - `get_protected_folder_id(name, user_id)` 의 `="default"` 기본값 제거 (`db_engine.py:1253`, `firestore_engine.py:183`) → 호출부에 명시적 user_id 강제.
  - 잔재 `user_directories/31` (테스트/시험지 0건 확인 후) 1건 삭제.
- parity_4b 테스트 재작성: `tests/test_engine_parity_4b.py`
  - 이전 전제(SQLite default vs Firestore admin) 폐기 — 4-B+ 마이그 + Firestore 순정화로 양쪽이 admin uid 통합되었으나 Firestore 가 단방향 authoritative 가 됨 (SQLite 미러 안 됨).
  - 새 형태 = FirestoreEngine 17개 READ 메서드 smoke test (shape/range/subset 검증).
  - 결과: **30/30 PASS**.
- 회귀: `test_4b_plus_smoke` 20/20, `test_4c_write_smoke` 40/40, `test_4d_write_smoke` 27/27, `test_5_indexer_smoke` 29/29, `test_engine_parity` 20/20 — 전부 PASS.

---

## Phase 0. 설계 (2026-04-26)

### 결정 사항
- **빌드 정책 = C안**: 단일 .exe + 메뉴 숨김 + Firestore Rules 이중 잠금
- **계정 발급 = 관리자 발급 only**. 자가가입 차단
- **계정 ID 형식 = 가짜 도메인 통일**: 사용자에게는 `admin`, `kim` 같은 짧은 ID 만 노출, 내부적으로는 `admin@sangsung.local`, `kim@sangsung.local` 로 Firebase Auth 에 저장
- **데이터 최적화**: 별도 단계로 인지하되 상세는 나중에 결정 (설계도 §11 참조)

### 산출물
- `로그인시스템설계도.md` — 마스터 설계 문서

---

## Phase 1. problems 이주 (2026-04-26 완료)

### 시작 전 상태
- SQLite `problem_bank.db` problems 29,451 행 (28 컬럼)
- Firestore `problems` 컬렉션 = 테스트 20건 (옛 이미지 방식 스키마 — 좌표 방식과 비호환)
- Firebase 키: `backend/naegiwangbank-firebase-adminsdk-fbsvc-5e5e798b33.json`

### 작업 순서
1. SQLite 백업 → `problem_bank.db.bak_pre_migration_20260426_155650` (20.4 MB)
2. ID 검증: `/` 없음, NULL/empty 없음, `__xxx__` 패턴 없음, 중복 없음, 최대 101자 ✓
3. 마이그레이션 스크립트 작성: `migrate_problems_to_firestore.py`
   - 모드: `--check`, `--cleanup`, `--sample N`, `--full`, `--verify`
   - idempotent (`set()` 사용 → 재실행 안전)
   - batch=500 commit
4. 기존 20건 cleanup
5. 10건 샘플 업로드 → 정상 확인
6. `--full` 1차 시도 → **19,500건에서 quota 초과 중단** (Spark 무료플랜 일일 20K writes 한도)
7. **Firebase 결제 플랜 Spark → Blaze 전환**
8. `--full` 재시도 → **29,451건 / 79.1초 / 372 docs/s 완료**
9. `--verify` → count 일치 ✓ + 표본 5건 전 필드 일치 ✓

### 산출물
- `migrate_problems_to_firestore.py` — 재사용 가능한 이주 스크립트
- `problem_bank.db.bak_pre_migration_20260426_155650` — 안전 백업

### 발견된 이슈 (별도 점검 필요, 지금 손대지 않음)
- source 코드 `NAESIN_A` (5,801건), `NAESIN_N` (7,265건) 은 `system_architecture.md` 에 미문서화
- 이주 시 그대로 보존, 추후 표준화 결정 필요

### 변경/유지 항목
- **변경**: Firebase 결제 플랜 Spark → Blaze
- **유지**: SQLite 는 그대로 둠 (GUI 는 Phase 4 까지 SQLite 그대로 사용)

---

## Phase 2. 로그인 시스템 (2026-04-26 진행중)

### 결정 사항
- 첫 admin 계정: ID=`admin` (내부: `admin@sangsung.local`) / 비번=`123456` (2026-04-27 변경)
- 로그인 화면 = **모달**로 메인 GUI 시작 시 띄움
- Firebase Authentication "이메일/비밀번호" 사용
- `users/{uid}` 문서 자동 생성 (관리자 발급 시)

### 작업 계획 (TODO)
- [ ] 2-A. Firebase Console 에서 Authentication "이메일/비밀번호" 활성화
- [ ] 2-B. 첫 admin 계정 시드 스크립트 (`seed_first_admin.py`)
  - Firebase Auth 사용자 생성 (`admin@sangsung.local` / `1213456`)
  - Firestore `users/{uid}` 문서 생성 (`role="admin"`, `active=true`)
- [ ] 2-C. 로컬 인증 모듈 (`backend/auth.py`)
  - `login(id, pw) → (uid, role)` 또는 예외
  - 토큰 캐시 `%APPDATA%\sangsung_math\auth_token.json`
  - 자동 로그인 (캐시 유효 시)
  - 로그아웃 (캐시 삭제)
- [ ] 2-D. 로그인 모달 GUI (`backend/login_dialog.py`)
  - 메인 GUI 시작 시 첫 진입
  - ID/비번 입력 → `auth.login()` 호출
  - 성공 시 → `current_user_id`, `current_user_role` 세팅 후 메인 GUI 진입
  - 실패 시 → 에러 메시지 + 재시도
- [ ] 2-E. 메인 GUI 통합 (`main_gui.py:67` 의 `current_user_id = "user1"` 제거)
- [x] 2-F. role 기반 메뉴 숨김 (admin 만 보이는 메뉴 식별 + 가드)
- [x] 2-G. Firestore Security Rules 작성 + 배포
- [x] 2-H. admin 의 "사용자 관리" 탭 (신규 계정 발급 / 비번 재설정 / 비활성화 / 역할변경 / 삭제)

### 진행 기록
- 2026-04-26: 설계 확정, 작업 계획 수립
- 2026-04-26: **2-A 완료** — Firebase Console 에서 이메일/비밀번호 Provider 활성화 (선생님 직접)
- 2026-04-26: **2-B 완료** — `seed_first_admin.py` 작성 + 실행
  - admin 계정 시드 성공: `display_id=admin / email=admin@sangsung.local / pw=123456` (최초 1213456 → 2026-04-27 123456 변경)
  - Firebase Auth uid: `8YrxTdZYYcYts5bm4pkOdog5Bn92`
  - Firestore `users/8YrxTdZYYcYts5bm4pkOdog5Bn92` 문서 생성 (`role=admin`, `active=true`)
- 2026-04-26: **2-C 완료** — `backend/auth.py` 작성
  - Firebase Auth REST API 사용 (Web API Key = `frontend/src/firebase.js` 에서 발견)
  - 토큰 캐시: `%APPDATA%\naegiwangbank\auth_token.json`
  - 함수: `login(id, pw)`, `logout()`, `load_cached_session()`, `is_admin()`, `get_current_user()`
  - 친화적 에러 메시지 매핑 (`EMAIL_NOT_FOUND`, `INVALID_PASSWORD`, ...)
  - 4가지 시나리오 단위 테스트 통과: 정상 / 잘못된 비번 / 모르는 ID / 캐시 복원
- 2026-04-26: **2-D 완료** — `backend/login_dialog.py` 작성
  - 다크 테마 모달 (380x280)
  - ID + 비번 입력, Enter 키 흐름, ESC 취소
  - 캐시 세션 있으면 다이얼로그 안 뜨고 즉시 반환 (자동 로그인)
  - 3가지 시나리오 통합 테스트 통과: 정상 / 잘못된 비번 후 재시도 / 캐시 자동 로그인
- 2026-04-27: **2-H 완료** — 사용자 관리 탭
  - `backend/user_admin.py` 신규 작성 — Firebase Admin SDK 래퍼
    - `list_users()` / `create_user()` / `reset_password()` / `set_active()` / `update_role()` / `delete_user()`
    - role 검증, 비번 6자 이상 강제, 본인 계정 보호는 GUI 에서 처리
  - `main_gui.py` Tab 6 "사용자 관리" 추가 (admin 전용)
  - 사용자 목록 행 액션: 비번 재설정 / 비활성화·활성화 / 역할 토글 (admin↔user) / 삭제
  - "+ 새 계정 발급" 다이얼로그: ID + 이름(선택) + 임시비번 + role 라디오
  - 본인 계정의 비활성화/역할변경/삭제는 GUI 차원에서 차단 (UI 가드)
  - 검증: list_users() 1명 (admin) 정상 조회
- 2026-04-27: **2-F 완료** — role 기반 메뉴 숨김
  - `main_gui.py` 탭 추가/빌드 시 `if self.current_user_role == "admin"` 가드
  - 일반 user 에게 "문제 등록 (Tab 4)", "문제 관리 (Tab 5)" 탭 노출 안 됨
  - 홈 화면 메뉴 카드에서도 admin_only 항목 (문제 등록 / 문제 관리) 제외
  - admin 으로 로그인 시 6개 탭 모두 보임 (검증: import OK)
- 2026-04-27: 추가 — 로그인 다이얼로그 강제 표시 (deiconify + topmost), 메인 GUI 도 같은 처리, [실행.bat](실행.bat) ASCII 진단 버전으로 보강
- 2026-04-26: **2-E 완료** — `main_gui.py` 통합
  - `if __name__ == "__main__":` 블록 — Firebase Admin SDK 초기화 + 로그인 모달 → 메인 GUI
  - `ModernProblemScannerGUI.__init__(root, session=...)` 시그니처 변경
  - `self.current_user_id`, `current_user_role`, `current_user_display_id`, `current_user_display_name` 세팅
  - 하단 정보 바: `사용자: admin (관리자)` 표시 (admin 일 때 태그 노출)
  - 헤드리스 통합 테스트 통과 (로그인 → GUI 초기화 → session 주입 검증)
- 2026-04-27: **2-G 완료** — Firestore Security Rules 작성 + 배포 + 검증
  - `firestore.rules` 작성 (설계도 §3-2 권한 모델 그대로 반영)
    - users / problems / problem_preferences / problem_scores / saved_tests / user_directories
    - default deny: `match /{document=**} { allow read,write: if false; }`
    - helpers: `isSignedIn`, `isSelf`, `isAdmin` (users/{uid}.role + active 동시 확인), `ownsResource`, `ownsIncoming`
  - `firebase.json` + `.firebaserc` (project=`naegiwangbank`) 추가
  - `deploy_firestore_rules.py` — Firebase CLI 없이 서비스 계정 키로 Firebase Rules REST API 직배포
    - 1) `POST /v1/projects/{pid}/rulesets` → ruleset 생성
    - 2) `PATCH /v1/projects/{pid}/releases/cloud.firestore` (body=UpdateReleaseRequest, updateMask=`rulesetName`)
    - 모드: `--dry-run`, `--status`, default(=배포)
  - **기존 룰 백업**: `firestore.rules.backup_20260427` (2026-02-12 만료된 기본 테스트모드 룰 → 사실상 모든 client 거부 상태였음)
  - **배포 결과**: ruleset `2f68116a-2441-4c9a-87c7-fff8785af948` 가 cloud.firestore 라이브
  - **검증** (`verify_firestore_rules.py`, 8/8 PASS):
    1. Admin SDK problems read PASS (룰 우회 정상)
    2. 무인증 problems list → 403 (default deny 작동)
    3. admin signIn → idToken OK
    4. admin problems list → 200 / write → 200 / cleanup delete → 200
    5. admin 자기 users 문서 read → 200
    6. unknown_collection_xyz → 403 (default deny 작동)
- 2026-04-27: **Phase 2 종료** — 모든 작업 항목 (2-A ~ 2-H) 완료. 이중 잠금 (앞단=메뉴 숨김 + 뒷단=Firestore Rules) 양쪽 다 살아 있음.

### 핵심 결정/발견 사항
- **Web API Key 발견 위치**: `frontend/src/firebase.js` (이미 공개됨, 비밀 아님)
- **`firebase_admin._apps` 확인**: 중복 초기화 방지
- **이메일 도메인 변환**: `auth._to_email()` / `auth._from_email()` 으로 대칭 처리
- **로그인 화면이 뜨기 전 root.withdraw()**: 로그인 다이얼로그만 보이고 메인 GUI 는 로그인 후 새로 생성

---

## Phase 3. 개인 데이터 이주 (2026-04-27 완료)

### 시작 전 상태 (SQLite)
| 테이블 | 행수 | user_id 분포 |
|---|---|---|
| saved_tests | 162 | default(160) + user1(2) |
| problem_scores | 2,131 | (user_id 컬럼 없음 → saved_tests JOIN 으로 derive) |
| problem_preferences | 16 | user1(15) + 8YrxTdZYYcYts5bm4pkOdog5Bn92(1) ← 어제 로그인 통합 후 GUI 가 새 admin uid 로 1건 기록한 것 |
| user_directories | 30 | default(30) |
| **합계** | **2,339** | |

### 작업 순서
1. 스크립트 작성: `migrate_user_data_to_firestore.py`
   - 모드: `--check`, `--cleanup [--yes]`, `--sample N`, `--full`, `--verify`, `--dry-run`
   - User ID 매핑: `default`, `user1`, 기존 admin uid → 모두 첫 admin uid (`8YrxTdZYYcYts5bm4pkOdog5Bn92`) 로 통합
   - Doc ID 정책 (idempotent):
     - `saved_tests/{sqlite_id}` (정수 → 문자열)
     - `user_directories/{sqlite_id}`
     - `problem_scores/{sqlite_id}`
     - `problem_preferences/{uid}_{base64(problem_id)}` ← 한글/특수문자 안전
   - JSON 필드(`problem_ids`, `metadata`)는 Firestore 네이티브 array/map 으로 변환
   - DATETIME 텍스트는 Python datetime → Firestore Timestamp
   - `problem_scores` 는 SQLite 에 user_id 가 없어서 `saved_tests.test_id → user_id` 사전 빌드 후 derive
2. `--check` 로 baseline 확인
3. `--sample 3` 으로 스모크 테스트 (12건 / 0.8s 통과, 샘플 doc 확인)
4. `--cleanup --yes` 로 admin uid 도큐먼트만 깔끔히 비움 (다른 사용자 데이터 보존 설계)
5. `--full` 실행 → **2,339건 / 5.8초** 이주 완료
6. `--verify` → 4 컬렉션 전부 행수 일치 + saved_tests 샘플 4 필드 일치

### 검증 결과
```
saved_tests:        SQLite 162 / Firestore 162  OK
user_directories:   SQLite 30 / Firestore 30   OK
problem_preferences:SQLite 16 / Firestore 16   OK
problem_scores:     SQLite 2131 / Firestore 2131 OK
샘플(saved_tests/29): title / unit_summary / directory_id / problem_ids(100개) 전부 일치
```

### 산출물
- `migrate_user_data_to_firestore.py` — 재사용 가능한 개인데이터 이주 스크립트 (idempotent)

### 변경/유지 항목
- **변경**: Firestore 에 4개 신규 컬렉션 (saved_tests / problem_scores / problem_preferences / user_directories) 데이터 채워짐
- **유지**: SQLite 그대로 (Phase 4 GUI 데이터 소스 전환 전까지 GUI 는 SQLite 사용)
- **메모**: 매 도큐먼트에 `legacy_user_id`, `legacy_id` 필드 보존 → 추후 추적/롤백 용도

### 발견 이슈 / 메모
- problem_preferences 16건 중 1건이 어제 로그인 통합 후 GUI 가 admin uid 로 직접 SQLite 에 기록한 결과 → LEGACY_USER_IDS 에 admin uid 도 포함시켜 재이주 가능하게 처리
- `firestore_v1` UserWarning (positional `where`) 출력되지만 동작 정상 (코드 가독성 위해 그대로 둠)

---

## Phase 4. GUI 데이터 소스 전환 (2026-04-27 진행중)

### 사전 분석 결과 (Explore 에이전트, 2026-04-27)
- `db_engine.py` 1,548줄에 public 메서드 **46개** 식별
- 카테고리: problems READ (15) / personal READ (17) / personal WRITE (7) / problems WRITE (5) / 기타 (2)
- 복잡 쿼리 ★★★: `search_exams_grouped` (GROUP BY + HAVING + LIKE), `fetch_random_problems` (RANDOM + 누적 제외), `query_counts` (동적 5+ 컬럼)
- 트랜잭션 의존: `delete_folder` (재귀), `save_scores` (delete + multi-insert)
- 호출자 상위: get_folders(8), get_tests(6), create_folder(5), update_problem_meta/set_exclusion/query_counts/get_test_detail/get_problems_by_files(4)

### 핵심 결정 (Phase 4 골격)
1. **인터페이스 불변**: 46개 public 메서드 시그니처 100% 유지
2. **이중 엔진**: `LocalDBEngine`(SQLite) 보존 + `FirestoreEngine` 신설 + `data_engine.py` 팩토리
3. **토글**: 환경변수/설정 `DATA_ENGINE=sqlite|firestore` 로 즉시 전환
4. **캐시**: problems(29K) 만 로컬 SQLite 미러 (`%APPDATA%\naegiwangbank\problems_cache.sqlite`), 개인 데이터는 Firestore 직접
5. **증분 sync**: `updated_at` watermark — 4-A 시작 시 전체 백필

### 4단계 분할 (설계도 §5 Phase 4 세부분할 표 참조)
- 4-A: 캐시 인프라 + problems READ (메서드 15개) ★★★
- 4-B: 개인 데이터 READ (메서드 17개) ★★☆
- 4-C: 개인 데이터 WRITE (메서드 7개) ★★☆
- 4-D: problems WRITE + 캐시 무효화 (메서드 5개) ★★★

### 작업 계획 (TODO)
- [x] 4-A. 캐시 인프라 + problems READ ✅ 2026-04-27
  - [x] `backfill_updated_at.py` — problems 29,446건 갱신 + 5건 스킵 (총 29,451건 OK)
  - [x] `backend/data_cache.py` — ProblemsCache (full=28.8s / incremental=0.7~1.2s)
  - [x] `backend/data_engine.py` — `make_engine()` 팩토리 + `DATA_ENGINE` 환경변수 토글
  - [x] `backend/firestore_engine.py` — problems READ는 cache_engine 위임, 그 외는 local_engine 폴백
  - [x] `tests/test_engine_parity.py` — 20케이스 PASS / 0 FAIL
  - [x] main_gui.py — `make_engine(_local_db_path)` 진입점 교체
- [x] 4-B. 개인 데이터 READ (Firestore 직접) ✅ 2026-04-28
  - [x] FirestoreEngine 17개 메서드 구현 (folders / tests / scores / preferences)
  - [x] 캐시 활용: `get_test_detail` 의 problem JOIN 만 캐시 SQLite 사용, 그 외 전부 Firestore 직접
  - [x] `tests/test_engine_parity_4b.py` — SQLite "default+user1" vs Firestore admin uid 합산 비교 **29/29 PASS**
    - [x] wiring 갭은 4-B+ 에서 정공법으로 해결 (아래 4-B+ 항목 참조)
- [x] 4-B+. main_gui.py uid 와이어링 + SQLite legacy user_id 일괄 갱신 (A안 정공법) ✅ 2026-04-28
- [x] 4-C. 개인 데이터 WRITE ✅ 2026-04-28
  - [x] FirestoreEngine 7개 WRITE 메서드 monkey-patch (create_folder / rename_folder / delete_folder / save_test / delete_test / set_preference / save_scores)
  - [x] `_next_int_id()` 트랜잭션 채번 (Phase 3 마이그레이션과 호환되는 int 시퀀스 doc.id 유지)
  - [x] `tests/test_4c_write_smoke.py` — 양 모드 **40/40 PASS**
  - [x] LocalDBEngine.delete_test 도 problem_scores 동시 정리하도록 수정 (양 모드 동작 일치)
- [x] 4-D. problems WRITE + 캐시 무효화 ✅ 2026-04-28
  - [x] FirestoreEngine 5개 WRITE 메서드 monkey-patch (update_problem_meta / set_exclusion_status / delete_problems / delete_exam / delete_exams_by_filenames)
  - [x] write-through 캐시 갱신: cache_engine 위임 + `_bump_cache_updated_at()` 으로 watermark 동기화
  - [x] Firestore batch 한도(500) 분할, AUDIT 로그(`exclusion_audit.log`) 동일 형식 유지
  - [x] `tests/test_4d_write_smoke.py` — 양 모드 **27/27 PASS** (서버+캐시 일관성 동시 검증)

### 진행 기록
- 2026-04-27: 사전 분석 완료, Phase 4 4분할 + 캐시 아키텍처 확정. 4-A 착수.
- 2026-04-27: **4-A 완료**. 캐시 동기화 인프라 + 토글 + 패리티 검증 + GUI 진입점 교체까지.
- 2026-04-28: **4-B 완료**. FirestoreEngine 17개 메서드 + parity test 29/29 PASS.
  - 와이어링 갭 (main_gui.py 22개 호출 user_id 미전달) 발견 → 4-B+ 로 분리.
- 2026-04-28: **4-B+ 완료** (A안 정공법). SQLite legacy user_id 일괄 갱신 (207건) + main_gui.py 28곳 `self.current_user_id` 주입 + Phase 2-H mis-place 잔재 (`_build_users_tab` 6개 메서드가 `ExclusionSettingDialog` 안에 있던 것) 정리 + 양 모드 헤드리스 admin GUI 부팅 PASS + smoke 20/20 PASS.
- 2026-04-28: **4-C 완료**. FirestoreEngine 에 7개 WRITE 메서드 monkey-patch + int 시퀀스 채번 트랜잭션. test_4c_write_smoke 40/40 PASS (양 모드). LocalDBEngine.delete_test 도 problem_scores 동시 정리 추가 (잠재 데드 데이터 누적 버그 함께 정리, 양 모드 동작 일치). 양 모드 admin GUI 부팅 회귀 PASS.
- 2026-04-28: **4-D 완료**. FirestoreEngine 에 5개 problems WRITE 메서드 monkey-patch + write-through 캐시 갱신 (cache_engine 위임 + `_bump_cache_updated_at` watermark 동기화). test_4d_write_smoke 27/27 PASS (서버+캐시 양쪽 일관성 동시 검증). 4-C/4-B+ 회귀 PASS. **Phase 4 (GUI 데이터 소스 전환) 전체 완료.**

### 4-A 완료 요약
- **신규 파일** (5개):
  - `backfill_updated_at.py` (스크립트)
  - `backend/data_cache.py` — 로컬 SQLite 미러 매니저
  - `backend/data_engine.py` — 팩토리 + 환경변수 토글
  - `backend/firestore_engine.py` — 하이브리드 엔진 (캐시 + 폴백)
  - `tests/test_engine_parity.py` — 20케이스 자동화
- **수정 파일**: `backend/main_gui.py` 2곳 (import 1줄 + 엔진 생성 3줄)
- **캐시 위치**: `%APPDATA%\naegiwangbank\problems_cache.sqlite` (29,451건)
- **토글**: `set DATA_ENGINE=firestore && python backend/main_gui.py` → 캐시+Firestore. 기본은 sqlite.
- **부팅 비용**: incremental ~1.2s (변경 없을 때), full ~28.8s (캐시 비웠을 때)
- **검증**: parity test 20/20 PASS, smoke test 양 모드 모두 정상 (count=29,451 일치).
- **남은 라우팅 이슈 없음** — problems READ 15개 + personal 데이터 fallback 구조로 모든 호출 경로 커버.

### 4-A 단계의 한계 (의도적, 다음 단계로 분리)
- 개인 데이터 (saved_tests, problem_scores, problem_preferences, user_directories) 는 여전히 SQLite 폴백 → Phase 4-B/4-C에서 처리.
- problems WRITE (delete_problems, set_exclusion_status, update_problem_meta 등) 도 SQLite 폴백 → Phase 4-D에서 캐시 무효화와 함께.
- 캐시 파일에 빈 user 테이블이 생기는 부수효과 (LocalDBEngine `__init__` 부수효과). 동작에 무해, 4-D에서 정리 검토.

### 4-B 완료 요약 (2026-04-28)
- **신규/확장 파일**:
  - `backend/firestore_engine.py` — 17개 개인 데이터 READ 메서드 추가 (130~468줄)
  - `tests/test_engine_parity_4b.py` — admin uid 기준 29 케이스 자동화
- **검증 전략**:
  - SQLite 행은 user_id="default"/"user1" 로 보존 (Phase 3 가 Firestore 만 admin uid 로 매핑)
  - 비교: `sql.get_X(LEGACY_UID) (+ user1)` vs `eng.get_X(ADMIN_UID)` — 카운트/핵심필드 일치 확인
  - **결과: 29/29 PASS** (folders 30, tests 162, scores 1790, preferences 5 sample 등)
- **캐시 활용도**:
  - `get_test_detail` 만 캐시 SQLite 에 problems JOIN (구 LocalDBEngine SQL 그대로 재사용)
  - 그 외 16개는 모두 Firestore 직접 호출 (개인데이터 총량 ~2,300건이라 캐시 불필요)
- **사소한 깡통**: `firestore_v1` UserWarning (positional `where`) 다수 출력. 동작 정상.

### 4-B+ 완료 요약 (2026-04-28, A안 정공법)
- **목표**: 4-B 와이어링 갭 (main_gui.py 가 `user_id` 미전달 → 기본값 `"default"` → Firestore 모드 빈 화면) 완전 해소.

#### 1) SQLite legacy user_id → admin uid 일괄 갱신
- 신규 스크립트: `migrate_sqlite_legacy_user_ids.py` (`--check` / `--apply`, idempotent)
- 백업: `problem_bank.db.bak_uid_remap_20260428_115708` (19.5 MB)
- 갱신 대상 (legacy `'default'`, `'user1'` → `8YrxTdZYYcYts5bm4pkOdog5Bn92`):
  - `saved_tests`: 162행
  - `user_directories`: 30행
  - `problem_preferences`: 15행
  - **합계 207행** / 0.05초
- `problem_scores` 는 user_id 컬럼이 없어 (saved_tests JOIN derive) 변경 대상 아님.

#### 2) main_gui.py — `self.current_user_id` 주입 (28곳)
- `get_folders()`, `get_tests(None)`, `get_tests(f_id)`, `get_test_detail(...)`, `get_all_tests_sorted()`, `get_protected_folder_id("오답", ...)`, `create_folder(...)` (+`parent_id` 변형), `save_test(...)` 등 personal 데이터 호출 사이트 전체.
- DB 엔진의 17개 personal 메서드 시그니처는 그대로 유지 (`user_id` 인자만 활성화).

#### 3) Phase 2-H 잔재 (mis-place) 정리 — 부수 발견
- `_build_users_tab` 외 5개 `_users_*` 메서드 (총 287줄) 가 `ExclusionSettingDialog` 안에 잘못 들어가 있어 admin GUI 부팅이 `AttributeError` 로 실패하던 것 발견.
- 백업: `backend/main_gui.py.bak_relocate_20260428_121609`
- 6개 메서드를 `ModernProblemScannerGUI` 끝(=`class ScoringDialog:` 앞)으로 이동.
- 결과: 클래스 구조 정상화, syntax OK.

#### 4) 검증
| 검증 | 결과 |
|---|---|
| `tests/test_engine_parity_4b.py` (29 케이스) | **29/29 PASS** |
| `tests/test_4b_plus_smoke.py` (admin uid 직접 호출, 양 모드) | **20/20 PASS** (SQLite 10 + Firestore 10) |
| 헤드리스 admin GUI 부팅 — SQLite 모드 | **OK** (6개 탭 + `_build_users_tab()` 호출 성공) |
| 헤드리스 admin GUI 부팅 — Firestore 모드 | **OK** (캐시 incremental 0.6s + users tab 정상) |
| 양 모드 카운트 일치 | folders 30, tests 162, problem_ids by rate 1790 |

#### 5) 산출물
- `migrate_sqlite_legacy_user_ids.py` — 재사용 가능 idempotent 스크립트
- `tests/test_engine_parity_4b.py` — admin uid 기준 패리티 자동화
- `tests/test_4b_plus_smoke.py` — 양 모드 admin uid 스모크 자동화
- 백업 2개: `problem_bank.db.bak_uid_remap_20260428_115708`, `backend/main_gui.py.bak_relocate_20260428_121609`

#### 6) 변경/유지 항목
- **변경**: SQLite 의 user_id 분포 = 단일 admin uid (207행) / main_gui.py 28곳 / `_build_users_tab` 위치
- **유지**: db_engine.py 시그니처 / FirestoreEngine 구현 / 캐시 구조 / Phase 4-C·4-D 작업 미시작

### 4-C 완료 요약 (2026-04-28)
- **목표**: 개인 데이터 7개 WRITE 메서드를 SQLite 폴백이 아닌 Firestore 직접 호출로 전환.

#### 1) FirestoreEngine WRITE 구현 (`backend/firestore_engine.py:478~`)
- `create_folder(name, user_id, parent_id=None) -> {success, id}` — 새 user_directories 도큐먼트 + SERVER_TIMESTAMP
- `rename_folder(folder_id, new_name) -> bool`
- `delete_folder(folder_id) -> {success, message}` — 보호 검사 + 비어있음 검사 + 하위폴더 재귀 batch 삭제
- `save_test(test_data, user_id) -> {success, id}` — insert/update 분기, 1000건/30개/50개 한도 재현
- `delete_test(test_id) -> bool` — saved_tests + 연관 problem_scores 동시 batch 삭제
- `set_preference(user_id, problem_id, preference) -> bool` — None 이면 삭제, `pref_doc_id(uid,pid)` 결정적 doc.id
- `save_scores(test_id, scores) -> bool` — 기존 점수 일괄 삭제 후 신규 insert (batch 400 한도 분할)

#### 2) int 시퀀스 채번 (`_next_int_id`)
- Phase 3 마이그레이션에서 `saved_tests/{int}`, `user_directories/{int}` 형식을 유지했기 때문에
  새 도큐먼트도 동일한 형식이어야 호환됨.
- `_counters/{collection}.next_id` 트랜잭션으로 안전 채번.
- 부트스트랩 시점엔 `_scan_max_legacy_id()` 로 최댓값+1 부터 시작.

#### 3) LocalDBEngine 양 모드 동작 일치
- `delete_test` 가 saved_tests 만 지우고 problem_scores 는 남기던 잠재 버그를 함께 정리
  (Firestore 모드가 항상 cascade delete 하므로 SQLite 도 동일하게 맞춤).

#### 4) 검증
| 검증 | 결과 |
|---|---|
| `tests/test_4c_write_smoke.py` (양 모드 각 20 케이스) | **40/40 PASS** |
| `tests/test_4b_plus_smoke.py` (회귀) | **20/20 PASS** |
| 헤드리스 admin GUI 부팅 — SQLite 모드 | **OK** (7 탭) |
| 헤드리스 admin GUI 부팅 — Firestore 모드 | **OK** (7 탭, 캐시 incremental 0.6s) |

#### 5) 산출물
- `backend/firestore_engine.py` — `_install_write_methods()` 블록 (478~726줄)
- `tests/test_4c_write_smoke.py` — 양 모드 자동화 (8 시나리오 × 2 모드)

#### 6) 변경/유지 항목
- **변경**: FirestoreEngine WRITE 7개 + LocalDBEngine.delete_test (점수 cascade)
- **유지**: 메서드 시그니처 / GUI / 캐시 구조 / `db_engine.py` 의 그 외 메서드

#### 7) 발견 메모
- 기존 4-C 시도가 만든 dummy 점수 잔재 2건 (`dummy_pid_1`, `dummy_pid_2` / test_id=220) 정리 완료.
- `firestore_v1` UserWarning (positional `where`) 다수 출력 — 동작 정상, Phase 4-D 또는 별도 단계에서 keyword `filter=` 로 일괄 변경 검토.

### 4-D 완료 요약 (2026-04-28)
- **목표**: problems 컬렉션 WRITE 5개 메서드를 Firestore 직접 호출 + 로컬 캐시 즉시 갱신.

#### 1) FirestoreEngine WRITE 구현 (`backend/firestore_engine.py:734~`)
- `update_problem_meta(problem_id, field, value, unit_name=None)` — `difficulty`/`unit_code` 분기 + `*_locked=1` + `updated_at=SERVER_TIMESTAMP`. 캐시는 `cache_engine` 위임.
- `set_exclusion_status(problem_ids, excluded) -> int` — batch update (500 분할) + AUDIT 로그(`exclusion_audit.log`) `via=Firestore` 태그 추가.
- `delete_problems(problem_ids) -> int` — batch delete (500 분할). incremental_sync 가 deleted 도큐먼트를 추적할 수 없으므로 **캐시는 명시적 `cache.delete_many()`**.
- `delete_exam(meta) -> int` — meta(school/year/...) 일치하는 후보 id 를 캐시에서 조회 → `delete_problems()` 위임.
- `delete_exams_by_filenames(file_names) -> int` — `file_name IN (...)` 후보 id 를 캐시에서 조회 → `delete_problems()` 위임.

#### 2) write-through 캐시 갱신
- `cache_engine.<method>()` 로 LocalDBEngine 의 SQL 그대로 캐시 SQLite 에 적용 (스키마 일치).
- `_bump_cache_updated_at(ids)` 로 캐시 측 `updated_at(epoch float)` 도 같이 현재 시각으로 갱신.
  → 다음 부팅의 `incremental_sync()` 가 같은 변경을 다시 받지 않음 (watermark 정상 작동).

#### 3) 검증
| 검증 | 결과 |
|---|---|
| `tests/test_4d_write_smoke.py` (양 모드, 7 메서드 시나리오) | **27/27 PASS** |
| `tests/test_4c_write_smoke.py` (회귀) | **40/40 PASS** |
| `tests/test_4b_plus_smoke.py` (회귀) | **20/20 PASS** |
| 양 모드 admin GUI 부팅 + 5개 메서드 노출 확인 | **OK** (7 탭) |

#### 4) 발견 메모
- Firestore 모드에서 `set_exclusion_status` AUDIT 로그가 2번 찍힘 (FirestoreEngine 자체 + cache_engine 의 LocalDBEngine 코드). 동작 무해, 노이즈만 발생. 필요 시 별도 단계에서 분리 검토.
- `firestore_v1` positional `where` UserWarning 여전. 4-D 의 캐시 검색은 캐시 SQLite 에서 처리하므로 신규 노출은 거의 없음.

#### 5) 산출물
- `backend/firestore_engine.py` — `_install_problems_write_methods()` 블록 (728~)
- `tests/test_4d_write_smoke.py` — 양 모드 자동화 (서버+캐시 동시 검증)

#### 6) 변경/유지 항목
- **변경**: FirestoreEngine 5개 problems WRITE 메서드 추가, write-through 캐시 패턴 적용
- **유지**: LocalDBEngine 의 5개 메서드 / 메서드 시그니처 / 캐시 스키마 / GUI 호출부

---

## Phase 5. 인덱서 직접 쓰기 (2026-04-28 완료)

### 목표
- 인덱싱 시 DB 쓰기 경로를 엔진 라우팅으로 일원화. SQLite 모드는 `problem_bank.db`, Firestore 모드는 Firestore + write-through 캐시.
- 기존 `hwp_metadata_parser_v2._post_process_file` 의 sqlite3 직접 INSERT + naive `_sync_to_firebase` 두 경로 통합.
- admin only (Firestore Security Rules 가 보장 + main_gui.py 의 "문제 등록" 탭 자체가 `current_user_role == "admin"` 일 때만 빌드됨 — line 129/134/139).

### 작업 순서
1. **사전 분석**: 인덱서들 (`backend/indexers/*.py`) 은 metadata helper 만 제공, SQL 미사용. 실제 DB 쓰기는 `backend/hwp_metadata_parser_v2.py:_post_process_file` 의 `INSERT OR REPLACE` 와 `_sync_to_firebase` 에서 일어남. db_engine 의 CLI helper `sync_json_dir` 은 production 경로가 아님.
2. **LocalDBEngine.ingest_indexed_problems(items)** 추가 (`backend/db_engine.py`) — INSERT OR REPLACE 배치, problem_id→id 매핑, MOCK_EXAM exam_number 자동 보정, 잠금/제외 상태 보존.
3. **FirestoreEngine.ingest_indexed_problems(items)** monkey-patch 추가 (`backend/firestore_engine.py:_install_indexer_write_methods()`) — Firestore batch upsert (`_FS_BATCH_LIMIT=400` 분할) + write-through 캐시 (`cache.upsert_one()`) + 기존 doc 잠금/제외 보존.
4. **HWPMetadataParserV2** 에 `engine` 파라미터 추가 (default `None`) → `_post_process_file` 가 engine 우선 호출, 미주입 시 legacy 직접 sqlite3 + naive Firebase 폴백 (CLI/스크립트 호환).
5. **main_gui.py** 에서 `make_engine()` 후 parser 생성하도록 순서 조정 + `engine=self.db_engine` 전달.
6. **smoke 테스트** `tests/test_5_indexer_smoke.py` 작성 — 양 모드, 더미 인덱싱 결과 inject → 캐시/Firestore 검증 → 정리.

### 잠금 보존 규칙
- 기존 doc 의 `difficulty_locked == 1` → 새 인덱싱의 `difficulty` 무시, 기존값 유지.
- 기존 doc 의 `unit_code_locked == 1` → 새 인덱싱의 `unit_code` / `middle_unit` / `mapped_unit_code` 무시, 기존값 유지.
- `is_excluded` 는 항상 기존값 유지 (사용자 출제 제외 상태 보호).
- 이는 sync_json_dir 의 기존 보존 로직과 동일. 기존 `_post_process_file` 의 단순 INSERT OR REPLACE 가 lock 을 wipe 했던 잠재 회귀 버그도 자동 수정.

### MOCK_EXAM exam_number
- `pn if pn<=22 else (pn-22-1) % 8 + 23` — 공통 1~22 그대로, 선택과목(확통/미적/기하) 23~30 각 8문제 → 23으로 시작. 기존 `_post_process_file` 로직 동일하게 이전.

### 검증
| 검증 | 결과 |
|---|---|
| `tests/test_5_indexer_smoke.py` (양 모드, 신규 ingest + lock 보존 + exam_number) | **29/29 PASS** |
| `tests/test_engine_parity.py` (4-A 회귀) | **20/20 PASS** |
| `tests/test_4b_plus_smoke.py` (4-B+ 회귀) | **20/20 PASS** |
| `tests/test_4c_write_smoke.py` (4-C 회귀) | **40/40 PASS** |
| `tests/test_4d_write_smoke.py` (4-D 회귀) | **27/27 PASS** |

### 산출물
- `backend/db_engine.py` — `LocalDBEngine.ingest_indexed_problems()` (171~270줄)
- `backend/firestore_engine.py` — `_install_indexer_write_methods()` 블록 (916줄~끝)
- `backend/hwp_metadata_parser_v2.py` — `__init__(engine=None)` + `_post_process_file` 엔진 라우팅 + legacy 폴백
- `backend/main_gui.py` — engine 먼저 생성 → parser 에 `engine=self.db_engine` 전달
- `tests/test_5_indexer_smoke.py` — 양 모드 자동화 (12 시나리오 × 2 모드)

### 변경/유지 항목
- **변경**: parser → engine 라우팅, lock/exclude 보존 규칙 추가, _post_process_file 의 직접 SQL 제거 (legacy 폴백으로 보존).
- **유지**: parser scan_folder/scan_file 시그니처 / 인덱서 인터페이스 / problems 컬렉션 스키마.

### 발견 메모
- `tests/test_engine_parity_4b.py` 9건 실패는 Phase 5 와 무관한 사전 조건. `LocalDBEngine.init_user_tables` 가 매 인스턴스 생성 시 `default` 사용자에 '오답' 폴더(id=31)를 자동 부트스트랩 → 4-B+ legacy uid 통합 후 Firestore admin uid(30개) 와 어긋남. 별도 단계에서 부트스트랩 로직 자체를 admin uid 기준으로 옮기는 게 정공법.
- 인덱서가 직접 Firestore 쓰는 게 아니라 parser 가 엔진을 통해 일원화하는 형태이므로, "인덱서 직접 쓰기" 라는 단계명은 의미상 "parser 직접 엔진 호출" 에 더 가까움. 향후 단계명 변경 시 참고.

---

## ★ 데이터 최적화 1차 (2026-04-28 완료)

### 동기
2~3명 규모 사용자 가정. Firestore 비용 폭탄 위험을 사전 차단. 사전 견적은 1순위가 가장 중요했으나 사용자 요청에 따라 1·2 모두 적용, 3 은 측정 후 불필요로 판정.

### 적용
**1순위. 인덱서 1-by-1 GET 제거** (`backend/firestore_engine.py:_install_indexer_write_methods`)
- 기존: `ingest_indexed_problems` 가 잠금 보존 위해 item 별 `fs.collection("problems").document(pid).get()` → 인덱싱 1회 = 26K Firestore reads.
- 변경: 캐시 SQLite 에 `IN (?, ?, ...)` 일괄 조회 (500 청크). Firestore reads = **0**.
- 정합성: 캐시는 `incremental_sync` 로 Firestore 와 동기화되므로 lock 의 진실의 원천. admin 단일/소수 사용 시나리오 가정.

**2순위. cache seed export/import** (`backend/data_cache.py`)
- 신규: `default_seed_path()` = `backend/seed/problems_cache.sqlite`.
- 신규: `ProblemsCache.export_seed(target_path=None)` — admin 이 배포 직전 1회 실행. `sqlite3.Connection.backup()` 으로 안전 dump.
- 신규: `ProblemsCache._try_import_seed()` — 부팅 시 캐시 비어있고 시드 동봉되어 있으면 파일 복사 + schema 보강.
- `ensure_synced()` 흐름 변경: empty cache → `_try_import_seed()` 성공 시 `incremental_sync()` 만 (시드 시점 이후 갱신분만), 실패 시 기존대로 `full_sync()`.
- 첫 부팅 reads: **29,451 → 0** (시드 동봉 시).

**3순위. 필드 다이어트** — 측정 결과 doc 평균 200B 미만 (search_text 평균 56자, pos_start/pos_end 합쳐 22자). 다이어트할 큰 필드 없음. **스킵.**

### 산출물
- `backend/data_cache.py` — `default_seed_path()`, `ProblemsCache.export_seed()`, `ProblemsCache._try_import_seed()`, `ensure_synced()` 시드 분기.
- `backend/firestore_engine.py:_install_indexer_write_methods` — 잠금 조회 캐시 SQL 일괄 변경.
- `backend/seed/problems_cache.sqlite` — 29,451건 / 22 MB (admin 빌드 시 생성, 배포 동봉 권장).

### 검증
| 검증 | 결과 |
|---|---|
| `tests/test_5_indexer_smoke.py` (lock 보존 분기) | **29/29 PASS** |
| `tests/test_engine_parity.py` (4-A) | **20/20 PASS** |
| `tests/test_4b_plus_smoke.py` (4-B+) | **20/20 PASS** |
| `tests/test_4c_write_smoke.py` (4-C) | **40/40 PASS** |
| `tests/test_4d_write_smoke.py` (4-D) | **27/27 PASS** |
| seed export/import 라운드트립 | **OK** (29,451건 동일 복원) |

### 비용 효과 (2~3명 가정, 견적)
- 인덱싱 1회 = 26K reads → **0 reads** (writes만, $0.05).
- 첫 부팅 = 29K reads → 시드 동봉 시 **0 reads** (incremental 만).
- 일상 incremental_sync = 변경분만, 거의 0.
- 월 비용 추정: $1 미만 (인덱싱 빈도 좌우).

### 운영 메모
- 시드 갱신: admin 이 인덱싱/문제 정리 후 `ProblemsCache().export_seed()` 호출 → `backend/seed/problems_cache.sqlite` 갱신 → 빌드/배포 시 동봉.
- 시드는 빌드 산출물 성격. 자주 갱신할 필요 없음 — 배포 시점 한 번이면 신규 사용자는 그 시점 + incremental 로 최신화됨.

---

## 다음 세션이 이어 받을 때 (recovery 가이드)

새 세션에서 이 작업을 이어가려면 순서:

1. `로그인시스템설계도.md` 통독 (전체 그림)
2. 본 파일 (`로그인시스템_작업현황.md`) 의 "진척 요약" 섹션 확인 → 어느 Phase 인지 파악
3. 현재 진행중인 Phase 의 "작업 계획 (TODO)" 에서 체크 안 된 첫 항목부터 시작
4. 작업 중 발견한 이슈/결정은 본 파일의 "진행 기록" 에 즉시 추가
5. Phase 끝나면 본 파일 + 설계도의 진척표 모두 갱신

### 자주 쓰는 경로
- 프로젝트 루트: `G:\문제은행\문제은행2`
- SQLite: `problem_bank.db`
- Firebase 키: `backend\naegiwangbank-firebase-adminsdk-fbsvc-5e5e798b33.json`
- 메인 GUI: `backend\main_gui.py`
- DB 엔진: `backend\db_engine.py`
- 마이그레이션: `migrate_problems_to_firestore.py`

### 자주 쓰는 명령
```bash
# Firestore 상태 확인
python migrate_problems_to_firestore.py --check

# GUI 재시작 (한국어 경로 우회)
python C:\Temp\run_gui.py
```
