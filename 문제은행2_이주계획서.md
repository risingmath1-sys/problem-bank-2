# 문제은행2 → 학원 서버 이주 계획서

작성일: 2026-05-26
작성자: 사용자 PC Claude (Sonnet)
독자: 메인 Claude (전체 컨텍스트 보유)

---

## 0. TL;DR

- **목표**: 사용자 PC에서 돌리던 SC(서버) + 로컬 GUI(인덱싱)를 **학원 미니PC 한 곳으로 통합 이주**
- **방식**: Git 기반 배포 (학원관리 프로그램과 동일 패턴)
- **외부 접속**: 학원 홈페이지 도메인 연결 + Cloudflare Tunnel
- **인덱싱**: 미니PC에 RDP 접속해서 진행
- **DB 정책 변경 (2026-05-26)**: Firestore 폐기 → SQLite-only (이미 적용 완료)

---

## 1. 프로젝트 개요

### 1-1. 무엇을 하는 프로그램?

수학 학원용 **문제은행 관리 + 시험지 자동 출제** 시스템.

- **인덱싱**: HWP 파일(한글) 안의 문제들을 자동 파싱 + 분류 + DB 저장
- **출제**: 단원/난이도/소스 기준으로 랜덤 선택 + HWP 시험지 자동 생성
- **시험지 관리**: 저장된 시험지 라이브러리, 채점, 오답 폴더 관리
- **사용자 관리**: admin/user 권한 분리

### 1-2. 운영 역할 분리 (2026-05-23 확정)

| 역할 | 위치 | 코드 영역 |
|----|----|----|
| **문제 출제 (SC)** | FastAPI 웹 서버 (브라우저 접속) | `server/` 위주 |
| **문제 등록 (인덱싱)** | 데스크탑 GUI + HWP COM | `backend/main_gui.py` + `backend/indexers/*` |

> **중요**: 인덱싱 관련 코드(`backend/indexers/*`, `naesin_n_unit_map.json`, `hwp_metadata_parser_v2.py`)는 데스크탑 위주로 손볼 것. 서버는 같은 `backend/` 모듈을 import 만 함.

---

## 2. 기술 스택

### 2-1. 백엔드 / 코어

| 영역 | 스택 | 비고 |
|----|----|----|
| **언어** | Python 3.13 | Windows 전용 (HWP COM 의존) |
| **웹 서버** | FastAPI 0.128 + uvicorn 0.40 | `server/main.py` |
| **로컬 GUI** | Tkinter | `backend/main_gui.py` (6,902줄) |
| **DB** | **SQLite (problem_bank.db)** ← 정본 | 약 20MB, 56,741 problems |
| **인증** | Firebase Auth + Firestore `users` 컬렉션 (legacy) | 로그인만 유지 |
| **HWP 자동화** | pywin32 311 + pyhwpx 1.6.8 | Windows + 한컴 오피스 필수 |
| **세션** | JWT (HS256) + 쿠키 | `server/auth_dep.py` |

### 2-2. 프론트엔드

| 영역 | 스택 |
|----|----|
| **템플릿** | Jinja2 3.1 |
| **상호작용** | HTMX 1.9.12 |
| **CSS** | Tailwind CSS (CDN) |

### 2-3. 외부 접속

| 영역 | 도구 | 비고 |
|----|----|----|
| **터널링** | Cloudflare Tunnel | 무료, 학원 내 + 외부 동시 가능 |
| **도메인** | 학원 도메인 서브경로/서브도메인 | 학원관리 프로그램과 동일 방식 |

---

## 3. 디렉토리 구조

```
G:\문제은행\문제은행2\
├── backend/                       # 도메인 로직 (서버 & GUI 공통)
│   ├── main_gui.py                # 로컬 GUI 메인 (6,902줄, 인덱싱 전용)
│   ├── db_engine.py               # LocalDBEngine (SQLite-only, 1,702줄) ⭐ 정본 엔진
│   ├── data_engine.py             # make_engine 팩토리 (DATA_ENGINE 토글)
│   ├── firestore_engine.py        # FirestoreEngine (dead code, 1,191줄) — 폐기됨, 보존
│   ├── data_cache.py              # ProblemsCache (dead code, 447줄) — 폐기됨, 보존
│   ├── hwp_generator.py           # HWP 출제 로직 (584줄)
│   ├── hwp_core.py                # HWP COM worker (subprocess, 1,626줄)
│   ├── hwp_metadata_parser_v2.py  # HWP 파싱/인덱싱
│   ├── indexers/                  # 소스별 인덱서
│   │   ├── naesin_ang_indexer.py
│   │   ├── naesin_n_indexer.py
│   │   ├── suneung_special_indexer.py
│   │   ├── suneung_wansung_indexer.py
│   │   └── mock_exam_indexer.py
│   ├── curriculum_config.json     # 2015→2022 단원코드 매핑 (67개)
│   ├── unit_hierarchy.json        # 단원 트리 (curriculum별)
│   ├── auth.py                    # Firebase Auth 헬퍼 (유지)
│   └── firebase_init.py           # Firebase Admin SDK 초기화 (유지)
│
├── server/                        # FastAPI 웹 서버
│   ├── main.py                    # FastAPI 진입점 + lifespan
│   ├── auth_dep.py                # SessionUser, require_user, require_admin
│   ├── routes/                    # 라우트들
│   │   ├── pages.py               # HTML 페이지 라우트
│   │   ├── api_auth.py            # 로그인/로그아웃
│   │   ├── api_exam.py            # 랜덤/원본/라이브러리 출제
│   │   ├── api_library.py         # 시험지 라이브러리
│   │   ├── api_manage.py          # 문제 관리 (admin)
│   │   ├── api_register.py        # 문제 등록 (admin, dead — 실제는 GUI)
│   │   ├── api_meta.py            # 메타데이터 (학교명 검색 등)
│   │   ├── api_users.py           # 사용자 관리 (admin)
│   │   └── api_admin.py           # cache sync 등 (legacy)
│   ├── workers/
│   │   └── job_queue.py           # HWP 출제 job queue (async)
│   ├── services/
│   │   ├── engine.py              # get_engine() 싱글톤
│   │   ├── source_files.py        # validate_problems
│   │   └── exam_session.py        # draft 세션 (단원 선택 → step1~3)
│   ├── templates/                 # Jinja2 templates (HTMX 사용)
│   │   ├── base.html              # 네비게이션 (admin 메뉴 분기)
│   │   ├── home.html              # 홈 (admin/user 메뉴 카드 분기)
│   │   └── partials/              # HTMX partial fragments
│   └── static/                    # CSS, 로고
│
├── problem_bank.db                # ⭐ SQLite 마스터 DB (Git ignore)
├── settings.json                  # source_hwp_dir 등 환경 설정
├── requirements.txt               # 의존성
├── 서버클라이언트시작.bat          # 서버 부팅 배치 (포트 8000 + 브라우저)
├── 진단_웹서버.bat                 # 진단용 배치
├── 포트정리.bat                   # 8000 포트 정리 배치
└── 문제은행2_이주계획서.md         # 이 문서
```

---

## 4. 데이터베이스 상태 (2026-05-26 동기화 완료)

### 4-1. SQLite 테이블

| 테이블 | 건수 | 비고 |
|----|----|----|
| `problems` | **56,741** | 문제 데이터 (정본) |
| `saved_tests` | 189 | 저장된 시험지 |
| `user_directories` | 29 | 시험지 폴더 (nested 지원) |
| `problem_preferences` | 20 | Good/Soso/Bad 선호도 |
| `problem_scores` | 2,055 | 채점 기록 (O/X) |

### 4-2. problems by source

| source | 건수 | 비고 |
|----|----|----|
| NAESIN_A | 18,047 | 내신기출A (내기왕) |
| NAESIN_N | 23,008 | 내신기출B (N사) |
| SUNEUNG_SPECIAL | 6,627 | 수능특강 |
| SUNEUNG_COMPLETE | 2,881 | 수능완성 |
| MOCK_EXAM | 6,178 | 모의고사 |

### 4-3. 의도적 빈값 (정상)

- **MOCK_EXAM unit_code 984건 빈값**: 단원분류 안 함 (2026-04-30 결정)
- **NAESIN_N 40건, NAESIN_A 36건, SUNEUNG_SPECIAL 30건 빈값**: 자투리, 의도적

---

## 5. 2026-05-26 핵심 정책 변경 (이미 적용 완료)

### 5-1. Firestore 폐기 → SQLite-only

**변경 전 (Firestore 정본)**:
```
[FirestoreEngine] ─→ Firestore (problems 컬렉션, 5.7만)
                  └→ ProblemsCache (SQLite 미러, %APPDATA%\naegiwangbank\)
                  └→ problem_bank.db (legacy SQLite, ~2.9만 — 격차 발생)
```

**변경 후 (SQLite 정본)**:
```
[LocalDBEngine] ─→ problem_bank.db (SQLite, 5.7만)  ⭐ 단일 마스터
```

### 5-2. 동기를 만든 사건들

1. 캐시 SQLite schema malformed 에러 (`%APPDATA%\naegiwangbank\problems_cache.sqlite`)
2. SC 서버 500 Internal Server Error 빈발
3. 사용자 결정: "캐시니 뭐니 이상해졌어, 무조건 회귀"
4. 10만 문제 도달 시 Firestore 비용 vs 로컬 SQLite 비교 → SQLite 압승

### 5-3. 적용된 코드 변경 (3 파일)

| 파일 | 변경 |
|----|----|
| `backend/data_engine.py` | `get_engine_mode()` default `"firestore"` → `"sqlite"` |
| `backend/db_engine.py` | `ensure_protected_folder_method` alias 추가 (LocalDBEngine 호환성) |
| `backend/main_gui.py` | 시작 부분에 `os.environ.setdefault("DATA_ENGINE", "sqlite")` |
| `server/main.py` | 시작 부분에 `os.environ.setdefault("DATA_ENGINE", "sqlite")` |

### 5-4. 1:1 마이그레이션 결과

Firestore의 모든 데이터를 SQLite로 1회 복사 + SQLite-only 데이터 정리 (학생 퇴원 등):
- `user_directories`: 30 → 29 (퇴원생 폴더 4개 삭제)
- `saved_tests`: 159 → 189 (Firestore에만 있던 39건 추가)
- `problem_preferences`: 16 → 20
- `problem_scores`: 2126 → 2055
- `problems`: 29,432 → 56,741 (어제 인덱싱한 약 2.7만건 포함)

### 5-5. Firestore 코드는 **dead code로 보존**

- `backend/firestore_engine.py` (1,191줄): 그대로 둠
- `backend/data_cache.py` (447줄): 그대로 둠
- 환경변수 `DATA_ENGINE=firestore` 명시하면 즉시 롤백 가능
- 미래에 백업용으로 재활용 가능 (단방향 push)

### 5-6. 유지된 Firestore 사용처

- **Firebase Auth (로그인)**: `backend/auth.py` — 사용자 인증, 비밀번호 관리
- **Firestore `users` 컬렉션**: role(admin/user), display_name 매핑

> 즉, **문제 데이터만 SQLite-only**, 인증은 Firebase 그대로.

### 5-7. DB 백업 파일들 (이주 시 정리 대상)

```
problem_bank.db.bak_premigrate_20260526_192716          (합집합 마이그레이션 전, 19.52MB)
problem_bank.db.bak_preproblems_20260526_193739         (problems 동기화 전)
problem_bank.db.bak_fs_authoritative_20260526_194337    (Firestore 정본 정책 전)
```

---

## 6. 권한 시스템 (이미 완성됨)

### 4중 안전망

| 레이어 | 위치 | 동작 |
|----|----|----|
| 상단 네비 | `server/templates/base.html` 라인 23-27 | `{% if user.role == 'admin' %}` |
| 홈 메뉴 카드 | `server/templates/home.html` 라인 47-68 | 같은 조건 |
| 페이지 라우트 | `server/routes/pages.py` | `if user.role != "admin": redirect "/"` |
| API 라우트 | `api_register.py`, `api_manage.py`, `api_users.py`, `api_admin.py` | `Depends(require_admin)` → 403 |

**일반 user 보이는 메뉴**: 랜덤 출제 / 원본 출제 / 시험지 관리 (3개)
**admin만 보이는 메뉴**: + 문제 등록 / 문제 관리 / 사용자 관리

---

## 7. 이주 계획 — 미니PC로

### 7-1. 미니PC 사양

- **모델**: HP ProDesk Mini 600 G4 리퍼브
- **가격**: 269,800원
- **결정일**: 2026-05-02
- **위치**: 학원 메인공유기에 모니터 없이 헤드리스 연결
- **기존 가동 중**: 학원관리 프로그램 (Git 기반)

### 7-2. 이주 단계

```
[1단계] 코드 정리 (사용자 PC)
  ├─ .gitignore 점검
  │   ├─ problem_bank.db          (Git ignore)
  │   ├─ *.bak_*                  (백업 파일 제외)
  │   ├─ settings.json            (환경별 다름)
  │   ├─ firebase 키 파일          (보안)
  │   └─ 사용자 캐시 파일들
  ├─ Firestore dead code 정리 여부 결정 (보존 권장)
  └─ Git push

[2단계] 미니PC 셋업
  ├─ Git clone
  ├─ Python 3.13 설치 (이미 학원관리에 있을 듯)
  ├─ pip install -r requirements.txt
  ├─ 한컴 오피스 설치 (HWP COM 필수)
  ├─ pyhwpx 레지스트리 등록 (자동, hwp_registry_manager.py)
  ├─ Firebase 키 파일 복사 (보안 경로)
  └─ problem_bank.db 1회 전송 (사용자 PC → 미니PC)

[3단계] 원본 HWP 파일 폴더 이전
  ├─ 사용자 PC의 원본 HWP 폴더를 미니PC로 복사
  ├─ settings.json 의 source_hwp_dir 갱신
  └─ 또는 네트워크 폴더 마운트

[4단계] RDP 셋업
  ├─ 미니PC 윈도우 원격 데스크톱 활성화
  ├─ 사용자 PC에서 접속 테스트
  └─ HWP COM이 RDP 세션에서 동작하는지 확인 (핵심!)

[5단계] SC 서버 자동 시작
  ├─ 윈도우 작업 스케줄러 또는 nssm으로 서비스화
  ├─ 부팅 시 자동 시작
  └─ 학원관리 프로그램과 동일 패턴 적용

[6단계] 도메인 연결
  ├─ Cloudflare Tunnel (학원관리와 동일)
  ├─ 학원 도메인 서브경로 또는 서브도메인
  └─ HTTPS 자동

[7단계] 검증
  ├─ 외부에서 로그인 테스트
  ├─ 인덱싱 1회 RDP 시연
  ├─ 출제 3종(랜덤/원본/라이브러리) 동작
  └─ admin/user 권한 분리 확인
```

### 7-3. 사용자 PC 정리 (이주 완료 후)

- 사용자 PC의 SC 서버 인스턴스 종료
- 사용자 PC의 `problem_bank.db`는 보관 (백업)
- 사용자 PC의 코드는 개발용으로만 (수정 → push → 미니PC pull)

---

## 8. 운영 흐름 (이주 후)

### 8-1. 코드 수정 흐름

```
[사용자 PC]                       [미니PC]
코드 수정
   ↓
git push                  ──→     git pull (수동 또는 webhook)
                                     ↓
                                  서버 재시작 (수동 또는 자동)
```

### 8-2. 인덱싱 흐름

```
[사용자 PC]   ──RDP──→   [미니PC]
                            ├─ main_gui.py 실행
                            ├─ HWP 파일 인덱싱 (한컴 자동화)
                            └─ problem_bank.db 직접 쓰기
                                  ↓
                            ├─ SC 서버가 같은 DB 읽음
                            └─ 즉시 반영 (재시작 불필요)
```

### 8-3. 일반 사용 흐름

```
[선생님 어디서나]   ──HTTPS──→   [학원 도메인]
   브라우저                          ↓
                            Cloudflare Tunnel
                                  ↓
                            [미니PC SC 서버]
                                  ├─ Firebase Auth (로그인)
                                  ├─ SQLite (problems, tests, …)
                                  └─ HWP COM 출제 → 파일 다운로드
```

---

## 9. 위험 요소 + 대응

| 위험 | 영향 | 대응 |
|----|----|----|
| **HWP COM이 RDP에서 동작 안 함** | 인덱싱 불가 | 사전 테스트 필수. 안 되면 사용자 PC에서 인덱싱 + DB 복사 (옵션 B 폴백) |
| **미니PC 사양 부족** | 인덱싱 느림 | HP ProDesk Mini 600 G4 충분 추정. 모니터 필요 시 → 가상 디스플레이 |
| **포트 8000 충돌** | 서버 안 뜸 | 학원관리가 다른 포트면 OK. 충돌 시 포트 변경 + Tunnel 설정 갱신 |
| **Firebase 키 노출** | 보안 사고 | `.gitignore`에 키 파일 추가 + 미니PC에 별도 복사 |
| **DB 손상** | 데이터 손실 | 매일 백업 자동화 필요 (Step 5, 미완) |
| **Cloudflare Tunnel 다운** | 외부 접속 불가 | 학원관리도 같은 경로면 단일 장애점 — 학원 내 SMB는 별도 |

---

## 10. 미완료 작업 (이주 후 진행)

### 10-1. 인덱싱 잔재 정리

- 수능특강 HWP 파일에 `@` 마커 삽입 (섹션 구분용)
- `_remove_trailing_blank_lines()` 안전 재구현 (현재 no-op)
- 누적 미분류 40건 단원 매핑 추가 (NAESIN_N)

### 10-2. 백업 자동화 (Step 5)

- 학원 미니PC → 사용자 PC 매일 자동 백업
- OneDrive/Dropbox 또는 SMB
- 윈도우 작업 스케줄러

### 10-3. Firestore dead code 청소 (선택)

- 6개월 운영 안정 확인 후 결정
- 안전망 보존 vs 코드 정리

---

## 11. 메인 Claude에게 — 컨텍스트 요약

이 프로젝트는 다음과 같은 흐름으로 진화했습니다:

```
2026-02~03: 로컬 GUI(tkinter) + SQLite 단일
2026-04:    Firestore 도입 (다중 PC 동기화 목적)
2026-04-28: FirestoreEngine + ProblemsCache 하이브리드 (DATA_ENGINE=firestore default)
2026-05-02: 시스템 대전환 결정 — 학원 미니PC + 웹 클라이언트 (맥 호환)
2026-05-13: FastAPI 서버 (server/) 구현, 배치 파일 정비
2026-05-23: 운영 역할 분리 — 출제=SC / 등록=로컬
2026-05-26: ⭐ Firestore 폐기 → SQLite-only 회귀 (이 문서)
            ⭐ 미니PC 이주 계획 수립
```

### 메인 Claude가 알아야 할 핵심 사실

1. **DB는 이제 `problem_bank.db` 하나**. `%APPDATA%\naegiwangbank\` 캐시는 dead.
2. **인증만 Firebase 유지**, 나머지 Firestore 코드는 dead code 보존.
3. **인덱싱과 SC는 같은 backend/ 모듈 공유**, but 다른 진입점.
4. **DATA_ENGINE 환경변수 토글 살아있음** — 만약 문제 발생 시 `firestore` 로 즉시 롤백.
5. **권한 시스템은 이미 완벽**, admin/user 분리 4중 안전망.
6. **이주 후엔 미니PC가 단일 마스터** — DB도 원본 HWP도 한 곳.

### 메인 Claude가 결정해줘야 할 것 (이주 진행 시)

- Q1. `.gitignore` 정확한 목록 — 학원관리 프로그램의 패턴 참고
- Q2. 학원관리 프로그램의 도메인/포트 어떻게 분리하나? (8000 충돌 회피)
- Q3. Firebase 키 파일 미니PC 복사 방법 (안전한 경로)
- Q4. 미니PC에 한컴 라이센스 설치 가능 여부 확인
- Q5. RDP HWP COM 호환성 사전 테스트 — 안 되면 옵션 B(사용자 PC 인덱싱) 폴백 계획

---

## 12. 참고 문서 (프로젝트 내)

- `system_architecture.md` — 시스템 핵심 원칙
- `서버클라이언트_설계도.md` § 0-B — 운영 역할 분리
- `HWP_COM_기술노트.md` — 2-인스턴스 분리 (포커스 탈취 회피)
- `따라붙기버그.md` — HWP 출제 시 버퍼 오염 버그
- `출제_불일치_감지.md` — validate 드롭 감지
- `feedback_parity_doctrine.md` — 서버≈로컬 동등성 원칙
- `feedback_restart_after_edit.md` — 코드 수정 후 항상 재시작

---

**끝.**

이주 진행은 메인 Claude의 검토 후 시작.
