# 배포용 설계도 (.exe)

> 본 문서는 .exe 배포 시스템 설계만 다룬다.
> 로그인 시스템은 `로그인시스템설계도.md` 참조.

---

## ⚠️ 운영 역할 분리 정책 (2026-05-23 확정)

> **서버 이전 후 거점이 분리된다. .exe 배포 정책에도 영향.**

| 기능 | 거점 | 비고 |
|---|---|---|
| **문제 출제** | 🌐 **SC (학원 서버)** | 브라우저 — 맥/윈도우 무관 |
| **문제 등록 (인덱싱)** | 💻 **로컬** (학원 PC) | `backend/main_gui.py` + HWP COM (Windows 전용) |

→ **.exe 배포는 관리자(인덱싱 담당) 만 사용**할 가능성이 높음. 출제 선생님들은 브라우저로 충분.
→ 자세한 내용은 `서버클라이언트_설계도.md` § 0-B 참조.

---

## 1. 결정 사항 요약 (2026-04-30)

| 항목 | 결정 |
|---|---|
| 코드 난독화 | **PyArmor 무료판** (강도 중) |
| admin SDK 키 | 빌드에서 **분리** (백엔드/별도 채널) |
| 빌드 트리거 | **`build.bat` 1버튼** |
| 버전 관리 | **자동 +1** (빌드 시 patch 증가) |
| 업데이트 배포 | **수동** (선생님이 .exe 직접 전달) |
| HWP 설치 | **사용자 직접** (스크립트/번들 없음) |
| 캐시 폴더명 | `%APPDATA%\naegiwangbank\` |
| 1년 만료 | **없음** (PC 인증으로 대체) |

---

## 2. PC 인증 (계정 도용 방지)

### 정책
- **1 계정 = 1 PC 고정**
- 첫 로그인 시 해당 PC GUID 자동 등록 (확인창 없음)
- 다른 PC 로그인 시도 → "이 계정은 다른 컴퓨터에서 사용 중입니다" 차단
- **관리자(선생님) 계정은 면제** (어디서든 로그인 가능)

### PC 식별 방법
- **Windows Machine GUID** (`HKLM\SOFTWARE\Microsoft\Cryptography\MachineGuid`)
- 강도: 약 (포맷 시 바뀜) — 의도적으로 이 정도면 충분

### PC 변경 처리 (HDD 교체·포맷·새 컴퓨터)
- 사용자가 관리자에게 요청
- 관리자가 Firestore Console에서 해당 사용자 doc의 `device_guid` 필드 삭제
- 사용자 다음 로그인 시 새 PC GUID 자동 등록

### Firestore 스키마 추가
```
users/{uid}: {
  ..기존 필드..,
  device_guid: "ABC123-..." | null,   // null = 미등록
  device_registered_at: timestamp | null,
}
```

### 코드 흐름 (의사 코드)
```python
def login_check_device(uid, role):
    if role == "admin":
        return True   # 관리자 면제
    current_guid = read_machine_guid()
    user_doc = db.users.get(uid)
    saved_guid = user_doc.get("device_guid")
    if saved_guid is None:
        # 첫 로그인 → 자동 등록
        db.users.update(uid, {"device_guid": current_guid,
                              "device_registered_at": now()})
        return True
    if saved_guid == current_guid:
        return True
    raise LoginBlocked("이 계정은 다른 컴퓨터에서 사용 중입니다.\n"
                       "기기 변경이 필요하면 선생님에게 문의하세요.")
```

---

## 3. 빌드 시스템

### 구성
- **`build.bat`** — 1버튼 빌드
- **PyInstaller** (--onefile, --windowed)
- **버전 자동 증가** — `version.txt` (`MAJOR.MINOR.PATCH` → 빌드 시 PATCH +1)
- **admin SDK 키 제외** — `.gitignore` + PyInstaller `--exclude` 명시

### 빌드 산출물
```
dist/
  내기왕문제은행_v1.0.42.exe      ← 사용자에게 전달
  build_log_20260430_1530.txt
```

### `build.bat` 흐름
1. `version.txt` 읽기 → PATCH +1 → 저장
2. PyInstaller 실행
3. 결과 `dist/내기왕문제은행_v{version}.exe`로 rename
4. 빌드 로그 저장
5. 완료 메시지 출력

---

## 4. admin SDK 키 분리 (Phase 2, 2026-05-01 완료)

### 채택 절충안
원래 설계는 "클라이언트 = Web SDK / admin SDK 키는 선생님 PC만"였으나,
firestore_engine.py 4-A/B/C/D + Phase 5 전면 재작성 부담이 커서
**키만 .exe 번들에서 분리**하는 절충안으로 진행. 완전 Web SDK 전환은 **Phase 6**로 분리.

### 보호 효과
- .exe **단독** 유출 시 admin 권한이 같이 새지 않음 (키 파일은 별도 채널)
- 키 + .exe **둘 다** 유출되면 admin 권한 노출 → 그래서 zip 전달 시 USB 도난 등 주의 필요
- 완전 차단은 Phase 6 (Web SDK 전환) 에서

### 키 탐색 우선순위 (`backend/firebase_init.py`)
1. 환경변수 `NAEGIWANGBANK_FIREBASE_KEY` 가 가리키는 파일
2. **`.exe 옆`** (frozen) / 프로젝트 루트 (dev) ← **정통 배포 위치**
3. `%APPDATA%\naegiwangbank\firebase-key.json` (사용자별)
4. `backend/<adminsdk>.json` (구버전 호환)

각 위치에서 `firebase-key.json` 또는 `naegiwangbank-firebase-adminsdk-*.json` 매칭.

### 배포 시나리오
- 빌드: `build.bat` → `dist/naegiwangbank_v{ver}.exe`
- 키 배치: `dist/firebase-key.json` 복사
- zip: 두 파일을 함께 zip → USB/메일/공유폴더로 사용자에게 전달
- 사용자: zip 풀어서 `.exe 옆에 firebase-key.json` 있는 상태로 실행 → 자동 발견

### 누락 시 동작
키 못 찾으면 GUI 진입 전에 `messagebox.showerror("Firebase 키 누락", ...)` 표시 →
탐색한 6개 후보 경로 안내 + "관리자에게 키 파일을 요청하세요" 안내 → `sys.exit(1)`.

---

## 5. 업데이트 배포 (수동)

- 빌드 후 산출 .exe를 선생님이 직접 사용자에게 전달 (USB/메일/공유 폴더)
- 자동 업데이트 없음
- 버전 표시: 프로그램 좌상단 또는 홈 탭에 `v1.0.42` 노출

---

## 6. 캐시 폴더

- 위치: `%APPDATA%\naegiwangbank\`
  - `cache.db` — 문제 캐시
  - `local_settings.json` — 사용자별 설정
- 배포 시 빈 폴더 생성, 첫 로그인 후 incremental_sync로 채워짐

---

## 7. 코드 난독화 (PyArmor 무료판)

- **강도**: 중 — 일반 사용자가 .exe 내부 파이썬 코드를 들여다볼 수 없음
- **비용**: 무료 (학원/제자 배포 용도라 충분)
- **build.bat 통합**: PyInstaller 직전에 `pyarmor gen backend/` 한 줄 추가
- **이후 업데이트**: 똑같이 1버튼, 추가 손작업 없음
- **제약**: 무료판은 일부 기능에 만료 제한이 붙을 수 있음 → 빌드 시 옵션 점검 필요

## 8. 미결 항목

- (없음 — 모든 결정 완료, 2026-04-30)
